AGENT: A15b-tests (Conversas suite)
RANGES: all 28 assigned tests/test_conversas_*.py reviewed in full (admin_role 1-240; agent_timeout 1-376; assignment_notes 1-219; audio 1-274; auth_local 1-171; hotfix_filters_resp 1-246; inbox_filters 1-498; media_foundation 1-247; media_storage 1-308; media_types 1-236; mobile_pwa 1-209; mobile_responsive_refinement 1-245; notifications 1-205; operational_state 1-461; outbound 1-175; outbound_integrity 1-427; queue 1-198; quick_replies 1-165; security 1-60; service_window 1-608; smoke_seed 1-188; tags 1-181; tags_sync 1-240; template_curation 1-430; template_param_map 1-493; template_variables 1-496; variables 1-1043; webhook 1-237) — none uncovered.

CRITICAL || CONFIRMED || coverage-gap-security || tests/test_conversas_webhook.py || 31 || 31 || META_APP_SECRET || The webhook ONLY authentication is the Meta HMAC, and NO assigned test ever exercises it: every single Conversas test sets META_APP_SECRET = "", routing receive_webhook down the else: branch that logs a warning and processes the body UNSIGNED || os.environ["META_APP_SECRET"] = "" at webhook test line 31, identical in all 13 app-booting Conversas tests; webhook.py:107-119 || _verify_meta_signature, the 403 on a forged/absent signature, the constant-time compare, and the 500 fail-closed when the secret is missing outside development are ALL DEAD CODE IN CI. An internet-facing endpoint that writes to the DB and triggers outbound sends has ZERO regression coverage on its authentication || Add a test file that sets META_APP_SECRET to a fixture and asserts valid->200, tampered->403, absent->403, wrong-prefix->403, and non-development with empty secret->500
CRITICAL || CONFIRMED || test-encodes-wrong-behaviour || tests/test_conversas_service_window.py || 418 || 445 || inbound/TIPOS loop || The suite CENTRAL 24h-window claim is asserted against a payload whose Meta timestamp is the string "1" (epoch 1970-01-01), yet the test asserts the window is OPEN afterwards. It therefore does not test the 24h window at all - it PINS the implementation use of server receipt time || "timestamp": "1" at 421-422; make_conv(numero, ago_hours=30) then check(service_window_open is True) at 442-445; webhook.py:399/408 sets last_customer_msg_at = datetime.now(tz.utc) while timestamp is read into a local at 363 and NEVER USED || The test ACTIVELY BLOCKS THE FIX for the confirmed anchor bug and would FAIL for all 10 message types if the anchor were corrected to the Meta-supplied timestamp || Anchor last_customer_msg_at on the Meta timestamp, then change these assertions to a realistic recent timestamp and add a case with an old timestamp asserting the window stays CLOSED
HIGH || CONFIRMED || test-encodes-wrong-behaviour || tests/test_conversas_outbound_integrity.py || 378 || 387 || SIMULADO block || The test EXPLICITLY ASSERTS THE FAIL-OPEN CREDENTIAL PATH: {"simulated": True} (returned whenever META_ACCESS_TOKEN/META_PHONE_NUMBER_ID are absent) must produce HTTP 200 and a persisted Message with status == "sent". NOTHING WAS SENT TO ANYONE || SIM_RESPONSE at 75; check(r_sim.status_code == 200) at 383; check(sim_msg.status == "sent" and whatsapp_msg_id is None) at 385-386; outbound.py:48-50 and whatsapp.py:111/213/252/334 || A production deploy with a rotated/expired/unset META_ACCESS_TOKEN SILENTLY MARKS EVERY OUTBOUND MESSAGE "sent". Operators see a normal history, customers receive nothing, and the only signal is an INFO log. This test makes that a LOCKED-IN CONTRACT, so fixing it requires deleting assertions || Gate the simulated branch on development and fail closed in production; assert a status that is NOT "sent"
HIGH || CONFIRMED || mock-erases-bug || tests/test_conversas_variables.py || 550 || 571 || _send_media || This file mocks ONLY whatsapp.send_text_message and never touches upload_media/send_media_message, while its env block sets no META_ACCESS_TOKEN. The multipart media send therefore runs the REAL whatsapp.py which returns simulated, and the test asserts 200/success on that path. It also WRITES A REAL FILE OUTSIDE scratch/ || the _send_media assertions at 568-571; whatsapp.py:213; artefact observed on disk: conversas/uploads/media/asset_1.png because the file never overrides CONVERSAS_MEDIA_DIR || The "media caption resolves variables" guarantee is proven only for a code path that NEVER CONTACTS META; any regression in upload/send request construction for media is invisible || Mock upload_media/send_media_message explicitly and set CONVERSAS_MEDIA_DIR under scratch/
HIGH || CONFIRMED || test-blocks-fix || tests/test_conversas_service_window.py || 588 || 589 || _require_open_window count || The guard is verified by an EXACT SOURCE-TEXT OCCURRENCE COUNT of 3. Adding a fourth free-form outbound route that CORRECTLY calls the guard turns the test RED; removing the guard from one route and adding a decoy string keeps it GREEN || check(py.count("_require_open_window(conversation)") == 3, ...) || DIRECTLY PENALISES CORRECT HARDENING. A developer adding a new free-form endpoint sees a failing test whose message says the guard is missing, when it is present - the path of least resistance is to skip the guard or edit the assertion || Replace with behavioural assertions (the file already does this for 3 routes at 245-273) and delete the count
HIGH || CONFIRMED || env-divergence-concurrency || tests/test_conversas_queue.py || 131 || 137 || claim 409 || The anti-double-attendance lock is only tested SEQUENTIALLY inside one TestClient process on SQLite || the sequential 409 assertion; conversations.py:950-967 is a check-then-act with no with_for_update() and no DB constraint; grep for FOR UPDATE over conversas/app returns NOTHING || On production Postgres with multiple workers two simultaneous claims both read atendente_id IS NULL and BOTH SUCCEED - last write wins, no 409, TWO AGENTS ON ONE CONVERSATION. The exact production incident the test is named for is UNREACHABLE by this test. Same shape for /release and /retry || Make the claim atomic (conditional UPDATE, 409 on rowcount 0) and test it with two concurrent sessions
HIGH || CONFIRMED || env-divergence-idempotency || tests/test_conversas_webhook.py || 108 || 148 || CASO 2 / CASO 2b || Webhook idempotency is proven only for serialised single-process SQLite. _process_incoming_message is SELECT-then-INSERT on whatsapp_msg_id; the column IS unique on the model, so on Postgres a concurrent Meta retry that LOSES the race raises IntegrityError - which escapes to the router blanket except (webhook.py:145-149) that logs and STILL RETURNS 200, aborting every remaining message in the batch || the serialised dedup assertions; webhook.py:360-364 and 410; webhook.py:145-149 || On Postgres a duplicate delivery can SILENTLY DROP the legitimate NEW messages that shared the batch, with 200 returned to Meta so nothing is retried - PERMANENT MESSAGE LOSS, precisely the failure class CASO 4/5 were written to prevent || Catch IntegrityError per-message, rollback+continue, and add a test that pre-inserts the row and asserts the sibling message still persists
HIGH || CONFIRMED || ci-misconfiguration || tests/test_conversas_security.py || 1 || 60 || module || This file contains NO CONVERSAS_DIR literal, so the workflow selector puts it in the CRM job (Python 3.11 + requirements.txt), not the conversas job || verified by running the workflow own selector; the file defines only ROOT and reads conversas/static/js/conversas.js; the workflow comment claims the discriminator separates "34 arquivos" while the repo now has 51 || It passes today only because it never imports app.*. The MOMENT anyone adds a Conversas import it fails or, WORSE, resolves app to the CRM package and asserts against the WRONG CODEBASE. The workflow comment documenting the invariant is already false || Add a CONVERSAS_DIR binding and refresh the stale comment
HIGH || CONFIRMED || silent-skip || tests/test_conversas_mobile_responsive_refinement.py || 223 || 233 || RESP02_MERGE git range || The no-backend-changes guard shells out to `git diff 12e4f97^1 12e4f97^2` wrapped in an except Exception that prints SKIP and adds NOTHING to failures || the subprocess call and the bare except || It vanishes silently whenever: fetch-depth is not 0 (CI pins fetch-depth: 0 SOLELY for this one guard), main is rebased/squashed so the commit is GC'd, git is missing, or the 30s timeout trips. The check is also PINNED TO A FROZEN HISTORICAL RANGE so it can never detect a NEW backend change - a whole-repo CI cost paid for an assertion about immutable history || Delete the guard or convert it to a live merge-base scope check; if kept, catch only the specific errors and RECORD A FAILURE
HIGH || CONFIRMED || coverage-gap || conversas/app/services/crm.py || 156 || 273 || auto_link_conversation || EVERY CRM write path is Postgres-only SQL and is STUBBED OUT in every test that could reach it || crm.py:193 uses '{...}'::jsonb and :156/238/252/272 use NOW(); _noop_false replaces auto_link_conversation in webhook, outbound_integrity, audio, media_foundation, media_storage, media_types, queue, operational_state and service_window tests || The ENTIRE CRM-integration boundary - including the CRM-down partial-failure path where a conversation is created but the lead insert fails mid-transaction - has ZERO EXECUTABLE COVERAGE and is STRUCTURALLY UNTESTABLE in the SQLite-only harness (SQLite rejects ::jsonb) || Add a Postgres-backed job for the crm.py writer paths, or route those inserts through SQLAlchemy Core so they are dialect-portable
HIGH || CONFIRMED || coverage-gap || conversas/app/routers/webhook.py || 364 || 378 || duplicate conversation creation || conversations.whatsapp has NO unique constraint and the webhook find-or-create is a plain SELECT-then-INSERT. NO assigned test covers two concurrent first-contact messages from the same number || conversation.py:45 index=True with no unique (contrast Message.whatsapp_msg_id at 107 which HAS it); webhook.py:369-378 || On Postgres with multiple workers, a burst of first messages from one number produces DUPLICATE conversation rows. All downstream reads use .first(), so HALF THE CUSTOMER MESSAGES land in an invisible second conversation. Nothing in the suite would notice || Unique index on conversations.whatsapp plus a dedup migration and a concurrency test
MEDIUM || CONFIRMED || cannot-fail || tests/test_conversas_inbox_filters.py || 176 || 176 || todos independe do usuario || The assertion COMPARES AN EXPRESSION TO ITSELF, so it is true for every possible implementation; it is also executed while _U2 is the authenticated user so it fails to compare across users the way its message claims || check(names("todos") == names("todos"), "todos independe do usuario") || A named invariant reports PASS while testing NOTHING. If todos were made to filter by atendente_id = current_user, this line still passes || Capture the list as the first user before the switch and assert equality across the two
MEDIUM || CONFIRMED || test-encodes-wrong-behaviour || tests/test_conversas_service_window.py || 595 || 599 || B - BIA intocados || The suite asserts the 24h guard must be ABSENT from webhook.py and whatsapp.py, FREEZING the design in which every auto-reply, BIA response and n8n fallback sends free-form text with no window check || check("_require_open_window" not in wh_py, ...) and check("service_window_open" not in wa_py, ...) || Mostly benign, but NOT for the reaction path: webhook.py:346 sets opens_window = False for reactions while _send_auto_reply_if_needed still runs, so a reaction after 24h of silence triggers a free-form send into a CLOSED window. Also applies to the debounced n8n reply (up to 15s+240s later). Adding a guard would turn these assertions RED || Guard the auto-reply and agent forward with service_window_open, then invert these into behavioural tests
MEDIUM || CONFIRMED || brittle-grep || tests/test_conversas_service_window.py || 572 || 584 || guards do JS || Three frontend guards match EXACT SOURCE TEXT including literal newlines and hard-coded 8-space indentation || the guard strings at 572-574 and the tplSending check at 583; same pattern at test_conversas_inbox_filters.py:468-469 || They break on any reformat that PRESERVES BEHAVIOUR, and pass unchanged if windowClosed() itself returns the wrong value. High-friction false failures train developers to edit assertions rather than investigate, while the real behaviours remain unprotected because NO JS IS EVER EXECUTED || Headless-DOM or Playwright check; at minimum normalise whitespace before matching
MEDIUM || CONFIRMED || mock-erases-bug || tests/test_conversas_outbound.py || 87 || 133 || _DummyUser/_fake_ok || The canonical outbound test overrides get_current_user with a class literal and REPLACES THE FUNCTION UNDER TEST wholesale || dependency_overrides[get_current_user] = lambda: _DummyUser() at 93 and whatsapp.send_text_message = _fake_ok at 121-125; the same override pattern appears in 20 of the 28 assigned files || NEITHER the authorization of /messages NOR the construction of the real Meta request (URL, messaging_product, Authorization header, body shape, timeout) is ever checked. A malformed Meta payload passes EVERY outbound test in the suite. The only place any real request body is inspected is test_conversas_media_storage.py:296-300 || Add a request-level test that patches httpx.AsyncClient and asserts URL, headers and JSON body
MEDIUM || CONFIRMED || env-divergence-timezone || tests/test_conversas_service_window.py || 227 || 236 || naive/aware normalisation || The naive-datetime case is exercised only as a direct Python call plus one SQLite round-trip. On production Postgres the column is TIMESTAMPTZ and returns tz-AWARE values, so service_window_open takes a DIFFERENT BRANCH than it ever does in CI || the naive assertion at 228-232; conversation.py:26-27 documents the split; webhook.py:157-163 hard-codes a fixed UTC-3 offset with no DST test || The branch that ACTUALLY RUNS IN PRODUCTION is the untested one. A session-timezone misconfiguration shifts the whole 24h window silently and CI cannot see it; business-hours auto-replies are off by an hour across DST transitions || Parametrise over naive and aware including a non-UTC tzinfo, and replace the fixed offset with zoneinfo plus a DST-boundary test
MEDIUM || CONFIRMED || coverage-gap || tests/test_conversas_webhook.py || 165 || 177 || CASO 3 status update || Delivery-status handling is tested ONLY for the single happy transition sent -> delivered on an existing wamid || the single assertion at 175-176 || Out-of-order callbacks (delivered after read), unknown/failed statuses with Meta error payloads, and statuses for an unknown wamid are ALL UNTESTED, so _process_status_update has no guard against a late delivered overwriting a terminal read. Meta explicitly does not guarantee ordering. A failed status carrying an error code has no asserted behaviour at all || Add out-of-order, failed-with-payload and unknown-wamid cases
MEDIUM || CONFIRMED || grep-only-test || tests/test_conversas_security.py || 34 || 55 || SEC-CONV-01 checks || The ONLY XSS test in the Conversas suite is four string/regex greps over conversas.js. It executes NO JavaScript and covers exactly ONE sink || the two .replace substring checks at 34-37 and the src regex at 46/50; the loop at 50-55 runs ZERO assertions if the regex finds nothing, and its [^}]* body cannot match any interpolation containing a nested brace || UNPROTECTED: escaping of message BODY text, href/onclick/data-* sinks, innerHTML assembly outside the marker-delimited sections, and whether escapeHtml actually NEUTRALISES a payload. A rename that kept a broken escapeHtml still passes || jsdom/Playwright test rendering a real payload; keep the greps only as a supplement
MEDIUM || CONFIRMED || flakiness || tests/test_conversas_operational_state.py || 209 || 250 || time.sleep FIFO ordering || FIFO ordering is established by FOUR time.sleep calls (0.05, 0.02, 0.02, 0.05) relied upon to produce distinct queued_at values; test_conversas_queue.py:109 does the same for last_customer_msg_at || the sleeps then the sorted-order assertion at 249-250 || Passes on today runners; a coarse clock or a loaded CI host collapses two timestamps and the FIFO assertion flips. The tie-break-by-id path that would save it is only tested for NULL queued_at, not for EQUAL non-NULL timestamps || Set queued_at explicitly to fixed spaced datetimes and add an equal-timestamp case
MEDIUM || CONFIRMED || brittle-assertion || tests/test_conversas_hotfix_filters_resp.py || 223 || 223 || todas retorna 6 || Several suites assert EXACT CUMULATIVE ROW COUNTS that depend on every preceding block in the file || check(todas6["total"] == 6, ...); also test_conversas_service_window.py:364 and test_conversas_inbox_filters.py:299 (total == 252) || Inserting a fixture anywhere above SILENTLY BREAKS an unrelated assertion far below; failures point at the wrong place and the pressure is to bump the magic number rather than investigate || Scope the counts by a per-block marker or assert deltas
MEDIUM || CONFIRMED || test-hygiene || tests/test_conversas_media_storage.py || 215 || 216 || outside file + storage dirs || Tests leave artefacts behind: a bare file written into scratch/ never removed, four media directories that accumulate across runs, and test_conversas_variables.py writing into conversas/uploads/media/ || outside.write_text with no cleanup; leftover dirs observed: scratch/media_storage_test, media_audio_test, media_types_test, smoke_seed_media; plus conversas/uploads/media/asset_1.png OUTSIDE scratch entirely || DB files are recreated per run but media directories are NOT - a stale asset from a previous run can satisfy an existence assertion for an asset the current run FAILED to write || shutil.rmtree alongside the existing DB unlink, and set CONVERSAS_MEDIA_DIR under scratch/
MEDIUM || CONFIRMED || mock-erases-bug || tests/test_conversas_operational_state.py || 137 || 138 || _schedule_agent_debounce stub || The BIA scheduling assertions REPLACE THE SCHEDULER ITSELF with a list-append lambda, so the tests prove only that a call site is reached || wh._schedule_agent_debounce = lambda cid: scheduled.append(cid) with assertions at 195, 220, 378 || The real 15s debounce - task cancellation on a second inbound, the _debounce_tasks dict, task leakage, and the interaction with the 240s agent timeout - is NEVER EXECUTED. _debounce_tasks is process-global mutable state with no coverage of cancellation or cleanup; a leaked task means DUPLICATE BIA REPLIES, or a reply for a conversation a human has since claimed || Focused async test with a shrunk debounce constant asserting cancellation and dict cleanup
LOW || CONFIRMED || redundant-assertion || tests/test_conversas_template_variables.py || 257 || 258 || nao carrega o token literal || Assertions logically IMPLIED by the immediately preceding equality check that can never fail independently || check(meta_params() == ["Joao"]) immediately followed by check(meta_params() != ["@PRIMEIRONOMECLIENTE"]); same at test_conversas_template_param_map.py:344 and test_conversas_notifications.py:171 || Inflates the PASS count and the apparent strength of the suite without adding discrimination, making "N checks passed" a poor proxy for coverage || Drop the implied assertions or make them independent
LOW || CONFIRMED || test-hygiene || tests/test_conversas_variables.py || 429 || 431 || unclosed sessions || SessionLocal() instances are constructed INLINE AS CALL ARGUMENTS and never closed, in a file that opens and closes sessions correctly everywhere else || variables_service.render(SessionLocal(), ..., VariableContext(SessionLocal())) - two sessions created, neither closed || Harmless on SQLite in a short-lived process; on a pooled Postgres engine the same pattern EXHAUSTS THE POOL. It is also the pattern a developer will copy into production code || Bind to locals and close, or use a with block

AGENT NOTES — SILENT-SKIP / VACUOUS CONDITIONS, EXHAUSTIVELY:
1. test_conversas_mobile_responsive_refinement.py:225-233 - try/except Exception around a git diff of a frozen range; prints SKIP, records NO failure. Triggers on shallow clone (CI pins fetch-depth: 0 solely to prevent this), history rewrite/GC of the commit, missing git, 30s timeout, or non-zero exit. Verified today the commit resolves, so it currently runs.
2. test_conversas_security.py:50-55 - the finditer loop executes ZERO assertions if the pattern matches nothing; the [^}]* body also cannot match interpolations containing braces.
3. test_conversas_variables.py:1030-1035 - qr_section derived from two find() calls; on marker loss the slice is empty and the "innerHTML not in qr_section" assertion passes VACUOUSLY (an adjacent assertion does fail, so the file still exits non-zero). Same shape at 986-994 and 1009-1020, and at test_conversas_notifications.py:140-141 and 155-156.
4. test_conversas_inbox_filters.py:176 - tautology, never fails under any implementation.
NO env-var or import-guarded skips exist; every file runs its body unconditionally.
CRITICAL CONVERSAS FLOWS WITH NO REGRESSION TEST: webhook HMAC signature verification (ALL modes); concurrent duplicate webhook delivery hitting the DB unique constraint (Postgres only); duplicate conversation creation; claim/handoff/release/retry under concurrency (all four check-then-act, every 409 test sequential); out-of-order and failed delivery-status callbacks; XSS of message BODY text and every non-src DOM sink (no JavaScript is ever executed by any test); CRM-down partial failure (auto_link_conversation is _noop_false in all 9 tests that reach it, and its SQL cannot run on SQLite at all); real Meta request construction for text and template sends; the n8n debounce scheduler itself.
STRONGEST PARTS OF THE SUITE (keep): media path traversal at the serve layer (test_conversas_media_storage.py:213-230), filename sanitisation (test_conversas_media_types.py:158-164), and template param arity/mismatch (test_conversas_template_param_map.py, test_conversas_template_variables.py) are genuine behavioural tests.
OVERALL SHAPE: unusually thorough on single-process business logic (variables, template param mapping, curation, outbound integrity), and unusually weak on exactly the three axes where this system will actually break - authentication of the webhook, concurrency on Postgres, and anything the browser executes.
NOT VERIFIED: actual Postgres behaviour (unique violations, TIMESTAMPTZ round-trip, ILIKE on accented text, isolation, COUNT FILTER plans); concurrency claims are static reasoning over check-then-act sequences, no race was executed; the suite was not re-run; whether production tables were created via create_all (carrying the whatsapp_msg_id UNIQUE) or via the migrations.
