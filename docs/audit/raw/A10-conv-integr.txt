AGENT: A10-conv-integr
RANGES: webhook.py 1-700; whatsapp.py 1-406; meta_templates.py 1-720; outbound.py 1-210; crm.py 1-509; variables.py 1-631; media_storage.py 1-171; media_policy.py 1-96; services/__init__.py 1-1 — all reviewed, none uncovered.

HIGH || CONFIRMED || error-handling/data-loss || conversas/app/routers/webhook.py || 130 || 149 || receive_webhook || The whole entry/changes loop is wrapped in one bare except Exception that logs then unconditionally returns 200, so any failure aborts every REMAINING message and status update in the same Meta payload and tells Meta the delivery succeeded || except Exception as e: logger.error(...) then comment Always return 200 to Meta and return status ok; no per-message try, no dead-letter, no re-raise || Inbound customer messages permanently lost with no Meta retry and no record except a log line; one poison message silently drops the rest of its batch || Wrap each _process_incoming_message/_process_status_update in its own try/except and persist failures to a dead-letter table
HIGH || CONFIRMED || resource-exhaustion || conversas/app/routers/webhook.py || 512 || 557 || _debounce_then_forward || A SessionLocal() is opened before _forward_to_agent and closed only in finally, held open across the n8n agent HTTP call whose read timeout is 240s; pool is pool_size=5 max_overflow=10 || db = SessionLocal() (512) ... await _forward_to_agent(...) (553) ... finally db.close() (557); AGENT_TIMEOUT = httpx.Timeout(240.0, connect=10.0) (42) || 15 concurrent Bia conversations exhaust the pool; every subsequent webhook POST blocks on checkout then 500s, Meta retries, inbound processing stops system-wide || Close the session before the agent call and reopen a short one to persist the reply, or move the agent call off the request session
HIGH || CONFIRMED || duplicate-send || conversas/app/routers/webhook.py || 497 || 553 || _debounce_then_forward/_forward_to_agent || The task removes itself from _debounce_tasks after the sleep and BEFORE the agent call, so a message arriving during the 1-4 minute agent call finds no task to cancel and schedules a second concurrent run; the second run cannot see the first run replies because they are recorded with commit=False || _debounce_tasks.pop(conversation_id, None) (509) before await _forward_to_agent (553); last_outbound query (528-531); record_outbound_message(..., commit=False) (670-673) with db.commit() only at 683 || Two Bia executions run concurrently on the same pending messages and both send to the real customer - duplicated interleaved replies plus duplicated tool side effects || Keep an in-flight marker per conversation_id for the whole duration of _forward_to_agent, not just the sleep
HIGH || HIGH || availability/data-loss || conversas/app/routers/webhook.py || 68 || 70 || _debounce_tasks || The 15s reply debounce lives only in a module-level dict of asyncio tasks with no persistence, no shutdown drain and no recovery, while compose runs the container with restart: always || AGENT_DEBOUNCE_SECONDS = 15; _debounce_tasks: dict[int, asyncio.Task] = {} (69-70); asyncio.create_task(_debounce_then_forward(...)) (493); no lifespan shutdown hook || Any deploy, crash or restart in the debounce/agent window silently drops the reply: the customer wrote and never gets an answer and nothing flags the conversation || Persist the pending-forward intent and reconcile on startup, or drain outstanding tasks in a lifespan shutdown handler
HIGH || HIGH || webhook-timeout/retry-storm || conversas/app/routers/webhook.py || 437 || 451 || _process_incoming_message || After committing the message the handler still awaits, inline and sequentially per message, mark_as_read (10s), the CRM auto-link (multiple sync SQL round trips incl. lead+funnel+tag INSERTs) and _send_auto_reply_if_needed (15s Meta send), all before returning 200; every DB call is blocking SQLAlchemy inside async def on a single uvicorn worker || await whatsapp.mark_as_read (438), await crm_service.auto_link_conversation (442), await _send_auto_reply_if_needed (447) - worst case over 25s per message times the batch size || Meta times the delivery out and retries; meanwhile the blocking sync DB calls stall the event loop so unrelated webhooks and API requests queue behind it || Return 200 right after committing the inbound row and move the rest to the existing background path
HIGH || CONFIRMED || cross-customer-data-leak || conversas/app/services/crm.py || 31 || 45 || lookup_lead_by_whatsapp || The lead is matched with whatsapp LIKE percent last-10-digits percent and auto_link_conversation writes that lead id permanently onto the conversation; variables.py then resolves CLIENTE tokens from that lead_id, and its own docstring names this function as unsafe for identity || suffix = normalized[-10:]; WHERE whatsapp LIKE :pattern LIMIT 1 (33-45) then conversation.lead_id = lead_data.get("id", 0) (335); variables.py:226-229 states it deliberately does NOT use lookup_lead_by_whatsapp because it could return another customer lead || A wrong lead binding sends customer B name and e-mail to customer A through template parameters and auto-replies and mis-attributes the whole conversation history || Match on a normalized exact number, refuse to auto-link when more than one lead matches
HIGH || HIGH || stored-xss || conversas/app/routers/webhook.py || 418 || 427 || _process_incoming_message media_meta || The mime_type of the inbound media payload is persisted verbatim into MediaAsset.meta_mime_type with no media_policy validation (media_policy is not imported here); store_bytes never overwrites an already-set value; routers/media.py serves the file with that value as Content-Type and content_disposition_type=inline || webhook.py:310 meta_mime_type from media_data then 420-427 MediaAsset(...); media_storage.py:115-116 if mime_type and not asset.meta_mime_type; media.py:80-84 FileResponse(media_type=asset.meta_mime_type or octet-stream, inline) || A customer using a non-standard WhatsApp client can declare an HTML/SVG mime on a polyglot image and the operator console renders it as script in the app origin - session/JWT theft on the agency inbox || Validate the declared mime with media_policy at ingestion, make the validated mime authoritative in store_bytes, serve unknown types as application/octet-stream with attachment disposition
MEDIUM || CONFIRMED || 24h-window-correctness || conversas/app/routers/webhook.py || 293 || 399 || _process_incoming_message || The Meta-supplied message timestamp is extracted and never used; the 24-hour service window is anchored on the server receipt time instead || timestamp = msg.get("timestamp", "") (293) is dead; window set from datetime.now(tz.utc) at 380 and 399 || Delayed webhook delivery makes our window later than Meta so service_window_open reports open while Meta rejects the free-form send with 131047; the reverse skew forces an unnecessary paid template || Anchor last_customer_msg_at on int(msg timestamp) in UTC, falling back to now() only when absent/unparseable
MEDIUM || CONFIRMED || idempotency/out-of-order || conversas/app/routers/webhook.py || 454 || 466 || _process_status_update || Status updates are applied unconditionally with no ordering rank and no replay guard, unlike inbound messages which are deduped by message_id at 359-363 || message.status = new_status; db.commit() with no comparison and no use of the update timestamp || Meta delivers statuses out of order and retries them so a late delivered overwrites read; a replayed captured payload (the HMAC is valid forever, no nonce) resets statuses at will; a failed overwritten by a stale sent hides a non-delivery || Apply a status rank (sent < delivered < read, failed terminal) and ignore backwards transitions
MEDIUM || CONFIRMED || lost-status || conversas/app/routers/webhook.py || 661 || 683 || _forward_to_agent || Agent reply parts are persisted with commit=False and committed only after the whole loop, which includes a 1.2s sleep between parts, so Meta status callbacks for early parts arrive while their rows are uncommitted and _process_status_update silently finds nothing || record_outbound_message(..., commit=False) (670-673), db.commit() at 683, asyncio.sleep(1.2) at 667; _process_status_update has if message: with no else || Delivery and especially failed statuses for Bia replies are permanently lost - a message WhatsApp rejected still shows as sent in the inbox || Commit each part right after recording it, and log unmatched wamids instead of returning silently
MEDIUM || CONFIRMED || wrong-template-identity || conversas/app/services/meta_templates.py || 596 || 613 || sync_template_statuses || The Meta lookup is keyed by name alone, contradicting this module own rule that template identity is (name, language); with the same name in two languages the last page entry wins || meta_lookup[name] = {...} (600) and meta_info = meta_lookup.get(lt.name) (613) vs header comment 27-29 || A local template can be marked APPROVED and given the meta_template_id of a different language variant, so the send path believes an unapproved template is sendable and the customer receives the wrong language || Key meta_lookup on (name, language)
MEDIUM || CONFIRMED || destructive-scope || conversas/app/services/meta_templates.py || 534 || 556 || delete_template_on_meta || Deletion is issued with only name= which per the Graph API deletes every language variant of that template on the WABA; the function takes no language argument || params = {"name": template_name}; client.delete(url, headers, params) (551-555) || Deleting one language silently revokes all translations on Meta, taking every localized send offline until recreated and re-approved || Accept a language and pass hsm_id/language so deletion targets the exact pair
MEDIUM || HIGH || ssrf/credential-exposure || conversas/app/services/meta_templates.py || 188 || 198 || _fetch_meta_templates || The pagination loop follows the paging.next URL from the response body with no host validation and no page cap, re-sending the Authorization Bearer Meta token to whatever host that URL names || next_url = data.get("paging", {}).get("next") then client.get(next_url, headers=headers) inside while next_url (190-196) || Response tampering or a bad base makes the WABA access token leave for an arbitrary host; the unbounded loop has no total time budget || Verify next_url scheme/host against base_url before following and cap the page count
MEDIUM || CONFIRMED || false-success || conversas/app/services/whatsapp.py || 108 || 111 || send_text_message and all send_* || With no credentials the client returns simulated True, which classify_wa_response maps to ok=True and record_outbound_message persists as status sent with no marker; _get_credentials silently falls through to env vars whenever ApiConfig.is_connected is false || whatsapp.py:109-111; outbound.py:48-50; outbound.py:103 status = sent if r[ok] else failed || A production instance whose DB config is disconnected and whose env token is empty (compose defaults META_ACCESS_TOKEN to empty) records every reply as delivered while sending nothing - a total silent outbound outage || Persist a distinct simulated status/flag and refuse the simulated path when ENVIRONMENT != development
MEDIUM || CONFIRMED || no-retry || conversas/app/services/whatsapp.py || 126 || 138 || send_text_message/send_template_message/send_media_message || No send path has retry, backoff or handling of 429/5xx; the schema carries send_attempts and last_attempt_at described as the base for retry but nothing ever retries || single attempt then _http_error_summary; outbound.py:105-106 always writes send_attempts=1; no scheduler/worker in the repo || A transient Meta 429/500 permanently drops the reply including the Bia fallback message, whose failure is only logged at ERROR || Bounded retry with exponential backoff for 429/5xx (safe: a failed send produced no message id), increment send_attempts, surface failed messages for operator retry
MEDIUM || CONFIRMED || config-asymmetry || conversas/app/routers/webhook.py || 113 || 121 || receive_webhook || The HMAC app secret is read only from the process env constant while every other Meta credential including the verify token can be overridden from the api_config DB row; ApiConfig has no meta_app_secret column at all || if META_APP_SECRET: (113) vs _get_verify_token(db) (74-79); models/api_config.py:17-22 || Rotating Meta credentials through the settings panel makes every inbound webhook fail with 403 forever with no UI signal and a green healthcheck; a missing env secret 500s every POST (compose defaults it empty with no :? guard) || Read the app secret with the same DB-then-env precedence and surface signature failures as a visible connection-health state
MEDIUM || CONFIRMED || unbounded-download || conversas/app/services/media_storage.py || 151 || 169 || download_media_asset || The pre-download size check relies on Meta self-reported file_size and media_policy.validate skips the size check when it is None; the real check happens only after the whole body was read into memory || media_storage.py:149,153; media_policy.py:90 if size_bytes is not None; whatsapp.py:188-189 response = await client.get(...); return content; post-hoc check at media_storage.py:167 || A missing or understated file_size lets an arbitrarily large body be buffered in RAM before rejection; even the compliant path buffers up to 100 MB per concurrent fetch on a single-worker service || Stream with client.stream and abort past media_policy.max_size_for(kind); treat missing file_size as unknown-and-streamed
MEDIUM || CONFIRMED || disk-exhaustion || conversas/app/services/media_storage.py || 102 || 120 || store_bytes || Files are written to MEDIA_STORAGE_DIR and never deleted anywhere; deleting a Message cascades the media_assets row but orphans the file; no retention policy or quota || path.write_bytes(content) (108) with no counterpart - no unlink/os.remove/rmtree/cleanup anywhere in conversas/; compose mounts conversas_media with no size limit || The media volume grows without bound on a host shared with the PostgreSQL data volume; disk exhaustion takes down CRM and Conversas together || Retention job deleting local mirrors older than N days and unlink on MediaAsset removal
MEDIUM || HIGH || duplicate-records || conversas/app/routers/webhook.py || 367 || 384 || _process_incoming_message || Conversation lookup-then-create is check-then-act with no unique constraint behind it (conversations.whatsapp is indexed, not unique) and no lock; the same race exists in crm.auto_create_lead_in_crm || webhook.py:367-382; models/conversation.py:45 whatsapp = Column(String(30), nullable=False, index=True) with no unique; crm.py:188-198 INSERT INTO leads after only the fuzzy LIKE lookup || Two messages from a new number arriving close together create two conversations and two CRM leads, each with its own debounce task, so Bia answers twice and the inbox splits the history || Unique constraint on the normalized whatsapp number and on the CRM lead number; handle IntegrityError by re-selecting
MEDIUM || HIGH || template-payload-validation || conversas/app/services/variables.py || 516 || 533 || render dynamic value resolution || Resolved variable values are only end-stripped; no removal of newlines/tabs/space runs and no length cap, although Meta rejects parameters containing those || resolved = str(resolved).strip() (528) is the only normalization; values flow to routers/conversations.py:171 then 179-181 into the template payload; sources include conversation.nome populated from the customer-controlled contacts profile name at webhook.py:349-351 || A lead or WhatsApp profile name with a newline or a long space run makes every template send to that customer fail with Meta 132000 - the one path that is supposed to work when the 24h window is closed || Normalize control characters, collapse whitespace runs, enforce Meta per-parameter and body length limits
MEDIUM || CONFIRMED || unbounded-payload || conversas/app/routers/webhook.py || 630 || 637 || _forward_to_agent || Despite the name recent_msgs the query has no limit and no time window: the complete message history is serialized into the n8n payload on every turn || recent_msgs = db.query(Message).filter(conversation_id).order_by(created_at.asc()).all() then a comprehension into payload historico || Ever-growing POST body and LLM context, pushing agent latency toward the 240s timeout that triggers the generic fallback, and growing memory per in-flight forward || Bound the history to last N messages or N hours
LOW || CONFIRMED || business-hours-logic || conversas/app/routers/webhook.py || 174 || 177 || _is_within_business_hours || Open/close times are compared as HH:MM strings in a single <= chain so any schedule crossing midnight can never match || return hours.open_time <= current_time <= hours.close_time (175) with current_time from strftime and String(5) columns || An overnight shift (18:00-02:00) evaluates as closed 24h a day so every customer gets the out-of-hours auto-reply; the fixed UTC-3 offset also assumes Brazil never reinstates DST || Handle wrap-around explicitly and use a real America/Sao_Paulo zoneinfo
LOW || CONFIRMED || verify-handshake || conversas/app/routers/webhook.py || 95 || 97 || verify_webhook || The verify token is compared with plain == (not hmac.compare_digest, unlike the POST path at 65) and the challenge is coerced with a bare int() that raises on non-numeric || if hub_mode == subscribe and hub_verify_token == expected_token and expected_token: return int(hub_challenge) || Non-constant-time comparison of a shared secret and a 500 instead of a clean failure whenever the challenge is not integer-parsable, making resubscription fail opaquely || hmac.compare_digest and return the challenge as a plain string
LOW || CONFIRMED || stale-cache || conversas/app/services/meta_templates.py || 272 || 276 || invalidate_catalog_cache || Documented as used by any credential change but a repo-wide grep finds no non-test caller, so the 5-minute template catalog cache survives credential changes || only its own definition matches || After switching WABA credentials the composer keeps offering the previous account templates for up to 5 minutes and sends fail at Meta || Call it from the api_config/settings save path or key the cache on the credential tuple
LOW || CONFIRMED || wrong-audit-data || conversas/app/services/meta_templates.py || 630 || 638 || sync_template_statuses || In the template-removed branch lt.status is overwritten with PENDING before being read into old_status || lt.status = PENDING (633) then details.append old_status: lt.status (634-638) || The sync report always claims the template was already PENDING, hiding that an APPROVED template disappeared from Meta || Capture old_status before the assignment as the sibling branch at 615 already does
LOW || CONFIRMED || inconsistent-sanitization || conversas/app/routers/webhook.py || 308 || 313 || _process_incoming_message media_meta || The Meta-supplied document filename is stored raw on the inbound path while the outbound path routes the same field through outbound._sanitize_filename; the stored value is emitted in Content-Disposition on download || filename from media_data (312) into MediaAsset (425) vs outbound.py:27-32 used at outbound.py:202; sink media.py:83 || Untrusted unbounded filenames reach the download header and the operator UI; the column is String(255) so an over-long name also risks a write error || Reuse _sanitize_filename at ingestion so both directions share one rule
LOW || CONFIRMED || guaranteed-failed-send || conversas/app/routers/webhook.py || 344 || 451 || _process_incoming_message || A reaction deliberately does not advance the window clock but still runs the full auto-reply and Bia forwarding path, both of which send free-form text || opens_window = msg_type != reaction (344) yet _send_auto_reply_if_needed (447) and _schedule_agent_debounce (450-451) run unconditionally and neither consults service_window_open || A reaction to an old message triggers a free-form send Meta rejects with 131047, persisted as failed for the operator to interpret, and burns a full agent execution || Skip auto-reply and agent forward when opens_window is False and the window is closed
LOW || CONFIRMED || useless-api-call || conversas/app/routers/webhook.py || 291 || 438 || _process_incoming_message || mark_as_read is awaited with msg_id that may legitimately be None (normalized at 291 to avoid a UNIQUE collision), producing a payload with message_id null || msg_id = msg.get("id") or None (291) then await whatsapp.mark_as_read(msg_id, db) (438) || A guaranteed-rejected round trip to Meta inside the latency-critical handler, adding up to 10s to a path already at risk of Meta timeout || Skip the call when msg_id is falsy

AGENT NOTES (verified good, not assumed): signature verified on the RAW body with hmac.compare_digest before any parsing; fail-closed outside development; message-id idempotency runs before any conversation mutation; deliberate absence of agent retry is correct given n8n responseNode mode; media_storage.resolve_local_file is a correct confinement check and local filenames are fully server-generated; variables.render is a genuine single-pass substitution with no rescan; httpx strips Authorization on cross-origin redirects so download_media_content does not leak the token.
NOT VERIFIED: whether Meta passes a client-declared mime unchanged (code path confirmed, attacker-control step inferred); Meta exact webhook deadline/retry schedule; runtime behaviour of the n8n Bia workflow; whether the CRM enforces uniqueness on leads.whatsapp; whether any deployment sets uvicorn --workers > 1.
