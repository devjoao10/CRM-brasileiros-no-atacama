AGENT: A14-devops
RANGES: docker-compose.yml 1-141; Dockerfile 1-24; conversas/Dockerfile 1-22; .dockerignore 1-13; conversas/.dockerignore 1-13; .gitignore 1-175; .env.example 1-115; .github/workflows/test.yml 1-90; .github/workflows/deploy.yml 1-61; conversas/requirements.txt 1-9 — all reviewed, none uncovered.

CRITICAL || CONFIRMED || secrets/deploy || .github/workflows/deploy.yml || 50 || 54 || appleboy/ssh-action || Production deploy authenticates to the VPS with an SSH PASSWORD handed to a third-party action pinned only to a floating major tag || uses: appleboy/ssh-action@v1 with password: secrets.VPS_PASSWORD and no key: || Whoever controls the v1 tag receives a reusable interactive shell password for the host running postgres, n8n and both apps; password auth also permits offline brute force against sshd || Switch to an ed25519 deploy key, set PasswordAuthentication no on the VPS, pin the action to a full commit SHA
CRITICAL || HIGH || db-privilege || docker-compose.yml || 53 || 54 || DATABASE_URL || The app connects as POSTGRES_USER, which the postgres image creates as the DATABASE SUPERUSER - the comment calling it owner is wrong || line 53 comment claims owner; line 54 DATABASE_URL uses ${POSTGRES_USER:-crm_user}; docker/postgres/init.sql:28 ALTER USER crm_user CONNECTION LIMIT 30 (superusers are exempt) and init.sql:32-33 REVOKEs a superuser ignores || Any SQL injection in the CRM yields superuser: COPY FROM PROGRAM = shell in the postgres container, read/alter of every row, and the entire init.sql hardening is decorative || Create a non-superuser crm_app role owning the schema with CRUD+DDL only and point DATABASE_URL at it
HIGH || CONFIRMED || compose-completeness || docker-compose.yml || 62 || 136 || traefik labels / networks || Every service carries traefik routing labels but the file declares NO networks: key and publishes NO host ports, so nothing in the repo connects these containers to the EXTERNAL Traefik || grep -n networks docker-compose.yml -> no match; services use expose: only; labels reference a letsencrypt certresolver defined in a compose project not in this repo || The compose file is not self-contained: a rebuild from scratch produces three unreachable containers and the working state depends on an undocumented manual docker network connect that deploy.yml never performs - disaster recovery is unreproducible || Declare an external network, attach crm/conversas/n8n, keep postgres internal-only, add traefik.docker.network labels
HIGH || HIGH || network-segmentation || docker-compose.yml || 3 || 141 || services / default network || All four services share one flat default bridge network; n8n - which executes workflow code reachable from inbound WhatsApp webhooks - sits on the same L3 segment as postgres:5432, crm:8000 and conversas:8001 || No networks: anywhere; expose: is documentation-only in Compose v2; n8n has HTTP Request and Code nodes by design || An n8n workflow can talk straight to postgres:5432 and to both apps internal APIs, bypassing Traefik, TLS and edge rate limiting; lateral movement from the least-trusted component to the database is one hop || Put postgres on a dedicated internal:true backend network joined only by crm/conversas; keep n8n on the frontend network only
HIGH || CONFIRMED || secrets-exposure || docker-compose.yml || 84 || 86 || N8N_RESTRICT_ENVIRONMENT_VARIABLES_ACCESS || n8n is explicitly configured to let workflow expressions and Code nodes read the process environment, which holds the Meta WhatsApp access token || line 84 N8N_RESTRICT_ENVIRONMENT_VARIABLES_ACCESS=false; line 86 META_ACCESS_TOKEN in the same container || Any workflow can read $env.META_ACCESS_TOKEN and exfiltrate the WhatsApp Business token, allowing sending as the business and reading the WABA || Set the flag true and move the Meta token into an n8n credential object
HIGH || CONFIRMED || auth/availability || docker-compose.yml || 69 || 96 || n8n service || n8n is published at a public HTTPS hostname with NO authentication env, NO N8N_ENCRYPTION_KEY and an UNPINNED image tag || no N8N_BASIC_AUTH_*/N8N_USER_MANAGEMENT_* in 75-88; line 70 image with no tag = :latest; no encryption key; no healthcheck || Whoever reaches n8n.crmbrasileirosnoatacama.cloud before the owner is set claims the owner account and inherits every workflow and credential; without a fixed encryption key stored credentials are undecryptable after a volume restore; :latest lets a pull cross a breaking major version || Pin a version tag, set N8N_ENCRYPTION_KEY, verify the owner account exists, add a Traefik basicauth/IP-allowlist in front
HIGH || CONFIRMED || config-drift || docker-compose.yml || 51 || 61 || crm.environment || Twelve variables documented in .env.example never reach the CRM container: there is no env_file: and they are absent from environment:, so production silently runs on placeholder defaults || crm environment lists only 8 vars; missing but read by code and documented: MAIL_USERNAME/PASSWORD/FROM/PORT/SERVER/FROM_NAME, APP_DOMAIN, SEED_INITIAL_ADMIN, ADMIN_INITIAL_EMAIL, ACCESS_TOKEN_EXPIRE_MINUTES, MAX_UPLOAD_SIZE_BYTES, API_KEY_EXPIRY_DAYS, INTERNAL_AI_AUTH_MAX_SKEW_SECONDS || Production SMTP runs with the literal placeholder credentials so any mail feature fails or authenticates with junk, and APP_DOMAIN stays http://127.0.0.1:8000 so every link mail_service.py builds points at localhost || Add env_file: .env to the crm service and assert MAIL_*/APP_DOMAIN are not placeholders when ENVIRONMENT=production
HIGH || CONFIRMED || bootstrap/config || docker-compose.yml || 58 || 58 || ADMIN_INITIAL_PASSWORD || Compose hard-requires a secret the running code can never use, and there is consequently no supported way to create the first admin in production || line 58 uses :? so it is mandatory; SEED_INITIAL_ADMIN appears nowhere in compose so config.py:62-63 defaults it to false because ENVIRONMENT=production; app/seed.py:19-20 then logs seed disabled and returns || Operators must place a real admin password in .env where it lingers unused while the account it creates is never made; first login on a fresh volume is impossible without manual SQL || Pass SEED_INITIAL_ADMIN through and make the password conditional, or drop the :? and document a docker compose exec bootstrap
HIGH || HIGH || vuln-dependency || requirements.txt || 4 || 4 || python-jose || The two services validate HS256 JWTs signed with the SAME SECRET_KEY but pin different python-jose versions; the CRM is on the vulnerable one || requirements.txt:4 python-jose==3.3.0 vs conversas/requirements.txt:5 ==3.4.0; compose:57 and :115 feed both the same SECRET_KEY || 3.3.0 carries CVE-2024-33663 (algorithm confusion) and CVE-2024-33664 (JWE decompression DoS), both fixed in 3.4.0; a token minted by either service is accepted by the other so the CRM is the weak half of a shared trust boundary || Pin 3.4.0 and add a CI check that shared auth deps match
HIGH || HIGH || vuln-dependency || requirements.txt || 7 || 7 || python-multipart || CRM pins 0.0.9 while conversas pins 0.0.20; 0.0.9 is the pre-fix version for the multipart parsing DoS || requirements.txt:7 vs conversas/requirements.txt:8; the CRM accepts uploads (config.py:77) || CVE-2024-53981: a malformed multipart boundary makes the parser burn CPU per byte, blocking the event loop; with no container CPU limit one request stalls the whole CRM || Bump to 0.0.20
HIGH || HIGH || vuln-dependency || requirements.txt || 8 || 8 || jinja2 || CRM pins 3.1.4, conversas 3.1.6; both render server-side templates || requirements.txt:8 vs conversas/requirements.txt:9 || 3.1.4 predates the fixes for CVE-2024-56201 and CVE-2024-56326 (3.1.5) and CVE-2025-27516 (3.1.6); the two services also render shared markup at different escaping behaviour || Bump to 3.1.6
HIGH || CONFIRMED || schema-race || docker-compose.yml || 48 || 111 || depends_on || Both services execute Base.metadata.create_all against the SAME database at startup, with different SQLAlchemy minors, ordered only relative to postgres and never relative to each other || app/main.py:64 and conversas/app/main.py:26; both depends_on postgres service_healthy; sqlalchemy 2.0.35 vs 2.0.40 || Both containers are released the moment postgres reports healthy, so first boot after any model addition runs concurrent CREATE TABLE / CREATE INDEX from two SQLAlchemy versions on one database - DuplicateTable errors, lock contention and a coin flip over whose reflection wins || Run schema creation once from a dedicated one-shot job, remove create_all from both lifespans, align sqlalchemy pins
HIGH || CONFIRMED || migrations || .github/workflows/deploy.yml || 9 || 26 || migrations_reviewed || The pre-deploy gate names only migrations m001 and m002; the repository contains ten, and nothing in the pipeline runs any of them || deploy.yml:9, :25, :39 all reference only m001/m002; migrations/ contains m001..m010; migrations/README.md also tables only 001 || create_all creates missing TABLES but never ALTERs existing ones, so an operator following the checklist literally deploys code that SELECTs columns m003-m010 add and gets UndefinedColumn at runtime; the migrations_reviewed input is unvalidated free text || Update the gate text and README to enumerate all migrations and add an explicit ordered migration step before up -d
HIGH || CONFIRMED || deploy-safety || .github/workflows/deploy.yml || 55 || 61 || Deploy via SSH script || The deploy pulls a moving branch head rather than a reviewed commit, never verifies the result, and has no rollback path || git pull origin main with no SHA/ref input despite workflow_dispatch inputs; build --no-cache then up -d then echo; no health assertion, no --remove-orphans, no image prune, no previous-image tag || Whatever landed on main between approval and the click is what ships; with restart:always a crash-looping container still leaves the workflow green; rolling back means hand-editing the VPS; repeated --no-cache builds accumulate dangling images until the disk fills || Take a ref input and checkout --detach, poll /api/health on both services and exit 1 on failure, tag prior images and prune
HIGH || CONFIRMED || ci-gating || .github/workflows/test.yml || 17 || 20 || on: pull_request || Tests only run on pull requests to main, and the deploy workflow has no dependency on them at all || test.yml:17-20 has no push: trigger; deploy.yml has no needs: and no status-check query || A direct push to main, or a merge commit whose result differs from the PR head, is never tested; deploy.yml will ship a red or entirely untested main because the only gate is a human typing DEPLOY || Add push: branches:[main], mark both jobs required via branch protection, and have deploy assert a successful Tests run for the target SHA
HIGH || CONFIRMED || observability/disk || docker-compose.yml || 3 || 141 || logging || No service configures log rotation; Docker json-file grows without bound || no logging: key anywhere; restart:always on all four services; postgresql.conf logs connections/disconnections/ddl and init.sql:52 sets log_statement=all for crm_readonly so every AI query is logged in full || A crash loop, or normal AI query volume, writes unrotated logs until the VPS root filesystem fills - at which point postgres cannot write WAL and the whole stack fails in a way that is hard to recover remotely || Add logging max-size/max-file per service or set the daemon default
MEDIUM || HIGH || db-credential-bug || docker/postgres/init-hardening.sh || 14 || 15 || CRM_READONLY_PASSWORD || Double quoting: the shell wraps the password in literal single quotes and psql :'VAR' quotes it a second time, so the stored password contains apostrophes the connection URL does not || init-hardening.sh:14 sets the psql variable to 'SENHA' INCLUDING quotes; init.sql:11 renders '''SENHA'''; compose:56 builds the URL with the UNquoted value || The AI read-only database path fails SCRAM authentication on every fresh volume; ai_tools.py:131-140 catches it and returns "verifique DATABASE_READONLY_URL" so the failure looks like a config mistake forever - the safest data path in the system is the one that never works || Drop the shell-added quotes and let :'VAR' do the single round of quoting
MEDIUM || CONFIRMED || default-credential || docker/postgres/init-hardening.sh || 14 || 14 || crm_readonly_secret || A hardcoded fallback password for the read-only database user is committed to the repository || ${CRM_READONLY_PASSWORD:-crm_readonly_secret} - the :- fires when unset OR empty || compose:13 forces the variable today, but any run of this image outside that compose file creates a database account whose password is public in git history || Remove the fallback and exit 1 when empty
MEDIUM || CONFIRMED || image-hardening || Dockerfile || 1 || 24 || FROM/CMD || CRM image runs as root, keeps the C toolchain in the final layer, pins no digest, declares no HEALTHCHECK and installs recommended packages || FROM python:3.11-slim (mutable tag, rebuilt --no-cache every deploy); gcc and libpq-dev with no --no-install-recommends and no multi-stage discard; COPY . .; no USER; no HEALTHCHECK || uvicorn runs as UID 0 so any RCE starts as root with gcc available for local privilege escalation; the untagged base means the image CI validated and the image the VPS builds can differ || Add a non-root user, use a builder stage, digest-pin the base, add --no-install-recommends
MEDIUM || CONFIRMED || image-hardening || conversas/Dockerfile || 1 || 22 || FROM/CMD || Conversas image likewise runs as root with the toolchain retained and must write to the mounted media volume as UID 0 || FROM python:3.12-slim mutable tag; gcc libpq-dev retained; COPY . .; no USER, no HEALTHCHECK; compose:130 mounts conversas_media which will be root-owned || Root-owned volume contents make a later non-root switch a manual chown migration, and WhatsApp media (attacker-supplied bytes) is written by a root process into a directory served back through routers/media.py || Non-root user, --chown the copy and media dir, split build stage, digest-pin
MEDIUM || CONFIRMED || build-context/data-leak || .dockerignore || 1 || 13 || .dockerignore || The CRM build context excludes neither local databases nor the scratch/graphify working directories, and the two .dockerignore files disagree || .dockerignore has no *.db, *.sqlite3, scratch/, graphify-out/, tests/, docs/, .github/, .claude/ rules; conversas/.dockerignore:12-13 DOES have *.db and *.sqlite3; .gitignore lists crm_atacama.db, *.db, scratch/, graphify-out/; scratch/ is 7.5M and graphify-out/ 7.6M inside a 21M context || A docker compose build from a developer machine bakes the local dev/test SQLite database - real leads, users, password hashes - into the production CRM image, where it survives every docker save/registry push || Mirror conversas rules and add scratch/, graphify-out/, tests/, docs/, .github/, .claude/, .env*
MEDIUM || CONFIRMED || data-loss || docker-compose.yml || 129 || 130 || volumes || Conversas media is on a persistent volume but the CRM upload directory is not - AI-generated documents live only in the container writable layer || line 130 is the only app volume; the crm service declares NO volumes:; main.py:38 UPLOAD_DIR resolves to /app/uploads and ai.py:27 / ai_tools.py:419 read the same path; .dockerignore:13 excludes uploads/ so the directory exists only at runtime || Every build && up -d destroys all AI-generated PDFs/DOCX/XLSX; any download link a user was given before a deploy 404s afterwards || Add a named volume crm_uploads:/app/uploads
MEDIUM || MEDIUM || healthcheck-correctness || docker-compose.yml || 28 || 32 || postgres.healthcheck || pg_isready connects over the local unix socket, which the entrypoint TEMPORARY init server also listens on, so the container can report healthy before TCP 5432 is up and before init.sql has run || line 29 has no -h; the official entrypoint starts the init-phase server with -c listen_addresses='' overriding postgresql.conf:21 while the initdb.d scripts execute; both apps gate on service_healthy || Both apps can be released while postgres is still initialising - they fail to connect over TCP, exit, and are restarted, turning deterministic ordering into a retry-until-lucky loop with no alert; on a slow first boot crm_readonly may not exist yet || Use pg_isready -h 127.0.0.1 -p 5432 and add start_period: 30s
MEDIUM || CONFIRMED || config-drift || docker-compose.yml || 16 || 18 || docker-entrypoint-initdb.d || The postgres hardening scripts run ONLY on the first boot of an empty pgdata volume, so every later edit to grants, timeouts, connection limits or the readonly password is silently never applied || lines 16-18 mount init.sql and init-hardening.sh into initdb.d which the image executes only when PGDATA is empty; pgdata is a persistent named volume || The production database actual grants can diverge arbitrarily from the reviewed init.sql with nothing detecting it; rotating CRM_READONLY_PASSWORD in .env changes only the URL, not the stored role password, which breaks the AI connection in a way that looks like an app bug || Move hardening into an idempotent migration run by the deploy pipeline and assert live grants against the intended state
MEDIUM || CONFIRMED || secret-handling || docker-compose.yml || 114 || 115 || conversas.environment || Conversas receives SECRET_KEY and the DB password as BARE ${VAR} while the crm service uses the fail-fast ${VAR:?} form, and the Conversas config has an insecure hardcoded default with no production guard || line 115 vs line 57; line 114 vs line 11; conversas/app/config.py:17 has no raise unlike app/config.py:16-23 || The only thing stopping Conversas from signing production JWTs with an empty or repo-published key is the :? in a DIFFERENT service block; delete or reorder the crm service and Conversas boots with a forgeable HS256 key the CRM also honours - full cross-service authentication bypass || Use ${SECRET_KEY:?} and ${POSTGRES_PASSWORD:?} in conversas too and make its config raise outside development
MEDIUM || CONFIRMED || resource-limits || docker-compose.yml || 3 || 141 || services || No service declares memory or CPU limits and no container-hardening options are used || no mem_limit, cpus, deploy.resources, pids_limit, read_only, cap_drop, security_opt, no-new-privileges or init: anywhere; postgresql.conf sizes shared_buffers/work_mem with no matching container reservation || On a single-host VPS a leak or runaway query in any service triggers the kernel OOM killer, which frequently selects postgres (largest RSS) - a database kill is the worst outcome of an application bug; uvicorn also runs as PID 1 with no init so it reaps no zombies || Add mem_limit/cpus per service with postgres headroom, plus no-new-privileges, cap_drop ALL and init:true
MEDIUM || CONFIRMED || ci-selection || .github/workflows/test.yml || 50 || 83 || grep -L/-l CONVERSAS_DIR || The job selector is a plain substring match over whole files including comments, so classification is accidental - and it already misfiles a test today || running both selectors shows tests/test_conversas_security.py - a Conversas test - landing in the CRM (3.11 + CRM deps) job because it uses sys.path.insert without that literal; the header comment claims 34 files while ls tests/test_*.py returns 51 || Any CRM test that so much as mentions CONVERSAS_DIR in a comment is exiled to the 3.12 job where passlib, bcrypt, slowapi, google-generativeai, aiofiles and openpyxl are absent, and fails on import for reasons unrelated to its subject || Replace the grep heuristic with an explicit marker enforced by a guard test and fix the stale count
MEDIUM || CONFIRMED || ci-guard-dead-code || .github/workflows/test.yml || 50 || 51 || selecao vazia guard || The empty-selection guard can never print its message because Actions runs run: steps under bash -e and the failing command substitution aborts the step first || files=$(grep -L ...) then [ -n "$files" ] || { echo; exit 1; } - GNU grep exits 1 when nothing is listed, the assignment inherits that status and errexit terminates the step; identical at 83-84; also for t in $files is unquoted || The intended diagnostic never appears, so a future rename of the discriminator produces an opaque exit-1 with no explanation - exactly the failure the guard was written to prevent || Use || true before the emptiness test and a while IFS= read -r loop
MEDIUM || CONFIRMED || silent-test-skip || .github/workflows/test.yml || 34 || 41 || pip install httpx || Five CRM suites swallow ImportError and print SKIP while exiting 0, and the dependency that keeps them alive is installed only in CI, outside requirements.txt || httpx is NOT in requirements.txt; tests/test_hub.py:87, test_rate_limit.py:52, test_internal_tasks.py:165, test_notifications_ui.py:85, test_perpetua_internal_auth.py:303 all except ImportError, print SKIP and exit zero || except ImportError is far broader than httpx missing - a broken from app.x import y anywhere in the chain raises ImportError too, so a genuine regression converts the rate-limit, session-consistency and Perpetua-HMAC tests into silent green || Catch only ModuleNotFoundError for the specific name and make an unexpected ImportError a hard failure; or add httpx to requirements-dev.txt
MEDIUM || CONFIRMED || conflicting-compose || n8n/docker-compose.yml || 13 || 20 || ports/N8N_PROTOCOL || A second conflicting n8n definition publishes port 5678 directly on the host over plain HTTP with no auth and no Traefik labels || ports 5678:5678; N8N_PROTOCOL=http; WEBHOOK_URL=http://localhost:5678/; no traefik labels, no auth env; declares evolution_instances/evolution_store volumes no service uses; re-declares n8n_data under a different project namespace || If anyone runs docker compose -f n8n/docker-compose.yml up -d, n8n is served unauthenticated over cleartext HTTP on the public IP - Docker published-port iptables rules sit ahead of ufw INPUT so a host firewall does not stop it || Delete the file or convert it to a documented local-dev-only file bound to 127.0.0.1
MEDIUM || CONFIRMED || doc/config-drift || .env.example || 84 || 86 || N8N_HOST/N8N_PROTOCOL/N8N_WEBHOOK_URL || The documented n8n configuration is plain HTTP on a raw IP, contradicting the TLS hostname the compose file actually routes || .env.example:84-86 vs compose:93-95 websecure + letsencrypt, while compose:83 sets N8N_SECURE_COOKIE=true and 76-80 feed those very variables into the container; no N8N_PROXY_HOPS || An operator filling in .env.example verbatim gets n8n generating http:// webhook URLs (which Meta refuses to call) while N8N_SECURE_COOKIE=true marks its session cookie Secure over a protocol n8n believes is HTTP; the drift is invisible until a WhatsApp callback silently stops arriving || Set the example to the https hostname and add N8N_PROXY_HOPS=1
MEDIUM || CONFIRMED || config-drift || .env.example || 88 || 99 || META_API_VERSION || META_API_VERSION is read by the code but documented in neither .env.example nor docker-compose.yml, so the Graph API version is unchangeable in production || conversas/app/config.py:28-29; the Meta block lists five other vars but not this one; compose:119-125 passes the same five || Meta retires Graph API versions on a rolling schedule; when v21.0 sunsets every WhatsApp send/media fetch fails and the only fix is a code change plus a full rebuild-and-deploy || Add META_API_VERSION to .env.example and compose with a default
MEDIUM || CONFIRMED || secret-hygiene || .gitignore || 123 || 123 || .env || Only the exact filename .env is ignored - no .env.* variants - in .gitignore and in BOTH .dockerignore files; there is also no rule for *.sql, *.sql.gz or backups/ || .gitignore:123, .dockerignore:1, conversas/.dockerignore:1; scripts/backup_postgres.sh:21 takes an overridable BACKUP_DIR || A routine cp .env .env.bak before an edit, or a BACKUP_DIR=./backups invocation, commits live production credentials or a full plaintext pg_dump of customer data to git - and the same files enter the Docker build context || Change all three rules to .env* with a !.env.example negation and add *.sql, *.sql.gz, *.dump, backups/
LOW || CONFIRMED || supply-chain || requirements.txt || 11 || 18 || open-ended pins || Eight CRM dependencies use >= with no upper bound and there is no lockfile, while the deploy rebuilds from scratch on the VPS || google-generativeai, python-dotenv, pdfplumber, python-docx, fpdf2, email-validator, slowapi, psycopg2-binary; pydantic and httpx are not pinned at all while conversas pins both; deploy.yml:59 build --no-cache || The image that ships is resolved at deploy time from PyPI newest releases, not the set CI validated hours earlier || Pin exact versions or a hashed lockfile installed with --require-hashes
LOW || CONFIRMED || dead-config || .env.example || 10 || 10 || ALGORITHM || ALGORITHM=HS256 is documented as a tunable but read by nothing - both services hardcode it || app/config.py:24 and conversas/app/config.py:18 are literals || An operator attempting RS256/EdDSA by editing .env sees no effect and no error || Remove the line or make both configs read it
LOW || CONFIRMED || dead-config || docker/postgres/pg_hba.conf || 10 || 12 || crm_app || pg_hba grants network access to a database role crm_app that is never created || lines 10-12 and 20-22 list crm_app; init.sql creates only crm_readonly; crm_app exists solely as a CONTAINER name and as a misleading example in app/config.py:34 || Six dead rules make the access-control file read as if a least-privilege app role exists when it does not, obscuring that the app connects as superuser; anyone later creating a role named crm_app silently activates access they did not intend to grant || Delete the rules or create the role and use it; fix the example connection string
LOW || CONFIRMED || supply-chain || .github/workflows/deploy.yml || 50 || 50 || action pinning || All third-party actions are pinned to mutable tags and neither workflow restricts GITHUB_TOKEN permissions || deploy.yml:50, test.yml:26, test.yml:28; no permissions: block in either file || appleboy/ssh-action@v1 is handed the VPS password on every deploy so a tag repoint is direct credential theft; GITHUB_TOKEN inherits the repository default which may be read-write || Pin every uses: to a full SHA and add permissions: contents: read
LOW || CONFIRMED || compose-hygiene || docker-compose.yml || 1 || 1 || version || The obsolete version: key emits a deprecation warning on every Compose v2 command || line 1 in both compose files || Noise on every deploy that trains operators to ignore Compose warnings || Delete the version: line
LOW || CONFIRMED || dead-config || docker-compose.yml || 61 || 61 || CONVERSAS_BASE_URL || The CRM is given a Conversas base URL that no CRM code ever uses || compose:61 and app/config.py:57 define it; grep -rn CONVERSAS_BASE_URL app/ returns only the definition || Implies a CRM-to-Conversas integration that does not exist; if such a call is added later it will silently use 127.0.0.1:8001 wherever the var is omitted || Remove it or wire the intended call with a test

AGENT NOTES — ENV VAR TABLE (name | required? | code default | if empty at runtime):
SECRET_KEY | crm :? (57), conversas BARE (115) | crm raises; conversas "dev-secret-key-change-me" | compose aborts via crm :?; if that block changes, conversas signs prod JWTs with a repo-published key
POSTGRES_PASSWORD | :? (11), BARE in both URLs | none | compose aborts; otherwise both apps fail SCRAM
CRM_READONLY_PASSWORD | :? (13) | init-hardening.sh falls back to a committed literal | compose aborts; outside compose a public password is created
POSTGRES_USER | :-crm_user | crm_user | defaults to the postgres SUPERUSER
ADMIN_INITIAL_PASSWORD | :? (58) | "" | compose aborts, yet the value is never used because SEED_INITIAL_ADMIN is not passed
GEMINI_API_KEY | :- (59) | "" | AI degrades, no startup warning
INTERNAL_AI_AUTH_SECRET | :- (60) | "" | fail-safe by design: internal AI tools disabled
META_APP_SECRET | :- (125) | "" | documented as making the webhook 500 on every Meta POST - a silent-empty var that takes WhatsApp intake fully offline
META_ACCESS_TOKEN/PHONE_NUMBER_ID/WABA_ID/VERIFY_TOKEN | all :- | "" | outbound sends and webhook verification fail at request time, not at boot
N8N_AGENT_ENABLED | :-true (118) | "false" in code | THREE-WAY DRIFT: compose default true, code default false, .env.example says false - forgetting the var silently ENABLES the auto-reply agent
N8N_HOST/PROTOCOL/WEBHOOK_URL | :-localhost/:-http | n/a | n8n emits localhost/http webhook URLs behind an HTTPS Traefik
ADMIN_WHATSAPP | :-5551990119605 | n/a | a real phone number is hardcoded as a compose default
NOT PASSED TO ANY CONTAINER but documented and read by code (no env_file: exists): MAIL_USERNAME, MAIL_PASSWORD, MAIL_FROM, MAIL_PORT, MAIL_SERVER, MAIL_FROM_NAME, APP_DOMAIN, SEED_INITIAL_ADMIN, ADMIN_INITIAL_EMAIL, ACCESS_TOKEN_EXPIRE_MINUTES, MAX_UPLOAD_SIZE_BYTES, API_KEY_EXPIRY_DAYS, INTERNAL_AI_AUTH_MAX_SKEW_SECONDS, CONVERSAS_SEED_DEV_DATA, CONVERSAS_DEV_EMAIL, CONVERSAS_DEV_PASSWORD.
READ BY CODE, UNDOCUMENTED, UNPASSABLE: META_API_VERSION. DOCUMENTED BUT READ BY NOTHING: ALGORITHM.
NOT VERIFIED: whether the external Traefik is attached to this project network on the live VPS; whether /opt/crm is a clean clone; the pg_isready-during-init race (derived from the image entrypoint behaviour); CVE applicability by version range only, no scanner run; whether any n8n workflow references $env; actual disk/log volume on the VPS.
