AGENT: A11-conv-core
RANGES: conversas/app/main.py 1-80; config.py 1-44; database.py 1-28; auth.py 1-161; seed.py 1-255; __init__.py 1-1; models/__init__.py 1-1, api_config.py 1-27, auto_reply.py 1-44, conversation.py 1-128, media_asset.py 1-59, message_variable.py 1-35, note.py 1-36, quick_reply.py 1-21, tag.py 1-41, template.py 1-118; schemas/__init__.py 1-1, api_config.py 1-57, conversation.py 1-158, message_variable.py 1-169, quick_reply.py 1-37, settings.py 1-54, template.py 1-128 — all reviewed, none uncovered.

CRITICAL || HIGH || schema-divergence || conversas/app/main.py || 26 || 26 || lifespan/create_all || Conversas Base.metadata contains the SHARED users table (auth.py:17-30 __tablename__="users", imported at main.py:10) and create_all runs at startup in BOTH services; compose starts crm and conversas concurrently (both only depends_on postgres), so on a fresh volume whichever boots first CREATES users. create_all NEVER adds missing columns to an existing table || main.py:26 create_all; conversas/app/auth.py:22-30 declares users WITHOUT email_verified, role String(20), nome String(200), is_active nullable, api_key non-unique/non-indexed; app/models/user.py:18-24 declares nome String(100), role SAEnum(UserRole) NOT NULL, email_verified Boolean NOT NULL, api_key unique+indexed || If Conversas wins the race, users is created without email_verified and the CRM never gets it back - every CRM ORM query on User emits SELECT users.email_verified and fails with UndefinedColumn: TOTAL CRM OUTAGE INCLUDING LOGIN; api_key uniqueness is also lost so two users could share an api_key hash and _get_user_from_api_key().first() authenticates an arbitrary one || Remove the users declaration from Conversas Base (separate MetaData or Core queries) and make users DDL the CRM exclusive responsibility; assert expected columns at Conversas startup instead of creating them
CRITICAL || HIGH || schema-divergence || conversas/app/seed.py || 50 || 58 || seed_dev_user || Writes role="admin" LOWERCASE into the SHARED users table, which the CRM declares as Enum(UserRole); SQLAlchemy Enum persists the member NAME, so on PostgreSQL the CRM create_all produced native TYPE userrole AS ENUM ('ADMIN','USER') and 'admin' is not a valid label || seed.py:50-56 User(..., role="admin", ...); app/models/user.py:9-11,21 class UserRole(str, enum.Enum): ADMIN = "admin" with Column(SAEnum(UserRole)); conversas/app/auth.py:26 role = Column(String(20)) || On a CRM-created schema the INSERT fails with invalid input value for enum userrole "admin", aborting the Conversas lifespan; on a Conversas-created VARCHAR schema the row IS written and the CRM later raises LookupError: 'admin' is not among the defined enum values on every read of that user - the exact class the CONV-VAR-01-HOTFIX-ADMIN-01 comment describes, but on the WRITE side, which the hotfix does not cover || Write the member NAME ("ADMIN"), or better stop writing to users from Conversas entirely
HIGH || HIGH || schema-divergence || conversas/app/seed.py || 50 || 58 || seed_dev_user || The INSERT omits users.email_verified, which the CRM declares NOT NULL with only a CLIENT-SIDE default (no server DEFAULT in the DDL) || seed.py:50-57 constructs User(...) from conversas/app/auth.py:22-30 which has no email_verified attribute; app/models/user.py:23 Column(Boolean, default=False, nullable=False) with no server_default || Against a CRM-created users table the seed INSERT raises IntegrityError inside the lifespan, so the Conversas container fails to start and restarts forever whenever CONVERSAS_SEED_DEV_DATA is on || Add server_default on the CRM side for columns Conversas cannot know about, or remove user creation from Conversas
HIGH || CONFIRMED || security || conversas/app/seed.py || 28 || 53 || _hash_password || Writes an UNSALTED SHA-256 password hash into the SHARED users table while the CRM stores and verifies bcrypt via passlib || seed.py:29-30 hashlib.sha256(password.encode()).hexdigest() used at seed.py:53; app/auth.py:20-31 CryptContext(schemes=["bcrypt"]) || The row is unusable by the CRM: pwd_context.verify on a bare hex digest raises UnknownHashError so CRM login for that account 500s instead of 401ing; the digest is trivially reversible and lands in the same table as real production credentials || Hash with app.auth.hash_password (bcrypt) or drop the Conversas-side user seed
CRITICAL || CONFIRMED || config || conversas/app/config.py || 17 || 17 || SECRET_KEY || Hardcoded fallback JWT signing key with NO production gate, unlike the CRM which raises when the secret is absent || config.py:17 SECRET_KEY = os.getenv("SECRET_KEY", "dev-secret-key-change-me") vs app/config.py:16-23 which raises RuntimeError outside development; docker-compose.yml:115 passes SECRET_KEY=${SECRET_KEY} for conversas WITHOUT the :? guard used for crm at line 57 || Any deploy path that reaches Conversas without SECRET_KEY set silently signs and accepts JWTs with a PUBLIC CONSTANT; get_current_user only checks that sub matches an active email, so anyone can forge an admin token and read/send the whole WhatsApp inbox || Mirror app/config.py: random key in development, raise outside it; add :? to the compose var
HIGH || CONFIRMED || config || conversas/app/seed.py || 20 || 22 || CONVERSAS_SEED_DEV_DATA || ONE env var controls TWO unrelated things: dev data seeding AND which authentication backend the login endpoint uses. It defaults to true because ENVIRONMENT itself defaults to development || seed.py:20-22 defaults to "true" when ENVIRONMENT == development; routers/auth.py:79 if CONVERSAS_SEED_DEV_DATA: selects LOCAL users authentication with _verify_password = SHA-256 instead of proxying to the CRM || Turning the flag on to get seed data silently downgrades authentication for the whole service from CRM bcrypt + 5/minute rate limit to unsalted SHA-256 with NO rate limit against the shared users table; a deploy that forgets ENVIRONMENT gets that plus /docs and wildcard CORS || Split the flag, add a separate explicit CONVERSAS_AUTH_MODE, and make ENVIRONMENT default to production
HIGH || CONFIRMED || security || conversas/app/main.py || 51 || 60 || app middleware stack || No security-headers middleware and no rate limiting, although Conversas serves the authenticated HTML inbox and a login endpoint; the CRM in the same repo has both || main.py:51-57 adds only CORSMiddleware then mounts static and routers; app/main.py:84-88 registers SlowAPIMiddleware and 105-128 SecurityHeadersMiddleware; grep for limiter|slowapi in conversas/ returns nothing || conversas.crmbrasileirosnoatacama.cloud can be FRAMED (clickjacking on an inbox that sends WhatsApp messages), sends no HSTS/nosniff, caches session-dependent pages, and /api/auth/login accepts unlimited attempts - brute force against the SHARED users table through the weaker of the two front doors || Reuse the CRM SecurityHeadersMiddleware and slowapi limiter, with a per-IP limit on login
HIGH || HIGH || security || conversas/app/main.py || 47 || 57 || CORS || allow_origins=["*"] together with allow_credentials=True whenever ENVIRONMENT is development - which is the DEFAULT, not an opt-in || main.py:47-57; Starlette CORSMiddleware ECHOES the request Origin back when allow_all_origins is combined with credentials || On any host where ENVIRONMENT was not explicitly set, any website a logged-in operator visits can READ the full conversation list, message bodies and lead data from the cookie-authenticated API || Never combine "*" with allow_credentials; keep an explicit origin list in every environment
MEDIUM || CONFIRMED || data-integrity || conversas/app/models/conversation.py || 44 || 45 || Conversation.lead_id/whatsapp || No unique constraint on the natural key of a conversation - whatsapp is indexed but not unique, lead_id likewise, so nothing at the DB level makes webhook processing idempotent per phone number || models/conversation.py:44-45 both Column(..., index=True) with no unique; compare messages where idempotency IS enforced at line 107 whatsapp_msg_id = Column(String(100), unique=True) || Two concurrent inbound webhooks for the same number (Meta retries aggressively) both miss the SELECT and both INSERT, producing split conversation threads for one customer, split unread counts and a lost 24h window on one duplicate || UNIQUE on whatsapp plus an idempotent upsert in the webhook path
MEDIUM || CONFIRMED || data-integrity || conversas/app/models/conversation.py || 47 || 108 || status/direction/msg_type || State columns are free String with no CHECK and no enum; the whitelist exists only inside ONE route handler, and the codebase documents that invalid values were in fact persisted before that handler existed || conversation.py:47,103,105,108 and media_asset.py:46 all plain String; routers/conversations.py:42-46 states the pre-CONV-06 code accepted any value in the PUT of status so LEGACY rows with status='aguardando' may exist || Every new write path must remember the whitelist; the one that forgot already corrupted production rows, and each filter now carries LEGACY_OPEN_STATUSES compensation forever || CHECK constraints or native enums via a migration
MEDIUM || HIGH || schema-divergence || conversas/app/models/tag.py || 30 || 30 || ConversationTag.nome || Tag name column is HALF the length of the CRM column it mirrors, and the mirror copies the CRM value verbatim || conversas tag.py:30 String(50) vs app/models/tag.py:21 String(100); conversas/app/services/crm.py:498-502 ConversationTag(nome=item["nome"], ...) || Any CRM tag name over 50 chars makes the read-repair mirror raise StringDataRightTruncation, so opening the affected conversation 500s and its tags never sync || Widen to String(100) to match the source of truth, or truncate explicitly
MEDIUM || HIGH || security || conversas/app/models/api_config.py || 17 || 17 || ApiConfig.meta_access_token || The permanent Meta Graph API access token is stored in CLEARTEXT in a shared-database table that the read-only role used by the CRM AI can SELECT || api_config.py:17 Column(String(500)); docker/postgres/init.sql:17-21 GRANT SELECT ON ALL TABLES to crm_readonly; app/config.py:37-39 DATABASE_READONLY_URL is the AI connection || A read-only DB credential, or any SQL surface reachable through the AI, yields a permanent WhatsApp Business token able to send messages as the company; the same grant also exposes users.hashed_password || Encrypt at rest or keep in secret storage; at minimum REVOKE SELECT ON api_config, users FROM crm_readonly
MEDIUM || CONFIRMED || security || conversas/app/schemas/api_config.py || 26 || 40 || ApiConfigResponse || The webhook verify token is returned in cleartext by the SAME response model that deliberately masks the access token || :21 docstring claims masking, :23 has_access_token: bool (masked) but :26 and :40 return meta_verify_token raw || Any client that can reach the config endpoint learns the secret Meta uses to prove ownership of the webhook URL || Mask it the same way (has_verify_token: bool) or return only a prefix
MEDIUM || CONFIRMED || validation || conversas/app/schemas/quick_reply.py || 10 || 18 || QuickReplyCreate.category/QuickReplyUpdate || The UPDATE schema drops every length bound the create schema enforces, and create has no bound on category; the router applies values raw || :13-18 all bare Optional[str] vs :7-10 create bounds (2-50/1-100/1-5000); columns are String(50)/String(100)/Text/String(50); routers/quick_replies.py:137+ assigns straight onto the ORM object || On PostgreSQL an over-long value is a 500 instead of a 422, and content becomes unbounded on update so one request can store an arbitrarily large blob echoed into every listing || Give Update the same Field bounds and add max_length=50 to create.category
MEDIUM || CONFIRMED || validation || conversas/app/schemas/template.py || 27 || 36 || TemplateUpdate || Same asymmetry for templates: every Meta-mandated bound present on create is absent on update || :15-24 create enforces name 512, header 60, body 1-1024, footer 60; :27-36 update declares all as bare Optional[str] || Update accepts values the DB column AND the Meta API reject: 500 on PostgreSQL, or a body over 1024 chars that only fails later at Meta submission with an opaque error || Reuse a shared base model or repeat the constraints
MEDIUM || HIGH || data-integrity || conversas/app/models/conversation.py || 50 || 52 || atendente_id/responsavel_id || User references with NO FK and no application-side existence check on the main update path; the unchecked value is then pushed into the CRM || conversation.py:50,52 bare Integer (no ForeignKey - users belongs to the CRM); schemas/conversation.py:98-102 no ge=1 (contrast :40 AssignRequest.user_id ge=1); routers/conversations.py:671-684 keeps the id when the user lookup misses, then 689-691 syncs it to the CRM lead || Any authenticated operator can point a conversation AND the linked CRM lead at a non-existent or deleted user id; the conversation shows no owner name and queue/ownership filters silently exclude it || Validate existence and is_active before assigning (422 otherwise) and apply ge=1
LOW || CONFIRMED || schema || conversas/app/models/template.py || 95 || 95 || MessageTemplate.name || name is UNIQUE ALONE although the identity of a Meta template is (name, language) - a limitation the sibling model in the SAME FILE documents as the reason it exists || :95 unique=True while :25-27 states the identity is (name, language); ServiceTemplate (:30-32) and TemplateParamMap (:75-77) both use composite UNIQUE || Creating the pt_BR and es_ES variants of one template through the app fails with 409/IntegrityError, so multi-language templates can only be created in the Business Manager || UniqueConstraint("name","language") via a migration
LOW || CONFIRMED || reliability || conversas/app/main.py || 10 || 16 || model imports || The explicit "ensure table is known" import block is INCOMPLETE: media_asset, note and tag are registered in Base.metadata only as a side effect of the router imports on line 17 || main.py:10-16 imports 8 model modules but not media_asset, note or tag; those arrive via main.py:17 from app.routers import ... media, tags, notes || Removing or lazily importing one of those routers makes create_all silently SKIP media_assets / conversation_tags / conversation_tag_links / conversation_notes on a fresh database, surfacing only at first use || Import the model modules explicitly next to the others
LOW || CONFIRMED || reliability || conversas/app/main.py || 60 || 60 || StaticFiles mount || Static directory resolved relative to the process CWD, not to the package || main.py:60 StaticFiles(directory="static"); works only because the Dockerfile sets WORKDIR /app || Any invocation from another directory raises at IMPORT time with "Directory 'static' does not exist" || Build the path from __file__
LOW || CONFIRMED || ops || conversas/app/main.py || 77 || 80 || health_check || Health endpoint returns a STATIC payload and never touches the database, but it is what the container healthcheck uses || main.py:77-80 returns a constant dict; docker-compose.yml:104-108 gates restarts on it || The container reports healthy while PostgreSQL is unreachable and every real request 500s, so orchestration never restarts or drains it || Execute SELECT 1 with a short timeout and return 503 on failure

AGENT NOTES — IMPORTANT CORRECTION TO THE WORKING ASSUMPTION:
The ONLY shared table is `users`. Verified by enumerating __tablename__ in both services: the sole collision is users. `tags` and `leads` are CRM-ONLY; Conversas uses its own conversation_tags + conversation_tag_links and has NO leads model (lead_id is a bare Integer with no FK; leads are reached only through raw SQL in conversas/app/services/crm.py).
users divergence table (column | CRM | Conversas | consequence):
nome | String(100) NOT NULL | String(200) NOT NULL | 100-200 char names insertable only if Conversas created the table
role | Enum(UserRole) NOT NULL, persists NAME "ADMIN" | String(20) nullable default "user" | READ side patched by is_admin_role(); WRITE side broken (seed.py writes "admin")
is_active | Boolean NOT NULL | Boolean nullable default True | NULL is_active possible if Conversas created the table
email_verified | Boolean NOT NULL, client-side default only | ABSENT | Conversas INSERT fails; CRM SELECT fails if Conversas created the table
api_key | String(255) UNIQUE indexed | String(255) no unique no index | duplicate api_key hashes possible; .first() picks arbitrarily
hashed_password | bcrypt (passlib) | SHA-256 hex written by seed.py:29 | cross-service login breaks with UnknownHashError
id / email / created_at / updated_at | identical | identical | ok
NOT VERIFIED: which service actually wins the create_all race on a fresh volume; runtime behaviour of the seed against the live production DB; whether the AI SQL surface can reach api_config in practice (the GRANT is confirmed); frontend datetime parsing of naive vs aware timestamps.
