AGENT: A15a-tests (CRM suite)
RANGES: tests/README.md 1-29; test_auth_session_consistency.py 1-403; test_hub.py 1-90; test_rate_limit.py 1-55; test_security_greps.py 1-71; test_render_templates.py 1-94; test_infra_compose_guard.py 1-83; test_internal_tasks.py 1-168; test_kanban_ui.py 1-77; test_notifications_ui.py 1-88; test_perf_query_count.py 1-185; test_perpetua_internal_auth.py 1-306; test_perpetua_pdf_generation.py 1-213; test_leads_deeplink_pinned.py 1-645; test_leads_destino_filter_dialect.py 1-94; test_leads_keyset_pagination.py 1-1005; test_leads_responsavel_atomic.py 1-167; test_leads_segment_drift.py 1-208; test_segments_sql_count.py 1-744; test_pipeline_inline_lead_edit.py 1-559; test_pipeline_review_final.py 1-458; test_pipeline_stage_pagination.py 1-446; test_pipeline_target_clear.py 1-333; test_pipeline_target_highlight.py 1-253 — all reviewed, none uncovered.

HIGH || CONFIRMED || cannot-fail-assertion || tests/test_pipeline_inline_lead_edit.py || 525 || 527 || test_card_remontado_escapa_dado_do_lead || The third assertion of the XSS test is terminated by `or True`, making it UNCONDITIONALLY TRUE - a dead assertion || assert "lead.nome" not in ... or "esc(" not in atualiza or True || The check that atualizarCardDoLead does not interpolate raw lead.nome is NOT ENFORCED; a regression re-introducing unescaped card HTML passes || Delete the dead clause or rewrite it as a real property check
HIGH || CONFIRMED || tautological-test || tests/test_pipeline_review_final.py || 401 || 416 || test_exclusao_do_dropdown_compila_em_postgresql || The test BUILDS ITS OWN NOT EXISTS query and asserts the compiled SQL contains NOT EXISTS; no production code is invoked || the locally constructed exists() then assert "not (exists" in sql || Asserts a SQLAlchemy invariant, not the app: if exclude_funnel_id reverts to a client-side filter this test STILL PASSES || Compile the real expression from the endpoint helper
HIGH || CONFIRMED || tautological-test || tests/test_leads_keyset_pagination.py || 734 || 740 || test_postgres_busca_usa_ilike_nos_tres_campos || The test constructs or_(nome.ilike, email.ilike, whatsapp.ilike) ITSELF then asserts the compiled SQL has 3 ILIKE || the local or_ then sql.count("ILIKE") == 3 || The claim "search covers nome/email/whatsapp case-insensitively on Postgres" is UNPROTECTED - dropping whatsapp from list_leads, or switching ILIKE to LIKE, keeps this green || Extract the search predicate into a helper and compile that
MEDIUM || CONFIRMED || tautological-test || tests/test_pipeline_review_final.py || 375 || 385 || test_queries_novas_compilam_em_postgresql || _stage_query IS production, but the ORDER BY and the keyset tuple_ comparison asserted at 379-381 are APPENDED BY THE TEST ITSELF || q = q.order_by(...); q = q.filter(tuple_(...) < (...)) then assert "order by" in baixo || The assertions about the keyset ORDER BY and the tie-break can NEVER FAIL regardless of what the endpoint does || Assert on the SQL the endpoint itself builds
HIGH || CONFIRMED || silent-skip || tests/test_leads_deeplink_pinned.py || 133 || 145 || test_nenhum_endpoint_novo_foi_criado || Guarded by `git show origin/main:app/routers/leads.py`; on non-zero exit the function RETURNS and the runner prints PASS || if base.returncode != 0: return || The crm CI job uses actions/checkout@v4 with the DEFAULT fetch-depth 1 (only the conversas job sets fetch-depth: 0), so origin/main is normally ABSENT and this guard NEVER RUNS IN CI || Fetch origin/main in the crm job, or fail loudly
HIGH || CONFIRMED || silent-skip || tests/test_pipeline_inline_lead_edit.py || 414 || 448 || test_pipeline_nao_ganhou_endpoint_novo/test_nenhum_py_de_aplicacao_alterado || Both scope guards RETURN silently when git cannot resolve origin/main || if base.returncode != 0: return at 418; if r.returncode != 0: return at 436 || In the CRM CI job neither the no-new-route nor the backend-untouched guard is ever evaluated; both report PASS || Fetch origin/main and make the missing baseline a hard failure
HIGH || CONFIRMED || silent-skip || tests/test_auth_session_consistency.py || 328 || 374 || _run_login_js and the 4 test_login_js_* tests || _run_login_js returns None when node is missing; each caller then RETURNS, and the runner counts them as passing || node = shutil.which("node"); if node is None: return None, then if r is None: return at 344, 353, 363, 372 || Without node, the ONLY tests that actually execute login.js - incident state does not navigate, one-shot hop guard, loop break, open-redirect rejection (AUTH-LOOP-01, THE PRODUCTION INCIDENT) - vanish, leaving only string greps || Follow the sibling files (assert node) and fail when node is absent
MEDIUM || CONFIRMED || silent-skip || tests/test_pipeline_target_highlight.py || 144 || 152 || test_animacao_e_curta_e_nao_infinita || The whole body is inside `if "animation" in bloco:` - removing the animation property makes every assertion EVAPORATE || the if wrapping the infinite/duration/prefers-reduced-motion asserts || Renaming the CSS property or moving the animation silently disables the reduced-motion accessibility check || Assert the animation exists instead of branching on it
MEDIUM || CONFIRMED || silent-skip || tests/test_leads_keyset_pagination.py || 243 || 251 || test_lead_em_dois_funis_nao_encolhe_a_pagina || The assertion that a lead in two funnels reports both is inside `if alvo:` || alvo = next(..., None) then if alvo: assert len(alvo["funis"]) == 2 || If ordering changes so the two-funnel lead falls off page 1, the joinedload-multiplication guard SILENTLY STOPS RUNNING || Query the lead by id directly
HIGH || CONFIRMED || false-claim-correction || tests/test_hub.py || 84 || 89 || __main__ ImportError handler || CORRECTION TO A SIBLING AGENT: the ImportError handler here exits 2, NOT 0 - CI treats it as a FAILURE || except ImportError as e: print("SKIP ..."); raise SystemExit(2) - identical at test_rate_limit.py:52-54, test_internal_tasks.py:165-167, test_notifications_ui.py:85-87, test_perpetua_internal_auth.py:303-305; the CI loop is `python "$t" || fail=1` || These five are NOT silent skips; they are LOUD. The real defect is the MISLEADING MESSAGE: a broken `from app.main import app` (a genuine regression) is reported as "SKIP: dependencia ausente (httpx?)" and the remaining tests in the file are abandoned || Narrow the handler to re-raise when httpx is not the missing module
HIGH || MEDIUM || hang || tests/test_hub.py || 44 || 78 || _client/test_hub_route_requires_valid_session || Nothing in the file bounds any wait: no timeout on TestClient startup, no timeout on client.get, no watchdog || with _client() as client: runs the lifespan with no deadline; client.get has no timeout=; FORENSIC EVIDENCE: after the timed-out baseline run every other CRM test left its DB in scratch/ but scratch/hub_test.db does NOT exist, proving the process never reached the first SQLite connection || The block is therefore in `from app.main import app` (which pulls google.generativeai/grpc via app/routers/ai.py:11) or in the lifespan - NOT in any assertion. It is the only red test and it cost 300s of the ~60min suite || Wrap client construction and every request with an explicit timeout and add a module-level faulthandler watchdog
MEDIUM || HIGH || resource-leak || tests/test_perf_query_count.py || 90 || 94 || _setup || TestClient is ENTERED and NEVER EXITED; _setup() is not cached so all three tests create and enter a separate client (3 leaked anyio portals + 3 logins per run) || client.__enter__() with no matching __exit__; the same leak in test_leads_deeplink_pinned.py:98, test_leads_keyset_pagination.py:150, test_pipeline_review_final.py:128, test_pipeline_stage_pagination.py:130, test_pipeline_target_highlight.py:94, test_segments_sql_count.py:173 || A never-stopped blocking portal keeps a non-daemon worker thread alive; interpreter shutdown must join it - the exact class of defect that produces the test_hub-style hang, and it triples lifespan cost in the perf file || Use `with TestClient(app) as client:` and cache the client
HIGH || HIGH || flakiness-time || tests/test_pipeline_stage_pagination.py || 268 || 274 || test_filtros_de_periodo || The seed spreads 300 entries at `agora - timedelta(minutes=i)` (up to 4h59m in the past) and the test asserts the `hoje` filter returns ALL 300 || agora = datetime.now(timezone.utc) at 88, e.updated_at = agora - timedelta(minutes=i) at 102, then assert body["total"] == N_GRANDE for periodo in ("hoje", ...) || Any run started between 00:00 and 05:00 puts part of the seed on the PREVIOUS CALENDAR DAY and the test fails for a reason unrelated to the code || Pin the seed relative to the start of the current day, or freeze the clock
MEDIUM || HIGH || env-divergence || tests/test_segments_sql_count.py || 74 || 74 || SEMENTE case "acento" || The adversarial seed ADVERTISES an accent case but uses UNACCENTED ASCII, so the SQLite-only lower() divergence is never exercised || ("acento", {"origem": "Indicacao"}, ...) - no cedilla, no accent; no FILTROS entry uses a non-ASCII campo_valor either || app/query_filters.py wraps both sides in func.lower(); SQLite lower() is ASCII-ONLY while Postgres is locale-aware, so a key/value like Endereco/ENDERECO matches in production and would NOT match under the test DB. Every set-equality test here is BLIND to it || Add seed rows and FILTROS entries with real accented keys/values
MEDIUM || HIGH || env-divergence || tests/test_leads_keyset_pagination.py || 258 || 300 || _oraculo/FILTROS || The Python oracle uses Unicode-aware str.lower() while the SQL side runs SQLite LIKE/ILIKE, and the ENTIRE SEED IS ASCII, so the two can never disagree || the oracle comprehension; seed names are f"Lead {i:03d}" + " Joao" (no accent); FILTROS search terms are all ASCII || Real leads are Brazilian names WITH ACCENTS; case/accent-insensitive search differs between SQLite and Postgres. NEITHER production dialect path is behaviourally tested - SQLite is the only gate || Seed accented names and add search cases for them
MEDIUM || HIGH || env-divergence || tests/test_segments_sql_count.py || 509 || 566 || the Postgres tests || EVERY Postgres claim in this file is a SUBSTRING CHECK on compiled SQL text; no Postgres engine ever executes it || assert "jsonb_typeof" in pg_sql, "jsonb_each_text" in sql, "ESCAPE" in sql, "@>" in sql - the behavioural oracle comparison runs only on SQLite || The file own docstring concedes it. SQL that contains the right tokens but is SEMANTICALLY WRONG on Postgres (wrong cast order, wrong ESCAPE char, NULL handling in jsonb_each_text) ships unnoticed || Add a Postgres job, or assert on a parsed plan
MEDIUM || HIGH || env-divergence || tests/test_leads_destino_filter_dialect.py || 46 || 76 || _sql/test_ramo_postgres_faz_cast_para_jsonb || The whole file monkeypatches modulo.IS_SQLITE and only inspects compiled SQL TEXT; it proves the CAST(... AS JSONB) @> string is emitted, not that it returns the right rows || the compile with literal_binds then the substring asserts || The original production bug is now string-guarded, but semantic divergences of @> on jsonb arrays remain untested; the SQLite branch uses LIKE over stringified JSON, which will ALSO MATCH SUBSTRINGS ACROSS ELEMENT BOUNDARIES (destino "Ata" matches "Atacama") - a false positive Postgres @> would not produce || Row-level tests against Postgres, plus a SQLite case proving a partial token does not match
HIGH || HIGH || weak-guard || tests/test_security_greps.py || 47 || 49 || test_admin_guards_present || The admin-guard regression test is a COUNT THRESHOLD set far below the real value, and the glob DOUBLE-COUNTS app-root modules || total = sum(...count("require_admin")...) for app/**/*.py and app/*.py; assert total >= 13; MEASURED TODAY: 39 occurrences (79 paths globbed, only 71 unique) || Roughly TWO THIRDS of the require_admin guards can be deleted and this test stays green; no test anywhere proves a non-admin actually receives 403 on any CRM admin endpoint || Per-endpoint assertion enumerating app.routes, and deduplicate the glob
MEDIUM || HIGH || grep-only-test || tests/test_security_greps.py || 41 || 44 || test_no_raw_destructive_sql || Greps for literal DROP TABLE / DELETE FROM in app/ and migrations/ only || the regex || EVERY real deletion in this codebase goes through the ORM (db.query(...).delete()) and is INVISIBLE to the grep; the test protects against a spelling, not against data loss. It would also keep passing after a refactor that builds the same SQL via text("DELETE " + "FROM ...") || Test the behaviour that matters (authorization on destructive endpoints, cascade rules)
MEDIUM || HIGH || grep-only-test || tests/test_kanban_ui.py || 32 || 70 || all four tests || The ENTIRE FILE asserts substrings of the rendered kanban template; no board behaviour is exercised || assert "updateCreateCardState" in html, 'classList.add("dragging")' in html, etc. || Unprotected: the create-card button actually being disabled, the modal actually rendering as an overlay, column names actually being escaped AT RUNTIME, drag-and-drop reordering, and the WHOLE operational cards/checklists/comments API (35 routes, ZERO tests) || Execute the extracted template JS under node with a DOM stub - the pattern test_pipeline_target_clear.py already uses
MEDIUM || HIGH || grep-only-test || tests/test_infra_compose_guard.py || 34 || 83 || module-level checks || The whole file is substring/regex matching over docker-compose.yml, deploy.yml and .env.example; brittle against formatting, blind to semantics || check("- conversas_media:/app/data/media" in COMPOSE, ...) and COMPOSE.split("volumes:")[-1] || Reformatting the YAML breaks the test without breaking prod; conversely a volume declared but mounted read-only, or a healthcheck with a wrong interval, PASSES. The split() also silently examines the WRONG BLOCK if a service gains a volumes: key after the top-level one || Parse the YAML and assert on the parsed structure
MEDIUM || HIGH || brittle-guard || tests/test_leads_segment_drift.py || 154 || 169 || test_compose_crm_recebe_internal_ai_auth_secret || Asserts an EXACT GLOBAL OCCURRENCE COUNT of a variable name in docker-compose.yml || assert text.count("INTERNAL_AI_AUTH_SECRET") == 2 || Any legitimate change - a comment mentioning the variable, passing it to a second service, an env_file refactor - turns this RED for no security reason; it encodes a formatting accident as a requirement and BLOCKS FUTURE EDITS || Assert presence-in-crm/absence-elsewhere against the parsed YAML (the sibling test at 172-183 already does this correctly)
MEDIUM || HIGH || mock-erases-bug || tests/test_leads_deeplink_pinned.py || 178 || 179 || _HARNESS closeLeadModal stub || The harness defines its OWN closeLeadModal and the real one is never loaded, yet the test claims to protect "Mutation A" || the stub; _FUNCOES at 195-197 lists ten real functions but closeLeadModal is ABSENT; test_fechar_modal_nao_limpa_o_destaque at 298 then calls the stub || The behavioural claim is asserted against TEST-OWNED CODE. Only the static grep at 586 touches the real function, and it would pass if the real closeLeadModal cleared the highlight INDIRECTLY || Load the real closeLeadModal into _FUNCOES like the other functions
MEDIUM || HIGH || shared-state || tests/test_leads_keyset_pagination.py || 531 || 573 || test_query_count_constante_com_o_volume || Inserts 4880 extra leads into the DB SHARED by every test in the file, then deletes them by NAME PATTERN in a finally block; tests run alphabetically so this executes BEFORE several set-equality tests || bulk_insert_mappings up to 5000 then delete where nome like "bulk %" || Any failure between insert and cleanup leaves 5000 rows and every later test asserting total == N fails with a MISLEADING message. It is also the single biggest contributor to this file runtime || Use a dedicated marker or a savepoint rollback, and move the volume test into its own database
MEDIUM || MEDIUM || shared-state || tests/test_pipeline_stage_pagination.py || 331 || 353 || test_mover_lead_coloca_no_topo_da_etapa_destino || Mutates the SHARED seeded board and "restores" it with a second move that rewrites updated_at and does NOT restore posicao || the two PUT /move calls with the comment about not contaminating other tests || The restore only HAPPENS to be harmless because the moved entry was already the newest; any change to the seed ordering makes the alphabetically later ordering/pagination tests depend on this test having run || Give the move test its own funnel/stage
MEDIUM || HIGH || swallowed-exception || tests/test_leads_responsavel_atomic.py || 138 || 141 || test_falha_no_historico_reverte_a_troca || The PUT that is supposed to fail is wrapped in a bare `except RuntimeError: pass` || try: client.put(...) except RuntimeError: pass || If the route starts catching the error internally and returns 500 AFTER committing, the exception never surfaces; only the subsequent state comparison saves the test. The injection point is a MODULE ATTRIBUTE (pipeline.LeadHistory = _explode) that depends on the router keeping its function-local import - moving that import to module scope SILENTLY DISARMS the fault injection || Assert the response status explicitly and assert _explode was actually invoked
MEDIUM || HIGH || coverage-gap || tests/test_leads_responsavel_atomic.py || 101 || 149 || whole file || "Atomicity" is proven ONLY for the exception-rollback path on SQLite; no concurrency, no locking || two tests only, both single-threaded against SQLite || SELECT ... FOR UPDATE is a SILENT NO-OP on SQLite and SQLite serialises writers anyway, so a missing row lock cannot be detected here; concurrent responsavel changes on Postgres are untested || Concurrency test against Postgres, or assert the production query uses with_for_update() when not IS_SQLITE
MEDIUM || MEDIUM || rate-limit-coupling || tests/test_auth_session_consistency.py || 379 || 386 || test_login_rate_limit_still_active || Two real logins earlier in the same process SHARE the 5/minute login bucket with this test six attempts; the file relies on DEFINITION ORDER to stay under the limit || TESTS = [...] with the comment that globals() preserves definition order so the rate-limit test is last; _login() is called at 192 and 198 || Reordering or adding a test that logs in flips _login() 200-assert into a 429 and produces a failure whose message says "login valido deveria passar" - a misdiagnosis. THREE separate files carry comments about avoiding this shared limit, i.e. the coupling is known and unmitigated || Reset the limiter between tests
LOW || HIGH || doc-drift || tests/README.md || 1 || 29 || whole file || The README documents 3 test files and a PYTEST workflow; the suite has 51 standalone files and CI NEVER invokes pytest || `python -m pytest tests/ -q` and a 3-row coverage table; "Pendente: Testes de auth/IDOR/HMAC" although those files now exist || A new contributor running the documented command gets a DIFFERENT (and in places broken - module-level client.__enter__(), module-level env mutation) execution model than CI one-process-per-file || Regenerate from the actual file list or point at the workflow
LOW || HIGH || fragile-extraction || tests/test_pipeline_inline_lead_edit.py || 40 || 46 || _fn || ALL JS extraction across five files relies on splitting on the literal "\n    }", i.e. on a 4-SPACE INDENTATION CONVENTION || return f[m.start():].split("\n    }", 1)[0] + "\n    }" - same idiom in test_leads_deeplink_pinned.py:158-165, test_leads_keyset_pagination.py:806-812, test_pipeline_target_clear.py:59-64, test_pipeline_target_highlight.py:109 || Re-indenting a template (or a Prettier run) SILENTLY TRUNCATES the extracted function to a prefix; assertions then fail or, WORSE, `not in` assertions PASS because the forbidden token fell outside the truncated slice - a whole class of cannot-fail outcomes || Extract by brace matching, or move the JS into real .js files
LOW || MEDIUM || leftover-state || tests/test_internal_tasks.py || 29 || 40 || _client || The scratch DB is NEVER deleted, so every local run accumulates tasks and notifications || sqlite:///./scratch/gi_test.db with no unlink - unlike test_rate_limit.py:27-29 and every leads/pipeline file || Assertions scanning for "the latest notification" still pass, but the DB grows unboundedly (528KB after the baseline run) and any future assertion on absolute counts becomes order-dependent on prior runs. Same in test_hub.py:48 and test_notifications_ui.py:49 || Delete the file once at module import
LOW || MEDIUM || runtime || tests/test_leads_deeplink_pinned.py || 214 || 233 || _roda || ~25 tests each spawn a fresh `node -e` process; across the five node-based files the suite spawns well over a hundred interpreters || subprocess.run([node, "-e", script]) called once per assertion group plus a 5-payload loop at 574 || Together with the 5000-lead insert and per-file FastAPI app imports this explains most of the ~60min wall time; per-file process isolation means the app import cost is paid 51 TIMES || Batch the scenarios into one node process per file

AGENT NOTES — COMPLETE LIST OF GENUINE SILENT-SKIP CONDITIONS (body not run, exit 0):
(1) node absent -> the 4 login.js behavioural tests in test_auth_session_consistency.py (332-334, 344, 353, 363, 372).
(2) `git show origin/main:app/routers/leads.py` fails -> test_nenhum_endpoint_novo_foi_criado (test_leads_deeplink_pinned.py:138).
(3) same for pipeline.py -> test_pipeline_nao_ganhou_endpoint_novo (test_pipeline_inline_lead_edit.py:418).
(4) `git diff origin/main` fails -> test_nenhum_py_de_aplicacao_alterado (test_pipeline_inline_lead_edit.py:436).
(5) "animation" not in bloco -> ALL of test_animacao_e_curta_e_nao_infinita (test_pipeline_target_highlight.py:146).
(6) target lead not on page 1 -> the two-funnel assertion (test_leads_keyset_pagination.py:248).
CORRECTION TO A SIBLING AGENT: test_hub.py, test_rate_limit.py, test_internal_tasks.py, test_notifications_ui.py and test_perpetua_internal_auth.py all `raise SystemExit(2)` on ImportError - NON-ZERO, so CI fails. They are LOUD, not silent. httpx is present locally (0.28.1) and installed in CI.
TESTS THAT WOULD KEEP PASSING AFTER A RENAME-THE-STRING-KEEP-THE-BUG REFACTOR: every assertion in test_kanban_ui.py, test_security_greps.py:47-49, test_infra_compose_guard.py, and all _fn()-based forbidden-token-absent assertions when brace extraction truncates.
TESTS THAT BLOCK LEGITIMATE FIXES: test_nenhum_py_de_aplicacao_alterado (test_pipeline_inline_lead_edit.py:426-448) FAILS ANY PR touching app/routers/leads.py, app/routers/pipeline.py, app/schemas/lead.py or app/models/lead.py - the file own docstring records that its earlier broader form already had to be narrowed once. test_compose_crm_recebe_internal_ai_auth_secret (test_leads_segment_drift.py:159) fails on any compose edit that mentions the variable twice.
CRITICAL CRM FLOWS WITH NO REGRESSION TEST AT ALL: /api/users (8 routes - user CRUD, role changes, deactivation), /api/teams (6), /api/tasks (4), /api/analytics (2), and the ENTIRE operational kanban API - boards (9), cards (13), checklists (6), comments (3), flow (3), pending (1); only notifications list + read-all are touched. Also untested: API-key generation/rotation, password change, email verification, admin-vs-non-admin authorization on ANY CRM endpoint (only string counting), the AI tool surface, upload size enforcement, Excel/CSV import-export, and lead delete/cascade.
SQLITE-ONLY GATE: every behavioural assertion in the leads/segments/pipeline files runs against SQLite. Postgres is covered EXCLUSIVELY by substring checks on compiled SQL. Case/accent handling, JSON-to-text coercion, FOR UPDATE, transaction isolation, unique-violation surfacing and timezone-aware datetime comparison are ALL unverified against production.
NOT VERIFIED: the exact blocking call inside test_hub.py (narrowed by the absence of scratch/hub_test.db, not by a stack trace); whether leaked portals actually block interpreter exit in this anyio version; whether origin/main exists in the crm CI job (deduced from checkout defaults); actual Postgres behaviour of any compiled SQL asserted in the dialect tests.
