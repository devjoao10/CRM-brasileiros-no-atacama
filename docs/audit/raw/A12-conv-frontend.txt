AGENT: A12-conv-frontend
RANGES: conversas/static/js/conversas.js 1-2423; settings.js 1-701; templates.js 1-516; auth.js 1-74; page-shell.js 1-39; conversas/templates/conversas.html 1-346; login.html 1-225; settings.html 1-337; templates.html 1-192; static/manifest.json 1-18; static/css/conversas.css 1-2013 — all reviewed, none uncovered.

CRITICAL || CONFIRMED || xss || conversas/static/js/conversas.js || 1864 || 1886 || window._openMedia || Media blob fetched from /api/media/{id} is opened in a new tab via a blob: URL; the Blob INHERITS the response Content-Type, which media.py sets from asset.meta_mime_type (the CUSTOMER-DECLARED mime, served content_disposition_type="inline"), so a WhatsApp message whose media declares text/html renders as HTML in a blob: URL carrying the app own origin || const blob = await resp.blob(); window.open(URL.createObjectURL(blob), '_blank') at 1882 + media.py:80-84 FileResponse(media_type=asset.meta_mime_type or octet-stream, inline) || Any WhatsApp customer can execute script in the agent origin: read localStorage.crm_access_token, read the non-HttpOnly access_token cookie, and drive every authenticated API (assign, send, read all conversations) as that agent || Validate the mime against an allowlist before createObjectURL, force application/octet-stream otherwise, prefer a forced download over window.open, and serve media from a separate sandboxed origin with Content-Disposition attachment
CRITICAL || CONFIRMED || xss || conversas/static/js/conversas.js || 1758 || 1782 || window._showInlineMedia || Same blob-origin hole on the inline-image path: the img that replaces the button gets onclick = () => window.open(url, '_blank') on the same unvalidated blob URL || el.src = url; el.onclick = () => window.open(url, '_blank') at 1769-1773 || Identical to _openMedia - full session takeover from a message a customer sends || Drop the window.open zoom affordance or use a lightbox; never hand a customer-typed mime to window.open
HIGH || CONFIRMED || secret-exposure || conversas/static/js/settings.js || 115 || 115 || renderApiConfig || The Meta webhook verify token is written into a plain input type="text" for every authenticated user; GET /api/config is gated only by get_current_user, and the Settings page renders the whole API tab with no client-side role gate || cfgVerifyToken.value = config.meta_verify_token + settings.html:111 + api_config.py:35-41 || Any agent, or any XSS on any page, can read the webhook verify token in cleartext and use it to complete Meta webhook handshake against the tenant, enabling forged inbound WhatsApp traffic || require_admin on GET /api/config, mask the token as has_verify_token, hide the API tab for non-admins
HIGH || CONFIRMED || auth || conversas/static/js/auth.js || 32 || 35 || Auth.clearAuth/logout || Logout only removes the two localStorage keys; the access_token COOKIE written by login.html is never deleted, and the backend accepts that cookie as a full credential || clearAuth removes only TOKEN_KEY and USER_KEY; no document.cookie expiry; app/auth.py:104 reads the cookie || After "Sair" the session stays valid server-side for the full 8h cookie lifetime; on a shared/kiosk device the next person can hit any /api/ URL and get the previous agent data || Clear the cookie in clearAuth and add a server-side logout that expires it
HIGH || CONFIRMED || auth || conversas/templates/login.html || 171 || 213 || login script || The session JWT is stored in localStorage AND mirrored into a JavaScript-written cookie with NO HttpOnly and NO Secure flag; the same token has two script-readable homes || document.cookie = `access_token=Bearer ${data.access_token}; path=/; max-age=28800; SameSite=Lax` at 213, repeated at 171 || Any XSS (see the media findings) exfiltrates a full 8-hour session token; without Secure the cookie is also sent over a plain-HTTP downgrade || Set the cookie server-side on login with HttpOnly; Secure; SameSite=Lax and stop keeping the raw token in localStorage
HIGH || CONFIRMED || race || conversas/static/js/conversas.js || 342 || 361 || DOMContentLoaded poll || The 5s detail poll captures activeConversation.id before the await but never re-checks it after; if the agent opens another conversation while the request is in flight, the OLD conversation payload is assigned to activeConversation and rendered || the fetch then activeConversation = data with no data.id === activeConversation.id guard || The open thread is silently replaced by a DIFFERENT customer messages, header and phone number; an agent can reply into the wrong conversation || Capture the id before the fetch and bail on mismatch; same guard for loadChat (783-812)
HIGH || CONFIRMED || race || conversas/static/js/conversas.js || 1889 || 1911 || window._retryMessage || The retry button has NO in-flight guard: the handler never disables the clicked button and sets no module flag (contrast tplSending at 898/1202), and the backend only rejects on status != failed, checked BEFORE the async WhatsApp call || the handler plus the button built at 2081 which is never disabled || A double-click (trivial on touch) sends the same message to the customer TWICE, on the same DB row so the duplicate is invisible in the UI || Disable the button and set an in-flight flag; server-side mark the row sending inside a locking transaction before calling Meta
HIGH || CONFIRMED || realtime || conversas/static/js/conversas.js || 1958 || 1991 || renderConversationList || The polling refresh rebuilds the whole conversation list with innerHTML every 5 seconds, destroying and recreating every row and resetting the .conv-list scroll offset to 0 || list.innerHTML = filtered.map(...).join('') called from loadConversations('refresh') on every 5s tick || An agent CANNOT SCROLL past the first screen of conversations for more than five seconds; on a long Todos list the inbox is effectively unusable and Carregar mais results are yanked back to the top || Diff-render by data-id, or at minimum save and restore list.scrollTop
HIGH || CONFIRMED || handoff || conversas/static/js/conversas.js || 348 || 360 || DOMContentLoaded poll || The open conversation is only refreshed when the message COUNT changes or the 24h window flips; atendente_id, responsavel_id, status and tags changes made by other agents are never picked up while the thread stays open || the windowChanged/newCount condition || Two agents can both see Assumir/Liberar for a conversation someone else already claimed (updateClaimButton reads the stale local copy) and both work the same customer; a conversation closed by a colleague still shows as open || Compare a server-provided version/updated_at, or re-render whenever any of id/status/atendente_id/responsavel_id/service_window_open/count differ
MEDIUM || CONFIRMED || error-handling || conversas/static/js/conversas.js || 1243 || 1295 || sendMessage || Clears the composer and paints an optimistic bubble BEFORE the POST, and the whole function has no try/catch; fetch rejects on any network error so the promise rejects unhandled after the text has already been discarded || input.value = '' then appendMessage(status sending) then the await, with no try/catch and no rollback || On a flaky mobile connection the agent typed message is LOST with no error shown, and a permanent fake bubble stays in the thread || try/catch, restore input.value, remove/mark the optimistic bubble, show a toast
MEDIUM || CONFIRMED || realtime || conversas/static/js/conversas.js || 2029 || 2051 || renderChat || Every re-render wipes the message container and forces scroll to the bottom, so a poll bringing one new message destroys any inline media the agent opened (playing audio/video and loaded images revert to buttons), clears text selection and yanks the viewport down mid-read || container.innerHTML = '' then forEach then scrollTop = scrollHeight || Audio being transcribed by the agent STOPS every time the customer sends a message; scrolling back through history is impossible while a conversation is active || Append only new messages and keep scrollTop unless already pinned to the bottom
MEDIUM || HIGH || resource-leak || conversas/static/js/conversas.js || 1765 || 1882 || _showInlineMedia/_downloadMedia/_playAudio/_openMedia || Four URL.createObjectURL call sites and NO URL.revokeObjectURL anywhere in the file; combined with the full chat re-render the same media is re-fetched and re-blobbed repeatedly || createObjectURL at 1765, 1792, 1854, 1882 || Every viewed video/audio/document is pinned in memory for the whole shift; a day-long tab on a low-end phone grows unbounded and eventually crashes || Revoke on unload / after click, and cache blobs by asset id
MEDIUM || CONFIRMED || silent-failure || conversas/static/js/conversas.js || 601 || 653 || btnCloseConv/btnToggleBot || Both handlers await the request and then UNCONDITIONALLY show a success toast without inspecting the response || the PUT then showToast at 604-608 and the identical pattern for is_bot_active at 647-651 || The agent is told the conversation was closed or the bot disabled when the server REFUSED; the bot keeps answering a customer the agent believes they took over || Check resp && resp.ok before toasting and surface readErrorDetail otherwise
MEDIUM || HIGH || race || conversas/static/js/conversas.js || 742 || 781 || loadConversations || A single listRequestSeq counter is shared by user actions AND the 5s poll, and offset for append derives from conversations.length which the poll can change mid-flight || const seq = ++listRequestSeq with offset: String(isAppend ? conversations.length : 0) and the unconditional refresh at 341 || If an append takes longer than 5s the poll bumps the sequence and the page silently discards it - "Carregar mais" does nothing with no feedback; when it does land, rows shifted by new inbound traffic are duplicated or skipped || Separate sequence counters per request kind and a stable cursor instead of a numeric offset
MEDIUM || CONFIRMED || error-handling || conversas/static/js/conversas.js || 1803 || 1832 || sendMediaFile || Media upload bypasses Auth.apiRequest and uses raw fetch, so it has neither the 401 handling nor any try/catch; the caption is cleared before the request || raw fetch with a manual Authorization header then if (resp.ok) || An expired token during upload shows a generic failure instead of a clean re-login; a network drop throws unhandled and loses the typed caption with no toast || try/catch plus an explicit 401 branch mirroring Auth.apiRequest
MEDIUM || MEDIUM || xss || conversas/static/js/settings.js || 273 || 291 || renderAutoReplies || r.trigger is interpolated RAW four times per row - into text, into an element id, and into two inline on* handlers inside single-quoted JS string literals - while the sibling fields go through escapeHtml || span text, id="ar-${r.trigger}", onchange and onclick both interpolating ${r.trigger} || Triggers are currently a server-seeded enum so this is LATENT; the moment a trigger becomes user-nameable it is direct script injection into an inline handler, and today an apostrophe already breaks both buttons || escapeHtml plus addEventListener with dataset.trigger
MEDIUM || HIGH || xss || conversas/static/js/templates.js || 500 || 505 || escapeHtml (weak variant) || This file escapeHtml OMITS the quote-escaping the other two copies have, and two values are interpolated with no escaping at all, one inside a DOUBLE-QUOTED attribute || return div.innerHTML with no quote replace, plus title="Meta ID: ${t.meta_template_id}" at 196 and ${t.language} at 209 || A Meta-sourced or DB-sourced value containing a double quote breaks out of the title attribute and can inject an event handler; the divergence also means any future attribute use of escapeHtml in this file is unsafe || Copy the quote-escaping variant from conversas.js:2397-2404 and route both values through it
MEDIUM || MEDIUM || mobile || conversas/static/css/conversas.css || 53 || 65 || body/.app-layout || The base layout is pinned to 100vh with overflow hidden; the dynamic-viewport fix exists ONLY inside the max-width 640px query, so tablets and landscape phones between 641px and 900px keep the static 100vh || body height 100vh and .app-layout height 100vh vs the 100dvh only at line 1325 || With the on-screen keyboard open at those widths the flex column still measures the full static viewport, pushing .chat-input and the send button below the fold with no way to scroll to it - the agent types BLIND || height: 100dvh at all widths with 100vh kept immediately before as the fallback
LOW || CONFIRMED || xss || conversas/static/js/conversas.js || 1959 || 1970 || renderConversationList/getInitials || Contact initials are the only conversation field interpolated into innerHTML without escapeHtml || const initials = getInitials(conv.nome || conv.whatsapp) then the avatar div - every sibling field uses escapeHtml || Limited to two characters so a script payload is not reachable, but a name like <img src=x yields a raw < that opens a bogus tag and swallows the following static div, corrupting the row markup || escapeHtml(initials)
LOW || CONFIRMED || lifecycle || conversas/static/js/conversas.js || 336 || 362 || DOMContentLoaded poll || pollInterval is assigned once and NEVER cleared, the callback is async so ticks overlap freely, and loadCounts/loadConversations are fired without awaiting || setInterval(async ...) at 336 with no clearInterval anywhere in the file || On a slow link requests pile up faster than they drain; after a 401 every queued request independently reassigns window.location.href while the timer keeps firing || Chain the next tick with setTimeout after the previous cycle resolves, and clearInterval on logout/visibilitychange/401
LOW || HIGH || resource-leak || conversas/static/js/conversas.js || 74 || 84 || notificationState || seenInboundIds and chatBaselined are Sets that only ever GROW - no eviction, no cap, never cleared when a conversation is closed || only .add() calls at 194-197 || Over a full shift with a high-traffic inbox both sets accumulate every message id the agent ever saw; unbounded in a tab that is never reloaded || Cap the set or clear on conversation switch
LOW || CONFIRMED || error-handling || conversas/static/js/auth.js || 23 || 26 || Auth.getUser || JSON.parse on the crm_user localStorage value with NO try/catch, while isAuthenticated only checks the token || getUser returns data ? JSON.parse(data) : null || A truncated or hand-edited crm_user throws inside updateClaimButton (1638) and loadNotes (1471), breaking claim/release and the notes panel with no visible error || try/catch returning null
LOW || CONFIRMED || silent-failure || conversas/static/js/templates.js || 482 || 498 || window._deleteTemplate || The delete handler only reacts to success; there is no else branch, so a 403/409/500 leaves the card on screen with no message || if (resp && resp.ok) { ... } with nothing after; same pattern in settings.js _deleteQR (449-456), _toggleAutoReply (294-300) and disconnectApi (252-261) || The admin believes the deletion is still pending and retries || Add an else branch surfacing err.detail
LOW || HIGH || validation || conversas/static/js/settings.js || 161 || 168 || saveApiConfig || The three !== undefined guards can NEVER be false because .value.trim() always returns a string; the code relies entirely on the backend treating an explicit JSON null as unchanged || const wabaId = ...value.trim(); if (wabaId !== undefined) payload.meta_waba_id = wabaId || null (identical for phone id and verify token) || Harmless today because api_config.py guards each field with is not None, but the frontend is one backend refactor away from silently WIPING the WABA id, phone id and webhook verify token whenever loadApiConfig fails and leaves the fields empty || Only add a key when the field is non-empty, or send an explicit unchanged sentinel
LOW || CONFIRMED || rendering || conversas/static/js/settings.js || 389 || 389 || renderQuickReplies || The preview ESCAPES first and TRUNCATES second, so the 100-character cut can land inside an HTML entity || escapeHtml(qr.content).substring(0,100) with the length check against the raw string || A quick reply whose 100th character region contains < & or a quote renders a mangled fragment such as &qu... || Truncate the raw string first, then escape
LOW || HIGH || pwa || conversas/static/manifest.json || 1 || 18 || manifest || Declares display: standalone with NO service worker registered anywhere in the codebase and a single 1024x1024 icon with purpose "any" only || no sw.js / serviceWorker.register in the repo, and no apple-touch-icon in conversas.html || Installed on a phone, any connectivity gap drops the agent onto a bare browser error page inside a chromeless window with no address bar and no reload affordance; Android renders the icon letterboxed without a maskable variant || Register a minimal offline-fallback service worker or drop to display: browser; add a maskable icon and smaller sizes
LOW || MEDIUM || css || conversas/static/css/conversas.css || 863 || 872 || .conv-sidebar mobile drawer || At the 640px breakpoint the sidebar drawer and the templates dropdown share z-index 100, and the sidebar comes EARLIER in the DOM so the later element wins on ties || .conv-sidebar z-index 100 vs conversas.html:124 #templatesDropdown z-index 100 under the same stacking root || If the templates dropdown is left open when the agent taps back to the list, the dropdown paints over the full-screen drawer and intercepts taps || Give the drawer a higher stacking level or close the dropdown in mobileBack

AGENT NOTES:
- ESCAPING HELPERS FOUND: (1) conversas.js:2397-2404 escapeHtml — textContent->innerHTML then replace " and ' ; correct for text AND quoted attributes; no URL-scheme validation. (2) settings.js:677-684 — byte-identical. (3) templates.js:500-505 — WEAKER: no quote escaping, unsafe in attribute context.
- RENDER PATHS THAT BYPASS ESCAPING: conversas.js:1970 initials; conversas.js:1882/1773 blob URL handed to window.open (the only real XSS reachable from customer content); settings.js:278/281/285/288 r.trigger; settings.js:326/329/332/334 weekday/open_time/close_time; templates.js:196 title attribute and 209 language.
- PATHS THAT ARE ESCAPED CORRECTLY (checked line by line): message body (2086), media_url in img src (2095, quotes escaped so no attribute break-out), failure reason title (2076), document filename in text and data-fn (2108-2109), contact name/preview/responsavel label (1975-1984), tag names (1546/1557/1690) with safeTagColor regex-validating the hex before it reaches style, notes content and author (1479-1480). Quick-reply, variable and template-picker lists are built with createElement/textContent throughout.
- NO auto-linkification of message text exists anywhere, so javascript:/data: URLs in customer text are INERT (rendered as plain text) — the only user-followable customer-controlled URL is the media blob.
- All window._* globals take Number()-coerced ids, so the inline onclick attributes generated in conversas.js are NOT injectable.
- TEMPLATE SEND CONTRACT VERIFIED against conversations.py:_build_template_send (81-141): the frontend correctly sends only the FREE positions in ascending order and the backend rejects wrong arity with 422 — no mismatch. Empty free params are blocked client-side and re-validated server-side.
- tplSending (898/1202-1240) is a correct double-click guard for template sends; sendMessage is incidentally guarded by clearing the input synchronously. _retryMessage is the ONE send path with no guard at all.
NOT VERIFIED: nothing executed; whether Meta actually relays a customer-declared text/html mime (the frontend sink and the backend verbatim pass-through are both confirmed, the upstream relay was not tested); whether AutoReply.trigger rows are strictly seeded; real on-screen-keyboard layout at 641-900px.
