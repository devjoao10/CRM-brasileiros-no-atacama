AGENT: A04-routers
RANGES: app/routers/__init__.py 1-0 (0-byte file); leads.py 1-878; pipeline.py 1-789; segments.py 1-392; tags.py 1-182; users.py 1-239; analytics.py 1-213; tasks.py 1-116; teams.py 1-99; pages.py 1-106 — all reviewed, none uncovered.

HIGH || CONFIRMED || swallowed-exception || app/routers/pipeline.py || 223 || 279 || get_kanban_board || The broad except Exception wrapping the whole handler also catches the intentional HTTPException(404) raised inside the same try, converting "Funil nao encontrado" into a generic 500 || line 226 raises 404 INSIDE the try opened at 223; 277-279 except Exception -> HTTPException(500) || Frontend and n8n cannot distinguish a bad funnel id from a server fault; every 404 is reported as an outage and retried || except HTTPException: raise first, or move the lookup outside the try
HIGH || CONFIRMED || performance-unbounded || app/routers/analytics.py || 129 || 137 || get_detailed_reports || Loads every Lead in the window into memory with joinedload(tags) and joinedload(funnel_entries).joinedload(funnel) and NO limit; the window is caller-controlled and unbounded backwards || all_leads = db.query(Lead).options(joinedload...).filter(created_at between).all() - start_date=1900-01-01 materialises the whole 19k-lead table plus all tags and funnel entries || Memory blow-up / worker stall; reachable 200x per minute per IP under the global slowapi limit || Aggregate with SQL GROUP BY as /dashboard does, and cap the allowed date span
HIGH || CONFIRMED || performance-unbounded || app/routers/segments.py || 339 || 356 || get_segment_leads || Fetches the ENTIRE filtered result set with .all() then slices in Python, so skip/limit never reach SQL - the exact anti-pattern leads.py:398-403 documents as already fixed elsewhere || all_results = fetch_q.order_by(created_at.desc()).all() then leads = unique_leads[skip:skip+limit]; identical in preview_segment 379-386 || A segment matching all leads loads every ORM object per request to return 100; no ORDER BY tiebreaker so paging is unstable on equal created_at || Push offset/limit into the query and add Lead.id.desc() as tiebreaker
HIGH || CONFIRMED || n+1 || app/routers/segments.py || 43 || 50 || _build_lead_response || One FunnelEntry query per lead in the response loop; with limit=500 that is 500 extra round-trips per request || entries = db.query(FunnelEntry).filter(lead_id == lead.id).options(joinedload(funnel)).all() called from 355 and 390 inside a list comprehension || Segment listing/preview latency scales linearly with page size; leads.py avoids this by eager-loading Lead.funnel_entries || selectinload(Lead.funnel_entries).joinedload(FunnelEntry.funnel) in _resolve_segment_query
HIGH || CONFIRMED || concurrency-blocking || app/routers/analytics.py || 115 || 137 || get_detailed_reports || Endpoint is async def but performs BLOCKING synchronous SQLAlchemy I/O, so it runs on the event loop and stalls every other request in the worker || async def at 115 then synchronous db.query(...).all() at 129-137; same pattern in users.py:23/34/73/86/131/168/195/215, tags.py:25/50/62/87/116/134/169, teams.py:196/205/218/230/250, analytics.py:17, leads.py:425/549 || Under load the whole ASGI worker blocks on DB I/O; the heaviest handlers (full-table report, DNS-validating user creation) are precisely the async def ones || Make these plain def so FastAPI runs them in the threadpool - leads/pipeline already do this
MEDIUM || CONFIRMED || route-shadowing || app/routers/users.py || 214 || 215 || verify_email_click || GET /api/users/verify-click is declared AFTER GET /api/users/{user_id}, so Starlette matches the parameterised route first, int-coercion of "verify-click" fails, and the endpoint is unreachable (422, never HTML) || @router.get("/{user_id}") with user_id: int at 72 vs @router.get("/verify-click") at 214; the author avoided this for /for-select by declaring it at line 22 || The e-mail verification link minted at users.py:124 can never be redeemed once SMTP is re-enabled || Move /verify-click above GET /{user_id}
MEDIUM || CONFIRMED || input-validation || app/routers/segments.py || 128 || 166 || _resolve_segment_query || Date filters are free Optional[str] in SegmentFilters and parsed with date.fromisoformat inside the query builder with no try/except, so a malformed value raises ValueError -> uncaught 500 || date.fromisoformat(data_chegada_de) at 129 and datetime.combine(date.fromisoformat(criado_de), ...) at 162-163; app/schemas/segment.py:13-16,27-28 declare them as str || POST /api/segments/preview with criado_de "ontem" returns 500; worse, a segment SAVED once with a bad date permanently 500s GET /api/segments for everyone because list_segments counts every segment || Type the fields as datetime.date so Pydantic returns 422
MEDIUM || CONFIRMED || n+1 || app/routers/segments.py || 205 || 212 || list_segments || One full COUNT over the leads table per saved segment, each with that segment own filters (JSON containment, EXISTS on custom fields) || for seg in segments: resp.lead_count = _count_segment_leads(...) which is a .count() on the whole leads table || With N segments the listing costs N expensive COUNTs; leads.py:218-227 documents the COUNT dominating the request at 19k leads (82% for the custom-field predicate) || Cache, compute lazily, or put it behind an opt-in flag as leads.py does with include_total
MEDIUM || CONFIRMED || dos-upload || app/routers/leads.py || 686 || 692 || import_leads || The entire uploaded body is read into memory BEFORE the size limit is enforced, so MAX_UPLOAD_SIZE_BYTES never prevents the allocation it is meant to bound || content = file.file.read() at 686 then if len(content) > MAX_UPLOAD_SIZE_BYTES: 413 at 688-692 || Any authenticated caller including an n8n API key can force the worker to buffer arbitrarily large uploads before the 413 || Enforce the limit while streaming, or reject on Content-Length first
MEDIUM || CONFIRMED || transaction || app/routers/leads.py || 760 || 791 || import_leads || Per-row try/except cannot catch the errors it advertises: db.add() only STAGES the object, so constraint/length violations surface at the single db.commit() at 784 - one bad row aborts the whole import with an unhandled 500 and no row detail, and there is no db.rollback() || except Exception -> errors.append at 780-781 guards only _process_row/Lead(...); the write is at if imported > 0: db.commit() (783-784); the response reports a success count the commit may never have achieved || The documented partial-success contract is fiction: importing 5000 leads where row 4999 has a 201-char nome returns 500 and imports NOTHING || Flush per row inside a SAVEPOINT (db.begin_nested()), catch IntegrityError/DataError there, rollback the savepoint so the rest still commits
MEDIUM || CONFIRMED || audit-bypass || app/routers/leads.py || 509 || 515 || update_lead || The generic update applies every submitted field blindly, so responsavel_id can be changed here with NO user-existence/is_active check and NO LeadHistory event - bypassing the dedicated endpoint that does both || setattr loop over model_dump(exclude_unset=True) vs update_lead_responsavel at 851-872 which validates User.is_active and writes a responsavel_changed history row in the same transaction || Lead ownership reassigned to a nonexistent or deactivated user (FK violation -> 500 on PostgreSQL) with no audit trail; the conversas responsavel snapshot then has nothing to reconcile against || Exclude responsavel_id from LeadUpdate or route it through the same validation + history helper
MEDIUM || CONFIRMED || dangerous-default || app/routers/leads.py || 836 || 857 || update_lead_responsavel || responsavel_id is an OPTIONAL query parameter defaulting to None, and None is the sentinel for "Agente IA" - so a request that simply OMITS the parameter silently unassigns the lead from its human owner || Query(None, description="ID do novo responsavel (null = Agente IA)") at 838 and lead.responsavel_id = responsavel_id at 857 with no distinction between omitted and explicitly null || A malformed or typo'd n8n call hands the lead back to the AI and logs it as an intentional change || Make the parameter required and use a sentinel such as 0 for Agente IA, matching the convention at leads.py:187 and users.py:29
MEDIUM || CONFIRMED || lost-update || app/routers/leads.py || 568 || 583 || append_anotacao || Classic read-modify-write on a JSON blob with no locking: two concurrent notes (the AI and a human on the same lead) both read the old anotacoes and the later commit overwrites the earlier note || campos = lead.campos_personalizados or {}; existing = campos.get("anotacoes",""); campos["anotacoes"] = new + existing then flag_modified + commit - the whole dict is rewritten || Silent loss of conversation summaries, the exact data this endpoint exists to preserve; campos_personalizados is also clobbered wholesale by any concurrent PUT /{lead_id} || SELECT ... FOR UPDATE on the lead row, or store notes in the existing append-only LeadHistory table
MEDIUM || CONFIRMED || timezone || app/routers/leads.py || 565 || 566 || append_anotacao || Note timestamps use naive server-local time while every other timestamp in the system is DateTime(timezone=True)/UTC || timestamp = dt.now().strftime(...) vs pipeline.py:355 and auth.py:37 which both use datetime.now(timezone.utc) || Annotation timestamps disagree with created_at on the same lead by the container UTC offset, making the history unreadable for a Brazil/Chile team || datetime.now(timezone.utc)
MEDIUM || CONFIRMED || timezone || app/routers/tasks.py || 45 || 49 || list_tasks || Naive datetimes are compared against Task.data_vencimento which is DateTime(timezone=True); PostgreSQL resolves the naive literal using the session TimeZone so day boundaries and overdue shift by the server offset || datetime.combine(due_date, datetime.min.time()) at 45 and datetime.now() at 49 against models/task.py:24; same naive/aware mix in analytics.py:36-37,54,126-127 and leads.py:394-396 || due_date filtering and the overdue/pending KPI silently include or exclude several hours of tasks || Build bounds as timezone-aware UTC and use datetime.now(timezone.utc)
MEDIUM || CONFIRMED || pagination || app/routers/tasks.py || 52 || 53 || list_tasks || Offset pagination with a single NULLABLE non-unique sort key, no tiebreaker, and a bare list response with no total || order_by(Task.data_vencimento.asc()).offset(skip).limit(limit).all(); the column is nullable and NULL ordering differs between SQLite (NULLs first) and PostgreSQL (NULLs last) - the exact dev/prod split app/database.py:7 branches on || Rows with equal or NULL due dates repeat or vanish across pages, and behaviour DIFFERS between dev and prod || Add Task.id.asc(), pin NULL handling with nullslast(), return a total
MEDIUM || CONFIRMED || mass-assignment || app/routers/tasks.py || 63 || 69 || create_task || user_id is taken straight from the request body so any authenticated user can create a task owned by any other user; lead_id, resultado_ia and the google_calendar fields are equally caller-controlled and unvalidated || final_user_id = data.user_id then Task(**task_data, user_id=final_user_id); app/schemas/task.py:12-16 exposes all of them and the comment at schemas/task.py:20 CLAIMS user_id is assigned by the backend - it is not || Task spoofing across users; an invalid lead_id hits the FK and returns 500 instead of 404 || Restrict body-supplied user_id to admins, validate the referenced user and lead, drop the AI/calendar fields from TaskCreate
MEDIUM || CONFIRMED || authorization || app/routers/tasks.py || 91 || 93 || update_task || The ownership check passes for the owner, but TaskUpdate.user_id is then applied, letting a non-admin reassign their own task to a different user - irreversibly, since they lose read access afterwards || the 403 check at 88-89 followed by the setattr loop at 92-93 with user_id: Optional[int] in schemas/task.py:29 || Tasks can be pushed onto other users or onto the AI queue (user_id=None) by any authenticated account || Strip user_id from TaskUpdate for non-admins
MEDIUM || CONFIRMED || race-condition || app/routers/pipeline.py || 581 || 591 || add_lead_to_funnel || posicao is derived from a COUNT immediately before insert, with no locking and no uniqueness on (funnel_id, etapa_id, posicao) || max_pos = ...count() then FunnelEntry(posicao=max_pos); identical in move_lead_stage 633-637 and transfer_lead 697-700; the comment at 287-290 acknowledges posicao REPEATS when entries leave a stage || The legacy board still orders by this column (order_by(FunnelEntry.posicao) at 234) so duplicated positions give nondeterministic card order between page loads || COALESCE(MAX(posicao),-1)+1 in one statement, or drop the column now that the paginated board orders by (updated_at, id)
MEDIUM || CONFIRMED || race-condition || app/routers/pipeline.py || 574 || 592 || add_lead_to_funnel || Duplicate-entry guard is a check-then-insert with NO database constraint behind it, so two concurrent requests both pass the check and both insert || existing = ...first(); if existing: 409 - models/pipeline.py:27-44 defines no UniqueConstraint on (lead_id, funnel_id) and the docstring at 502-503 confirms it; transfer_lead repeats it at 678-683 || Duplicate cards for the same lead in one funnel; locate_lead then picks one arbitrarily and the two entries drift into different stages || UniqueConstraint(lead_id, funnel_id) and convert IntegrityError to 409
MEDIUM || CONFIRMED || data-integrity || app/routers/pipeline.py || 181 || 192 || update_funnel || The stage list can be replaced wholesale with no check that removed stage ids are unreferenced, orphaning every FunnelEntry sitting in a deleted stage || the etapas branch validates only id uniqueness (182-184) then setattr; the board only iterates funnel.etapas (262-270) and get_board_meta only counts stages present in funnel.etapas (400-408) || Leads silently DISAPPEAR from the pipeline - invisible on the board, uncounted in meta, still returned by locate_lead pointing at a stage that no longer exists || Reject removal of a stage that still has entries, or require an explicit migration target
MEDIUM || CONFIRMED || performance-unbounded || app/routers/pipeline.py || 228 || 258 || get_kanban_board || The legacy board loads every FunnelEntry of a funnel with its lead and tags, then filters is_active and responsavel_id in PYTHON instead of SQL || entries = query.all() with no limit at 237 then the Python filters at 248-254; the replacement path _stage_query (332-373) does exactly these filters in SQL || A 3000-card funnel materialises 3000 entries + leads + tags per request; the endpoint is still reachable and still used || Push the filters into the query (reuse _stage_query) and cap per-stage results, or retire the endpoint
MEDIUM || CONFIRMED || input-validation || app/routers/users.py || 112 || 118 || create_user || role is an unconstrained str in the schema but a SAEnum(UserRole) column, so any value outside {admin,user} raises LookupError at flush and returns 500 instead of 422 || User(role=data.role) with role: str = Field(default="user") in schemas/user.py:30 against Column(SAEnum(UserRole)); update_user has the same gap at 158-159 || Admin tooling gets an opaque 500 for a typo and the failed flush aborts the surrounding transaction || Type the field as UserRole in UserCreate/UserUpdate
MEDIUM || CONFIRMED || blocking-io || app/routers/users.py || 95 || 102 || create_user || Performs a live DNS/MX lookup inside an async def handler, blocking the event loop for the resolver timeout || validate_email(data.email, check_deliverability=True) inside async def create_user || A slow or unreachable resolver stalls the entire worker; user creation also fails outright when outbound DNS is filtered || Make the handler def, or validate syntax only
MEDIUM || CONFIRMED || input-validation || app/routers/leads.py || 483 || 483 || create_lead || status_venda is an unvalidated free string while the whole reporting layer hard-codes three values, so any other value is stored and then silently invisible to every KPI || status_venda: str = Field("em_negociacao") with no enum; analytics.py:46 counts only == "venda" and analytics.py:140-142 only em_negociacao/venda/perda || An n8n workflow sending "Venda" or "ganho" creates leads that never appear in conversion metrics and can never be filtered back out || Literal/enum in LeadBase and LeadUpdate
MEDIUM || CONFIRMED || like-injection || app/routers/leads.py || 822 || 826 || get_lead_by_whatsapp || The path segment is interpolated into an ILIKE pattern after a normalisation that strips only + space and -, so LIKE metacharacters % and _ survive and act as wildcards || suffix = normalized[-11:]; Lead.whatsapp.ilike(f"%{suffix}") - GET /api/leads/by-whatsapp/___________ matches any 11-char-suffixed number and returns an arbitrary lead || The conversas auto-link path can be made to bind a conversation to the WRONG lead, exposing that lead name/email/whatsapp; leads.py:171-179 has the same unescaped pattern for search || Reject non-digits after normalisation, or pass autoescape=True as app/query_filters.py:63 already does correctly
MEDIUM || CONFIRMED || performance || app/routers/leads.py || 261 || 268 || list_destinos || Full-column scan pulled into Python to build a distinct list, on every page load of the leads screen || db.query(Lead.destinos).filter(isnot(None)).all() then a Python loop; list_custom_field_keys at 430-436 does the same with campos_personalizados || Two unbounded scans over the whole 19k-row table per screen load, growing linearly || Compute distinct values in SQL (jsonb_array_elements_text / jsonb_object_keys) or cache
MEDIUM || CONFIRMED || race-condition || app/routers/tags.py || 72 || 83 || create_tag || Uniqueness is enforced by check-then-insert against a column that ALSO has a DB unique index, so concurrent creates surface as an unhandled IntegrityError 500 instead of the intended 409 || existing check then db.add/commit against Column(String(100), unique=True); same shape in pipeline.py:144-158, segments.py:251-262, users.py:105-120 || n8n retries and parallel operators get 500s where the API documents 409, and the aborted transaction leaves the request with no rollback path || Catch IntegrityError around the commit and translate to 409; keep the pre-check as a fast path only
MEDIUM || CONFIRMED || authorization || app/routers/leads.py || 440 || 515 || get_lead/update_lead || Lead.responsavel_id MODELS ownership but no read or write path enforces it: every authenticated principal can read and modify every lead || only Depends(get_current_user) with no comparison against lead.responsavel_id; same for pipeline.py move_lead_stage (605-649), transfer_lead (653-712), remove_lead_from_funnel (716-734) and tags.py set_lead_tags (134-165) || Any principal accepted by get_current_user - any JWT holder, ANY n8n API key, and any HMAC-signed internal-AI request naming an arbitrary user id - has full read/write over the whole lead base || If per-owner isolation is intended add an ownership predicate; if not, document the flat trust model so responsavel is not mistaken for access control
LOW || CONFIRMED || error-handling || app/routers/pipeline.py || 620 || 621 || move_lead_stage || The funnel lookup result is dereferenced without a None check, so a missing funnel produces an AttributeError 500 instead of 404 || funnel = ...first() immediately followed by funnel.etapas; transfer_lead has the same unguarded old_funnel.nome at 686/691 while remove_lead_from_funnel at 725-729 DOES guard it || Reachable if the FK is not enforced (SQLite without the PRAGMA, or a schema created without the constraint); inconsistent with defensive handling three functions away || Add the 404 check get_funnel already uses
LOW || CONFIRMED || inconsistent-contract || app/routers/pipeline.py || 450 || 457 || get_stage_cards || An invalid or forged cursor is SILENTLY ignored and the first page returned, whereas the equivalent code in leads.py raises 400 || _cursor_decode returns None on failure (323-329); leads.py:49-58 raises HTTPException(400, "Cursor invalido") for the same condition || A client with a corrupted cursor silently restarts pagination from the top and re-renders duplicates instead of getting a handleable error || Align the two behaviours and document which
LOW || CONFIRMED || inconsistent-contract || app/routers/segments.py || 353 || 356 || get_segment_leads || Returns LeadListResponse without has_more/next_cursor so the schema defaults has_more=False even when more pages exist || schemas/lead.py:196-197 declare the defaults; leads.py:415-421 correctly computes has_more; preview_segment repeats the omission at 388-391 || A frontend that trusts has_more stops paging a segment after the first page || Set has_more = skip + len(leads) < total in both
LOW || CONFIRMED || robustness || app/routers/leads.py || 44 || 46 || _cursor_encode || Assumes created_at is never NULL, unlike the analogous helper in pipeline.py which explicitly guards it; Lead.created_at is nullable in the DDL and conversas writes to this table with raw SQL || base64(f"{lead.created_at.isoformat()}|{lead.id}") vs pipeline.py:310-320 which returns None when updated_at is None, with a comment about rows inserted by direct SQL || A single lead row with NULL created_at turns the whole leads listing into a 500 at the cursor step; currently latent because conversas/app/services/crm.py:190-194 does set created_at NOW() || Mirror the pipeline.py guard or make the column nullable=False
LOW || CONFIRMED || authorization || app/routers/tags.py || 62 || 91 || create_tag/update_tag || Tag creation and renaming are open to any authenticated user while DELETION requires admin - an inconsistent gate on the same global resource || get_current_user on create (65) and update (90) vs require_admin on delete (118) || Any account including an n8n API key can rename a tag referenced by saved segments (tag_ids in models/segment.py:32) and by every lead card, changing what those segments visibly mean || One policy for the tag vocabulary across all three verbs
LOW || CONFIRMED || validation-gap || app/routers/teams.py || 276 || 281 || set_team_members || Silently drops unknown user ids instead of reporting them, and the response count masks the discrepancy || users = query.filter(User.id.in_(user_ids)).all(); team.users = users; return f"Equipe atualizada com {len(users)} membros" - tags.py:153-157 handles the same case correctly by raising 404 with the missing ids || An admin submitting 10 ids of which 3 are stale sees "7 membros" and must notice the number themselves || Reuse the tags.py pattern
LOW || CONFIRMED || duplicate-creation || app/routers/teams.py || 205 || 215 || create_team || No uniqueness check and no unique constraint on the team name, unlike every sibling resource || Team(nome=data.nome) with Column(String(100), nullable=False) and no unique=True, while tags, funnels, segments and users all check || Repeated POSTs create indistinguishable teams; membership becomes ambiguous in the UI selector || Unique constraint on teams.nome plus the 409
LOW || CONFIRMED || error-handling || app/routers/analytics.py || 121 || 127 || get_detailed_reports || Missing the start_date > end_date guard its sibling endpoint has, and l.created_at.strftime assumes a non-null column || no equivalent of analytics.py:32-33; line 159 uses created_at.strftime with created_at nullable in models/lead.py:41 || Inverted ranges return an empty report with no explanation instead of 400; a NULL created_at gives a 500 || Copy the range validation from /dashboard and guard null created_at
LOW || CONFIRMED || ux-regression || app/routers/pages.py || 322 || 387 || dashboard_page and 8 others || Only /gestao/pendencias forwards the requested path as ?next=; the other nine protected pages drop it, so an expired session always lands on /hub instead of the requested page || page_login_redirect(request) at 326,334,342,350,358,366,373,379,386 vs page_login_redirect(request, "/gestao/pendencias") at 318; login.js:6-10,46,117 does honour and sanitise next || Deep links from conversas into /pipeline or /leads lose their destination whenever the cookie has expired || Pass request.url.path as next_url in all of them
LOW || CONFIRMED || inconsistent-guard || app/routers/users.py || 158 || 159 || update_user || delete_user explicitly forbids self-deactivation but update_user applies is_active and role from the body with no such check, so the same action is reachable through the other verb || the setattr loop with is_active: Optional[bool] and role: Optional[str] in schemas/user.py:40-41 vs users.py:181-185 which raises 400 on self-deactivation || The last admin can demote or deactivate themselves via PUT and lock every admin route out of the system || Apply the self-modification guard in update_user and refuse to demote the last remaining admin

AGENT NOTES — ENDPOINT INVENTORY (method path :: auth dependency):
leads.py — GET /api/leads, /destinos, /segment, /segment/campos-personalizados-chaves, /{id}; POST /api/leads, /import; PUT /{id}, /{id}/anotacoes, /{id}/responsavel; GET /by-whatsapp/{whatsapp} :: get_current_user. DELETE /{id} :: require_admin.
pipeline.py — GET /funnels, /funnels/{id}, /board/{fid}, /board/{fid}/meta, /board/{fid}/stage/{eid}, /locate/{lid}, /history/{lid}; POST /funnels/{fid}/leads, /entries/{eid}/transfer, /history/{lid}/note; PUT /entries/{eid}/move; DELETE /entries/{eid} :: get_current_user. POST/PUT/DELETE /funnels :: require_admin.
segments.py — GET /api/segments, /{id}, /{id}/leads; POST /preview :: get_current_user. POST /api/segments, PUT /{id}, DELETE /{id} :: require_admin.
tags.py — GET /api/tags, /{id}, /lead/{lid}; POST /api/tags; PUT /{id}, /lead/{lid} :: get_current_user. DELETE /{id} :: require_admin.
users.py — GET /for-select :: get_current_user. GET /api/users, /{id}; POST; PUT /{id}; DELETE /{id}; POST /{id}/verify-email :: require_admin. GET /verify-click :: NO AUTH (token only) AND UNREACHABLE.
analytics.py — GET /dashboard, /reports :: get_current_user.
tasks.py — GET/POST /api/tasks, PUT /{id}, DELETE /{id} :: get_current_user (+ per-row owner-or-admin check on PUT/DELETE only).
teams.py — all :: require_admin.
pages.py — / and /login public; the other 11 pages :: require_page_session, which resolves the cookie through the SAME _get_user_from_jwt as get_current_user (signature, exp, user exists, is_active). VERIFIED CLEAN: no page trusts mere cookie presence, and redirect targets are hardcoded server-side and sanitised client-side.
app/routers/__init__.py is a 0-byte file; routers are imported directly by app/main.py:18-20.
NOT VERIFIED: nothing executed; claims about missing unique constraints and nullable columns reflect the SQLAlchemy model definitions, not the live production schema.
