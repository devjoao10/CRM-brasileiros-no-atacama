AGENT: A08b-frontend
RANGES: templates/base.html 1-62; login.html 1-114; hub.html 1-137; dashboard.html 1-320; equipes.html 1-818; segmentacao.html 1-646; tarefas.html 1-572; tags.html 1-244; relatorios.html 1-231; gestao/pendencias.html 1-312; operational/boards.html 1-242; operational/pending.html 1-194; partials/_sidebar.html 1-124; _topbar.html 1-44; static/js/auth.js 1-108; login.js 1-146; layout.js 1-59; notifications.js 1-146; static/css/base.css 1-112; components.css 1-449; layout.css 1-296; login.css 1-349; variables.css 1-120 — all reviewed, none uncovered.

CRITICAL || CONFIRMED || xss || templates/tarefas.html || 437 || 437 || renderTasks || The task object is serialized with JSON.stringify into a SINGLE-QUOTED onclick attribute; only ' is entity-escaped, & is NOT, so a title containing the literal text &quot; is HTML-decoded by the parser into a real double quote that terminates the JSON string and starts JS expression context || onclick='editTask(${JSON.stringify(t).replace(/'/g, "&#39;")})' - a task titled &quot;});alert(1);// renders as editTask({"titulo":""});alert(1);//"...}) after attribute decoding || Stored XSS executed on EVERY VIEWER of the task list (admins included); with NO CSP anywhere the payload reads the JWT from localStorage and the access_token cookie - full account takeover from any authenticated low-privilege user || Render data-task-id and attach the handler with addEventListener, looking the task up in allTasks by id (the pattern gestao/pendencias.html:255-263 already uses)
HIGH || CONFIRMED || xss || templates/tags.html || 185 || 185 || loadTags || esc() converts ' to &#39; but the value is placed inside a JS STRING inside an HTML ATTRIBUTE - the parser decodes &#39; back to a real quote BEFORE the JS is compiled, so the escaping does not protect this context at all || onclick="deleteTag(${t.id}, '${esc(t.nome)}')" - a tag named x');alert(1);// decodes to deleteTag(1, 'x');alert(1);//') || Stored XSS on the Tags page for every user; no CSP; token theft via localStorage || Pass only t.id through the attribute and resolve the name in JS from the loaded array
HIGH || CONFIRMED || xss || templates/equipes.html || 718 || 718 || renderTeams || Same &#39;-decodes-to-quote break-out; team name injected into a JS string inside an onclick || onclick="deleteTeam(${t.id}, '${esc(t.nome)}')" || Stored XSS reachable by anyone who can create/rename a team; the Equipe page is ADMIN-FACING so it targets the highest-privilege users || data-team-id + addEventListener
HIGH || CONFIRMED || xss || templates/segmentacao.html || 392 || 392 || loadSegments || Same break-out; segment name injected into a JS string inside onclick || onclick="deleteSegment(${s.id},'${esc(s.nome)}')" || Stored XSS on the Segmentacao page || id-only in the attribute, resolve the name in JS
HIGH || CONFIRMED || xss || templates/segmentacao.html || 309 || 309 || renderDestinoChips || Destino value (free text from /api/leads/destinos, i.e. LEAD-SUPPLIED data) is interpolated RAW - not through esc() - into both a JS string AND an HTML attribute || `<span class="fb-tag-chip${sel?' sel':''}" onclick="toggleDestino('${d}')">${esc(d)}</span>` - note esc(d) IS used for the visible text but the onclick copy is unescaped || A destino containing a quote breaks the attribute outright; stored XSS driven by LEAD DATA which arrives from the n8n/API integration rather than a trusted operator || data-destino via element.dataset and addEventListener
HIGH || CONFIRMED || xss || templates/relatorios.html || 223 || 227 || renderTableGeneric || The breakdown keys (custom-field origem values, tag names, funnel/etapa names) are concatenated into innerHTML with NO escaping; this page has NO esc() helper at all || tbody.innerHTML += `<tr><td><span style="...">${k}</span></td>...` where k comes from data.breakdown.origems/.tags/.etapas || Stored XSS on the reports page. The page is presented as admin-only, so a low-privilege user who can set a lead custom field or a tag name lands a payload in the ADMIN browser || Escape k with a shared helper, or build the row with createElement/textContent
HIGH || CONFIRMED || xss || templates/segmentacao.html || 608 || 609 || renderDetailTable || Lead email and WhatsApp are inserted into innerHTML WITHOUT esc() while the adjacent l.nome cell IS escaped - the omission is inconsistent, not intentional || <td>${l.email||...}</td> and <td>${l.whatsapp||...}</td> vs <td><strong>${esc(l.nome)}</strong></td> on line 607 || Stored XSS from lead data (which the n8n/API integration writes without operator review) in the segment leads side panel || esc() both
HIGH || CONFIRMED || auth || static/js/auth.js || 57 || 81 || Auth.apiRequest || The client UNCONDITIONALLY attaches Authorization: Bearer <localStorage token>, and app/auth.py:180-184 raises 401 on a present-but-invalid Bearer WITHOUT falling through to the cookie branch at 186-195. A stale localStorage token therefore POISONS EVERY API CALL even when the cookie is perfectly valid || if (token) { headers['Authorization'] = ... } then if (response.status === 401) { this.clearAuth(); window.location.href = '/login'; } || The server-rendered page loads fine (cookie valid), then the first XHR 401s, wipes localStorage and BOUNCES THE USER TO /login MID-SESSION - including from the 60s notifications poller, losing whatever was typed in an open modal. This is the client half of the confirmed backend fall-through defect || Send the Bearer header only when there is no cookie session, or drop it entirely; server-side, fall through to the cookie
HIGH || CONFIRMED || auth || static/js/login.js || 30 || 53 || DOMContentLoaded || When Auth.isAuthenticated() is true the handler RETURNS at line 51 before any form wiring. All THREE failure paths inside that branch (session invalid 34-37, hop-guard 38-44, network error 48-50) call clearAuth() and leave the user staring at a login form with NO SUBMIT LISTENER and no focus || the isAuthenticated branch with the return at 51 skipping lines 55-127 || The user CANNOT LOG IN until they manually reload; the exact scenario is "localStorage token survived but the cookie expired", i.e. the common expiry case || Move the form wiring above the branch, or re-run setup after clearAuth()
HIGH || CONFIRMED || security || templates/login.html || 68 || 102 || loginForm || The form has NO action and NO method, and its submit handler is conditionally never attached (see the previous finding). A submit in that state performs the BROWSER DEFAULT: GET to the current URL with the named inputs as QUERY PARAMETERS - including name="password" || <form id="loginForm" novalidate> with <input type="password" id="password" name="password"> || The PLAINTEXT PASSWORD lands in the URL bar, browser history, the server access log and any proxy log. Reachable whenever login.js takes one of its early-return paths || Add onsubmit="return false" / action+method as a floor, remove name from the password input, and fix the listener gap
MEDIUM || CONFIRMED || auth || static/js/auth.js || 86 || 94 || Auth.logout || The logout fetch result is never inspected: a rejected promise is swallowed by an empty catch, and a non-2xx response does NOT reject in fetch at all, so a 500 or 401 is indistinguishable from success || try { await fetch('/api/auth/logout', {method:'POST'}) } catch (e) { } then clearAuth() and redirect || The user is told they are logged out while the access_token cookie may still be LIVE on the server, so re-navigating restores the session. On a shared machine this is a real session-fixation exposure || Check response.ok; on failure surface an error, or at minimum expire the cookie client-side
MEDIUM || CONFIRMED || auth || app/main.py || 115 || 123 || SecurityHeadersMiddleware || Cache-Control: no-store is applied only to _NO_STORE_PATHS, /api/auth/* and 302s to /login. The authenticated PAGE SHELLS get no cache directive at all || the branch at 117-122; pages.py also serves /dashboard, /leads, /tags, /pipeline, /segmentacao, /equipe, /tarefas, /relatorios, /gestao/pendencias, /operational/* || After logout the back button re-renders the cached authenticated shell from bfcache/disk before the JS auth gate fires; on a shared browser this leaks the org structure and the previous user name/role || Add every page route to the no-store set
MEDIUM || CONFIRMED || authz || templates/relatorios.html || 134 || 139 || DOMContentLoaded || The admin restriction is enforced PURELY IN THE BROWSER from the crm_user blob in localStorage, and the backing endpoint has NO admin check - analytics.py:114-119 depends only on get_current_user || if (user && user.role !== 'admin') { alert(...); window.location.href = "/dashboard"; return; } || Any authenticated NON-ADMIN can read the full company report (all leads, all sales, all losses, all breakdowns) by calling /api/analytics/reports directly or by editing crm_user.role in localStorage || Add Depends(require_admin) to /api/analytics/reports; treat the client check as cosmetic
MEDIUM || HIGH || reliability || static/js/notifications.js || 144 || 145 || setInterval || The poller is installed once and never cleared, has no visibility gating, no in-flight guard and no backoff. Every 60s it calls Auth.apiRequest, which on 401 clears auth and navigates away || fetchNotifications(); setInterval(fetchNotifications, POLL_MS) with POLL_MS = 60000 || A backgrounded tab polls forever; when the token expires the POLLER - not a user action - triggers the redirect, discarding any half-filled modal without warning. Requests also pile up when a throttled tab resumes || Skip when document.hidden, guard overlapping requests, make the 401 path non-navigating for background polls
MEDIUM || CONFIRMED || reliability || templates/equipes.html || 585 || 598 || fetchUsers/fetchTeams || Both helpers check only if (resp) and never resp.ok. On any non-401 error the body is parsed anyway and allUsers/allTeams silently become [] - or resp.json() throws, rejecting the Promise.all so renderUsers/renderTeams never run || the two helpers; segmentacao.html:362-365 already implements the correct pattern || A 403 or 500 renders as "Nenhum usuario cadastrado" - a server failure indistinguishable from an empty system - or leaves "Carregando..." forever with no retry || Check resp.ok and render an explicit error state with retry
MEDIUM || CONFIRMED || reliability || templates/tarefas.html || 361 || 366 || fetchTasks || No resp.ok check; the JSON body is assigned straight to allTasks and immediately mapped || allTasks = await resp.json(); renderTasks(); - on a 500 FastAPI returns {"detail": ...}, an object with no .map, so renderTasks throws || The list stays at "Carregando tarefas..." permanently with an uncaught TypeError and no recovery but a reload || Guard on resp.ok and Array.isArray
MEDIUM || CONFIRMED || reliability || templates/tags.html || 165 || 174 || loadTags || Same missing resp.ok; data.tags.length dereferences a property that does not exist on an error payload || the fetch then if (data.tags.length === 0) || Permanent "Carregando tags..." on any server error || Check resp.ok and render an error/retry state
MEDIUM || CONFIRMED || reliability || templates/gestao/pendencias.html || 175 || 192 || loadUsers/loadTasks || Both bail out with a bare return on failure, leaving the initial placeholder markup and an EMPTY responsavel select || if (!r || !r.ok) return; with the grid keeping the loading empty-state from line 88 || Indefinite spinner with no error message; the "Nova Pendencia" modal also opens with an empty REQUIRED select, so submitting sends assignee_id: NaN || Render an explicit error state and disable create while users is empty
MEDIUM || CONFIRMED || reliability || templates/dashboard.html || 241 || 245 || loadAnalytics || The failure path only writes to console.error - the four KPI tiles and the chart are left at their placeholder with nothing indicating an error || if (!response || !response.ok) { console.error(...); return; } || A backend outage looks like an EMPTY CRM on the primary landing page; users act on blank numbers || Inline error banner with retry
MEDIUM || CONFIRMED || reliability || templates/relatorios.html || 156 || 162 || loadReports || The button label is reset but never DISABLED, and on failure the function returns silently leaving all three tables at "Carregando dados..." || reqBtn.textContent = 'Gerando...' then if (!resp || !resp.ok) return || Repeated clicks fire overlapping report queries against an EXPENSIVE endpoint (analytics loads every Lead in range) and the last-ARRIVING response wins; failures are invisible || Disable the button and render an error state
MEDIUM || HIGH || race || templates/segmentacao.html || 562 || 573 || debouncePreview/previewCount || The 300ms debounce applies ONLY to text inputs; all 14 onchange handlers call previewCount() directly, with no request sequencing || the debounce helper vs onchange="previewCount()" on the selects || The "N leads correspondem a estes filtros" preview can display a count for a filter set the user ALREADY CHANGED, and it is the number they base the saved segment on || Monotonic request id or AbortController
MEDIUM || HIGH || race || templates/tarefas.html || 247 || 254 || tab click handler || Tab switching calls fetchTasks() with no in-flight cancellation and no guard that the response still matches currentTab || the click handler then allTasks = await resp.json() unconditionally || Rapid tab switching can leave one tab visually active while showing ANOTHER tab tasks, including tasks the user then edits or deletes || Capture currentTab at request time and discard on mismatch
MEDIUM || CONFIRMED || double-submit || templates/equipes.html || 660 || 686 || saveUser || The Save button is never disabled and the modal is not closed until the response arrives, so every extra click fires another POST /api/users || no guard before the await; tarefas.html:550-556 shows the correct pattern IN THE SAME CODEBASE || Duplicate user accounts from an impatient double-click; on the email-unique path the second request 409s and the user sees an error after a successful create || Disable before the await, re-enable in a finally
MEDIUM || CONFIRMED || double-submit || templates/equipes.html || 766 || 797 || saveTeam || Same missing guard, AND the follow-up member-assignment POST at 787 is not checked for ok either - a failed membership write reports success || the members POST then closeTeamModal(); loadAllData(); || Duplicate teams plus SILENT LOSS of team membership changes || Disable the button and check the members-POST response
MEDIUM || CONFIRMED || double-submit || templates/segmentacao.html || 499 || 516 || saveSegment || No submit guard; "Salvar Lista" stays clickable for the whole POST/PUT || the await with no disable || Duplicate segmentation lists || Disable around the await
MEDIUM || CONFIRMED || double-submit || templates/gestao/pendencias.html || 287 || 310 || submitTask || A real form submit handler with no button disable, so Enter-Enter or a double-click posts twice || onsubmit = submitTask with no guard || Duplicate internal pendencias assigned to the same person || Disable and re-enable in a finally
MEDIUM || CONFIRMED || double-submit || templates/operational/boards.html || 217 || 238 || createBoardForm.onsubmit || No submit guard, and the failure branch reports a FABRICATED CAUSE for every non-ok status || else { alert("Apenas administradores podem criar novos quadros operacionais!") } - a 500, a 422, or response === null (the 401 path) all produce the same misleading permission message || Duplicate boards; a real server error is misdiagnosed as a permission problem, sending users down the wrong support path || Disable during the request and branch on response.status
MEDIUM || CONFIRMED || css || templates/operational/pending.html || 8 || 12 || .content-area override || The page overrides the GLOBAL .content-area into a hard two-column grid with NO media query anywhere in the file; layout.css only adjusts padding at 640px, it does not reset grid-template-columns || .content-area { display: grid; grid-template-columns: 1fr 1fr; ... } with .pending-card-box padding || On a 375px phone each column is ~160px minus 40px padding, so titles, descriptions and the "Marcar lida" buttons are compressed to unusable width - the page is EFFECTIVELY BROKEN on mobile, and it is the sidebar "Cards Operacionais" destination || Add a single-column media query and scope the override to a page-specific class
MEDIUM || HIGH || css || templates/tarefas.html || 55 || 57 || .task-card.is-overdue || --color-danger is NEVER DEFINED - variables.css defines --color-error and the legacy alias block does not alias danger. An undefined custom property makes the declaration invalid at computed-value time, so border-left-color resolves to currentColor (dark body text) rather than red || border-left-color: var(--color-danger) at 55 and 57 || Overdue and cancelled tasks LOSE their only strong visual marker; the accent bar renders near-black, leaving only a faint tint. Overdue work stops standing out || Use var(--color-error) or add the alias
MEDIUM || HIGH || css || templates/segmentacao.html || 10 || 23 || --font-weight-* usage || --font-weight-bold/semibold/medium are used across FIVE assigned templates but are NOT DEFINED in variables.css (which defines --fw-bold etc.). font-weight is an INHERITED property, so the invalid declaration falls back to the INHERITED value, not to the earlier cascade value || .seg-header h1{...font-weight:var(--font-weight-bold)} while base.css:38-43 already set h1..h6 to --fw-semibold, so this rule makes the heading LIGHTER than an unstyled h1; same at equipes.html:203/305, tarefas.html:14, tags.html:10, relatorios.html:20, dashboard.html:115 || Every page heading, card title, table header and KPI value using the legacy name renders at NORMAL WEIGHT; the visual hierarchy silently collapses on SIX PAGES || Add the aliases to variables.css
MEDIUM || CONFIRMED || supply-chain || templates/dashboard.html || 7 || 7 || extra_head || Chart.js is loaded UNPINNED from a third-party CDN with no SRI, and there is no CSP header anywhere in the app || <script src="https://cdn.jsdelivr.net/npm/chart.js"></script> - same unpinned URL in relatorios.html:7; tarefas.html:7 at least pins fullcalendar but still has no integrity || An upstream major release breaks the dashboard and reports page with no change on our side; a CDN compromise executes arbitrary script in an authenticated session that keeps the JWT in localStorage, with no CSP to contain it || Pin the version, add integrity+crossorigin, or vendor into /static
MEDIUM || HIGH || css/js || templates/tarefas.html || 314 || 339 || initCalendar || FullCalendar is constructed and render()ed on page load while #calendarView is display:none, so it measures a ZERO-WIDTH container. switchView calls render() again, which is a NO-OP on an already-rendered calendar - the API for re-measuring is updateSize() || calendar.render() at 338 with the hidden div at 163, then if (calendar) { calendar.render(); } at 308-310 || The calendar view can render collapsed or mis-sized on first switch until the window is resized || calendar.updateSize(), or construct lazily on first switch
LOW || CONFIRMED || xss || templates/equipes.html || 710 || 710 || renderTeams || t.cor is interpolated RAW into a style attribute while the identical value four lines earlier at 614 goes through esc() || style="border-left-color: ${t.cor}" vs esc(t.cor) at 614 || Exploitability is limited because schemas/team.py:8 caps cor at max_length=7, too short to close the attribute and open a handler - but the field has NO hex-format validator so a malformed value can still corrupt the attribute || esc() here too, plus a #RRGGBB validator in the schemas
LOW || CONFIRMED || xss || templates/equipes.html || 806 || 810 || showAlert || The error message is written with innerHTML instead of textContent, and the value comes straight from the server response body || div.innerHTML = `<span>Erro:</span> <span>${msg}</span>` called with err.detail at 684 and 795 || If any API error detail ever echoes user input (a common FastAPI pattern for duplicate-name errors) it executes. Every sibling page uses textContent for this || textContent on a dedicated span
LOW || CONFIRMED || robustness || templates/gestao/pendencias.html || 155 || 155 || module scope || Auth.getUser() is called at SCRIPT-PARSE TIME, before the DOMContentLoaded auth gate at 160, and getUser runs JSON.parse with no try/catch || const me = Auth.getUser(); at top level || A corrupted crm_user throws during parsing, KILLING THE ENTIRE INLINE SCRIPT - the auth gate never runs, no handlers bind, and the page sits at "Carregando pendencias..." with no redirect || try/catch in auth.js getUser that clears the key and returns null, fixing all callers at once
LOW || CONFIRMED || robustness || static/js/auth.js || 33 || 36 || Auth.getUser || JSON.parse on a localStorage value with no error handling; EVERY page calls this (layout.js:23, hub.html:130, dashboard.html:207, tarefas.html:241, relatorios.html:134, boards.html:149, pendencias.html:155) || getUser returns data ? JSON.parse(data) : null || ONE corrupted entry hard-breaks EVERY PAGE in the app with no self-recovery, since the recovery code lives after the throw || try/catch, clearAuth() and return null
LOW || HIGH || css || templates/hub.html || 46 || 68 || .sector-card.operacional || --color-secondary is UNDEFINED in variables.css, so border-top-color and color fall back to currentColor/inherited || the two rules; also components.css:385 and segmentacao.html:99 || The three hub sector cards were designed to be colour-coded and the Operacional one loses its accent entirely; the atacama destination chip becomes visually identical to outro || Define --color-secondary (the intended value appears to be #C05621 per the matching rgba backgrounds)
LOW || CONFIRMED || css || templates/equipes.html || 415 || 415 || content block || The page opens its OWN .content-area inside the one base.html already provides at line 50, so the global padding rule applies TWICE || the duplicate wrapper; same in operational/boards.html:104 and operational/pending.html:69 || 96px of horizontal padding instead of 48px on desktop, narrowing the users table and teams grid; on pending.html the duplicate also nests a grid inside a grid, compounding the mobile squeeze || Remove the inner wrapper from all three
LOW || CONFIRMED || css || templates/tags.html || 39 || 39 || .empty-state || --space-16 is not defined (the scale stops at --space-12). padding is NOT inherited so the invalid declaration resolves to the INITIAL value, 0 || padding: var(--space-16); same at segmentacao.html:30 || Empty states render flush against their container, reading as a rendering glitch rather than a deliberate state || Use --space-12 or define --space-16
LOW || CONFIRMED || css || templates/equipes.html || 279 || 279 || .modal || --shadow-xl is not defined (the scale stops at --shadow-lg); box-shadow is not inherited so it resolves to none || box-shadow: var(--shadow-xl); same at segmentacao.html:37, tarefas.html:94, tags.html:45 || All four modals render with NO elevation against the blurred overlay on the four heaviest CRUD screens || Use --shadow-lg or define --shadow-xl
LOW || CONFIRMED || bug || templates/equipes.html || 571 || 576 || switchTab || The function relies on the implicit global window.event to find the clicked tab rather than receiving the event or the element || event.currentTarget.classList.add('active') invoked from onclick="switchTab('usuarios')" || window.event is a non-standard legacy holdover; if the function is ever called programmatically or from an async continuation, event is undefined or stale and the tab state desynchronises || Pass the element or bind with addEventListener
LOW || CONFIRMED || a11y/css || templates/segmentacao.html || 85 || 86 || .detail-panel || The leads side panel is moved OFF-SCREEN with right:-680px and never hidden from the accessibility or focus tree - no visibility:hidden, no inert, no display:none || the two rules toggled by classList add/remove('open') || Keyboard users tabbing through the page hit the panel close button, its Exportar CSV button and every table cell while the panel is INVISIBLE off-screen, with focus apparently vanishing || visibility: hidden (transitioned) or the inert attribute in the closed state
LOW || CONFIRMED || reliability || templates/segmentacao.html || 622 || 635 || exportLeads || The response is checked for null but not for ok, then d.leads is dereferenced || const d = await r.json(); const leads = d.leads || []; || A 500 silently produces "Nenhum lead para exportar" - the user concludes the segment is empty rather than that the export failed || Check r.ok and surface the failure
LOW || CONFIRMED || xss || templates/tarefas.html || 423 || 423 || renderTasks || t.status and t.tipo are interpolated raw into double-quoted data attributes with no escaping, unlike t.titulo on line 427 which uses esc() || data-status="${t.status}" data-tipo="${t.tipo}" || Constrained by the server-side enum today so not exploitable, but an unguarded sink the moment those fields become free-text; the CSS selectors at 52-55 depend on the exact values || Escape both, or map through a known-value allowlist

AGENT NOTES — CLIENT-SIDE AUTH IS A THREE-WAY SPLIT WITH NO SINGLE OWNER:
(1) The SERVER decides per page request via require_page_session (app/auth.py:218-229), which reads ONLY the access_token cookie.
(2) layout.js:20 re-decides on every page via Auth.requireAuth(), which checks ONLY localStorage['crm_access_token'] PRESENCE - it never validates the token, so a valid-but-expired JWT passes and a cleared localStorage fails despite a live cookie.
(3) Auth.apiRequest decides a THIRD time per XHR by attaching the localStorage token as a Bearer header; app/auth.py:180-184 then 401s on a bad Bearer WITHOUT falling through to the cookie branch. On that 401, auth.js:74-78 wipes local state and hard-navigates to /login - from ANY page, including the 60s poller, discarding open modal state.
The AUTH-LOOP-01 fix (login.js:26-51, auth.js HOP_KEY) closes the specific /login-/hub ping-pong by revalidating against /api/auth/me with credentials same-origin and no Bearer header, plus a one-shot sessionStorage hop guard. It does NOT unify the three deciders; it only breaks the loop after one bounce. The ?next= handling in safeNext() (login.js:9-21) IS genuinely safe - new URL(raw, origin) plus a same-origin check rejects //evil.com, https://evil.com and javascript:.
NEW FAILURE MODE INTRODUCED BY THAT FIX: every early-return inside the isAuthenticated() branch calls clearAuth() and then hits the return on line 51, which skips the form wiring on 55-127. The user gets a login form with NO submit listener; pressing Enter performs the browser default - a GET to /login with name="email" and name="password" IN THE QUERY STRING.
Logout is incomplete in both directions: auth.js:86-94 ignores the fetch result (a 500 does not reject in fetch), and only /login, /hub, /api/auth/* and /login-bound 302s get Cache-Control: no-store, so authenticated page shells stay in bfcache.
There is NO CSP header anywhere in the repo, which turns each of the SIX confirmed XSS sinks into full session theft (the JWT is in localStorage). The shared esc() helper is copy-pasted into six files and is CORRECT for HTML text but WRONG for the JS-in-attribute context it is most often used in: escaping ' to &#39; does not help because the HTML parser decodes the entity back to a real quote before the JS is compiled.
NOT VERIFIED: nothing executed; bfcache back-button behaviour, FullCalendar zero-width measurement and CSS custom-property fallback rendering are marked HIGH rather than CONFIRMED where they depend on runtime; whether /api/leads/destinos values are operator- or integration-supplied.
