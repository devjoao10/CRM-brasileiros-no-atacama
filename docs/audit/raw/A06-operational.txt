AGENT: A06-operational
RANGES: all 21 assigned files read 100% (2127 lines); no uncovered ranges.

HIGH || CONFIRMED || data-integrity/ordering || app/repositories/operational_card_repo.py || 18 || 28 || list_cards_by_board/list_cards_by_list || Kanban cards have no persisted order: OperationalCard has no position column and both list methods return query.all() with no order_by || return query.all() vs sibling list_lists_by_board using order_by(OperationalList.position.asc()); CardMoveRequest carries only to_list_id (app/schemas/operational.py:242) and templates/operational/kanban.html:617-627 posts only to_list_id || Card order is whatever the DB returns; on PostgreSQL an UPDATE rewrites the row to the heap tail so every edit reshuffles the column; two users see different orders for the same board || Add position column, order_by(position,id), extend CardMoveRequest with target index, renumber siblings inside the move transaction
HIGH || CONFIRMED || concurrency/lost-update || app/services/operational_flow_service.py || 16 || 56 || move_card || Unguarded read-modify-write on card.list_id: plain SELECT, no row lock, no version column, no conditional UPDATE || card = card_repo.get_card(card_id) resolves to .first() with no with_for_update(); from_list_id = card.list_id (33); update_card(card, board_id/list_id) (41-44) || Last writer wins silently and the loser still gets 200 plus a movement row; audit trail records two movements both claiming from_list_id=A || Re-read with with_for_update() or add a version column and conditional UPDATE; 409 on stale from_list_id
HIGH || CONFIRMED || authorization/integrity || app/services/operational_flow_service.py || 24 || 44 || move_card || move trusts client to_list_id and silently reassigns card.board_id to the destination list board; create_card enforces the same invariant and update_card forbids changing it || to_board_id = to_list.board_id then update_card(...) with no board comparison; create_card raises "A lista informada nao pertence ao quadro informado" (app/services/operational_card_service.py:51-52); update_card pops board_id/list_id (76-78) || Any authenticated user teleports any card into any board; the card OperationalCardFieldValue rows keep pointing at the origin board so set_field_value permanently rejects them while list_field_values still returns them - unrepairable through the API || Reject move when to_list.board_id != card.board_id, or migrate/drop field values in the same transaction
HIGH || CONFIRMED || authorization/idor || app/routers/operational_cards.py || 30 || 366 || all operational routers || No ownership or membership concept at all: no owner column on OperationalBoard, no board-members table; every non-admin route is Depends(get_current_user) plus an existence check || Routes at lines 35,57,78,125,151,180,203,330 depend only on get_current_user; the only ownership check in the whole module is notification.user_id != user_id (app/services/operational_notification_service.py:33) || Any low-privilege account can enumerate and mutate every board, card, comment, checklist and assignment in the company with no audit distinction || Board membership table plus assert_board_access(user, board_id) invoked in the SERVICE layer so non-HTTP callers inherit it
MEDIUM || CONFIRMED || cascade/orphan || app/services/operational_board_service.py || 33 || 71 || archive_board/archive_list || Archiving is a single-row flag flip that never cascades to lists or cards || archive_board sets board.is_archived=True only (app/repositories/operational_board_repo.py:30-32); cards still pass the OperationalCard.is_archived==False filter in app/services/operational_pending_service.py:18 || Archived boards keep pushing their cards into every user my-pending; cards in an archived list vanish from the Kanban yet stay fully mutable via API || Cascade the flag inside one transaction or derive effective archived state from the parent chain; extend the move guard to to_list.board.is_archived
MEDIUM || CONFIRMED || validation/dead-code || app/services/operational_card_service.py || 27 || 35 || list_cards_by_board/list_cards_by_list/create_field_definition/list_field_definitions || Four methods call the repo getter purely for a side effect and discard the result, so the commented existence validation never happens || comment Valida existencia do board followed by self.board_repo.get_board(board_id) with the return dropped (28-29, 34, 170, 184); correct pattern exists at get_card_or_404 (21-25) || GET /api/operational/boards/999999/cards returns 200 [] instead of 404; a bogus board_id on field-definition create fails only at commit as an opaque 500; the routers ValueError-to-404 branches are unreachable || Use a get_board_or_404 helper that raises ValueError
MEDIUM || CONFIRMED || concurrency/duplicates || app/services/operational_card_service.py || 112 || 147 || add_assignee || Check-then-insert with no UNIQUE constraint anywhere in app/models/operational/ to fall back on || existing_link = get_assignee_link(card_id,user_id); if existing_link: return; add_assignee(...) (121-125); OperationalCardAssignee (app/models/operational/card.py:62-75) declares no UniqueConstraint || Two concurrent assign requests create two link rows; the user then receives duplicate notifications forever and remove_assignee deletes only one so the duplicate is unremovable || UniqueConstraint(card_id,user_id) plus a dedupe migration; catch IntegrityError as the idempotent path
MEDIUM || CONFIRMED || concurrency/duplicates || app/services/operational_card_service.py || 219 || 224 || set_field_value || Same check-then-insert race on custom field values, no UNIQUE(card_id,definition_id), and the reader uses .first() on an ambiguous set || get_field_value filters and takes .first() (app/repositories/operational_card_repo.py:80-84); OperationalCardFieldValue (app/models/operational/card.py:104-114) has no unique constraint || Two value rows for one field; card detail flips between reads; the field renders twice - multiple sources of truth with no way to tell which value is current || UniqueConstraint(card_id,definition_id) and an upsert instead of read-then-branch
MEDIUM || CONFIRMED || concurrency/ordering || app/services/operational_board_service.py || 50 || 64 || create_list/update_list || List position is client-supplied, never normalized, not unique, siblings never shifted, and the sort has no tiebreak || create_list passes data straight to the repo; schema default position=0 (app/schemas/operational.py:36); order_by(position.asc()) alone (app/repositories/operational_board_repo.py:41); kanban.html:673 sends position=lists.length || Duplicate positions order non-deterministically so board columns swap places between page loads for different users; gaps never self-heal || Renumber the board lists in one transaction, add (board_id,position) uniqueness, sort by (position,id)
MEDIUM || CONFIRMED || security/injection || app/services/operational_comment_service.py || 81 || 84 || _parse_and_create_mentions || A SQL LIKE pattern is built from user-controlled text whose character class includes the LIKE single-character wildcard underscore, and ambiguity is resolved with an unordered .first() || tokens = re.findall(r"@(\w+)", content) then User.email.like(token + "@%"); the comment text @a_min compiles to LIKE 'a_min@%' which matches admin@... || A user can direct a mention record and a notification at an account they never named, including the admin; where several users match, an arbitrary one is picked || Escape LIKE metacharacters or restrict the token regex to [A-Za-z0-9]; compare the email local-part by equality; reject rather than silently pick on ambiguity
MEDIUM || HIGH || performance/dos || app/services/operational_comment_service.py || 69 || 98 || _parse_and_create_mentions || Unbounded N+1 driven by request body size: one SELECT plus one INSERT plus one flush per mention token, with no cap and no max_length on the comment || for token in tokens: fresh query(User).first() (81-84) then create_notification which flushes (app/services/operational_notification_service.py:24); CommentCreate.content is Field(..., min_length=1) with no max_length (app/schemas/operational.py:214) || A single comment with thousands of @x tokens issues thousands of round trips while holding a pooled connection (pool_size 10, max_overflow 20) - cheap pool exhaustion that stalls the whole CRM || Add max_length to content, cap processed tokens, resolve all tokens with one WHERE IN plus a bulk insert
MEDIUM || CONFIRMED || notifications/duplicates || app/services/operational_comment_service.py || 74 || 98 || _parse_and_create_mentions || Dedup is on tokens not resolved users, self-mentions notify the actor, inactive users are still mentioned despite the warning text claiming otherwise, and the warning uses bare print || tokens = list(set(tokens)) dedupes strings only so @admin and @Admin both fire; no user.id != current_user.id guard unlike move_card; no is_active filter; print(...) at line 102 while a logger exists (app/auth.py:18) || Duplicate mention notifications for one comment, self-notifications, mentions accumulating on deactivated accounts, and the compliance warning invisible in production logs || Dedup by resolved user id, skip the actor, filter is_active, replace print with logger.warning
MEDIUM || CONFIRMED || timezone || app/services/operational_checklist_service.py || 95 || 126 || update_checklist_item/toggle_checklist_item || Naive local-time datetimes written into DateTime(timezone=True) columns while sibling timestamps come from the DB clock || data["checked_at"] = datetime.now() (95) and update_data["checked_at"] = datetime.now() (126) into Column(DateTime(timezone=True)) (app/models/operational/checklist.py:39); same defect at app/repositories/operational_notification_repo.py:26 and :33; correct form at app/auth.py:37 || checked_at and read_at are offset from every DB-generated timestamp by the host UTC offset, so audit timelines interleave two clocks and read can appear to precede created || Use datetime.now(timezone.utc) in all four places, or func.now()
MEDIUM || CONFIRMED || validation/500 || app/services/operational_checklist_service.py || 90 || 100 || update_checklist_item || Explicit JSON null survives exclude_unset=True and is written into a NOT NULL column by the repository blind setattr loop || body is_checked null gives setattr(item,'is_checked',None) (app/repositories/operational_checklist_repo.py:32-36) into Column(Boolean, default=False, nullable=False); same shape for CardUpdate.title/is_archived and ListUpdate.name/position/is_archived || Any client sending an explicit null - a common JS pattern for clearing a field - gets an opaque 500 instead of a 422, on card, list, board and checklist-item updates || Forbid null for non-nullable fields in the schemas, or filter None values for those keys before the setattr loop
MEDIUM || CONFIRMED || validation/silent-corruption || app/services/operational_card_service.py || 198 || 217 || set_field_value || Custom field values are routed by field_type but never validated against it; select_options and is_required are never enforced anywhere || db_data["value_boolean"] = bool(raw_val) stores True for arbitrary text; value_date = raw_val with only a comment as the check; definition.select_options never referenced; is_required (app/models/operational/card.py:86) never read || Boolean fields silently record True for junk, select fields accept values outside their options so reporting groups on non-existent values, date fields 500 or store unparsed strings, required fields are never required || Validate raw_val against field_type and select_options membership, raising ValueError to 400
MEDIUM || CONFIRMED || layering || app/routers/operational_cards.py || 176 || 195 || list_card_assignees || The router reaches through the service into its repository, bypassing the service layer every sibling endpoint uses || service.get_card_or_404(card_id) then return service.card_repo.list_assignees(card_id) (184-185) vs service.list_field_values(card_id) at 311 || Any rule later added at service level - board access, filtering inactive users, pagination - will silently not apply to this route || Add OperationalCardService.list_assignees and have the router call only that
MEDIUM || CONFIRMED || api-contract || app/routers/operational_flow.py || 26 || 48 || move_operational_card || Declares response_model=CardMovementResponse but returns a different JSON shape via JSONResponse on the no-op path; FastAPI does not validate raw Response objects || if movement is None: return JSONResponse(200, status no_change) (39-46) while the service returns None for a same-list move || OpenAPI and generated clients break on the no_change branch; a rejected no-op is indistinguishable from a successful move to the UI which only checks response.ok (kanban.html:628-630) || Return 204 for the no-op or use a union response model
MEDIUM || CONFIRMED || performance/pagination || app/repositories/operational_notification_repo.py || 14 || 18 || list_notifications_by_user || No list endpoint in the module has pagination or a limit, and the fastest-growing tables are never pruned || .all() with no limit/offset; same in operational_flow_repo.py:20-27, operational_comment_repo.py:13-14, operational_card_repo.py:18-28 and operational_pending_service.py:13-25 || The notification bell and /api/operational/my-pending degrade continuously for every user; long-lived boards make /cards and /activity-logs the slowest routes || Limit/offset or keyset pagination with a bounded default page size; cap the pending payload
LOW || CONFIRMED || authorization/error-semantics || app/services/operational_notification_service.py || 27 || 39 || mark_as_read || The one genuine authorization check raises ValueError, which the router maps to 400 Bad Request || raise ValueError permission message (33-34) mapped to HTTP_400_BAD_REQUEST with detail=str(e) (app/routers/operational_notifications.py:44-49) || Authorization failures are indistinguishable from malformed requests in logs and alerting; the distinct message confirms the id exists, an enumeration oracle || Dedicated PermissionError mapped to 403 with a generic message
LOW || CONFIRMED || consistency/business-rule || app/services/operational_card_service.py || 37 || 58 || create_card and archived-card write rules || The archived-means-read-only rule is enforced on two write paths and ignored on six, and creation into archived containers is not blocked || create_checklist and create_comment raise; update_checklist_item, toggle_checklist_item, set_field_value, add_assignee and remove_assignee do not; create_card validates existence but never is_archived || Archived cards keep accumulating state no view surfaces; new cards created into archived columns are invisible in the UI yet count in my-pending || Central assert_card_writable(card) called by every mutating service method; check is_archived on board and list in create_card
LOW || CONFIRMED || audit || app/services/operational_card_service.py || 73 || 93 || update_card || Archiving through the generic PUT records action update instead of action archive || CardUpdate.is_archived (app/schemas/operational.py:87) passes through the setattr loop and logs updated_fields is_archived; same hole via BoardUpdate and ListUpdate || Queries filtering OperationalActivityLog.action == archive miss every archive done through PUT; unarchiving has no audit action at all || Strip is_archived in update_card and route it through archive_card and a new unarchive_card
LOW || CONFIRMED || audit || app/services/operational_board_service.py || 20 || 71 || board and list mutations || All six methods accept current_user and never use it; no created_by column and no activity log for structural changes || current_user unused in all six; OperationalBoard and OperationalList have no created_by; card-level operations do write activity logs || Who created a column or archived the board that made a team cards disappear is unanswerable || created_by column plus a board-scoped activity log for structural mutations
LOW || CONFIRMED || observability || app/routers/operational_boards.py || 37 || 41 || all routers, generic except Exception || Every route ends in a bare except Exception that discards the exception and returns a fixed message with no logging; several bind e and never use it || except Exception as e: raise HTTPException(500, fixed message) at 37-41, 61-66, 83-87, 108-113, 132-137, 159-163, 177-188, 205-214, 228-238 and in all seven sibling routers; app/main.py registers no global exception handler || Every failure mode above surfaces as an identical untraceable 500 with no stack trace anywhere || logger.exception before raising the sanitized HTTPException

AGENT NOTES:
- No denormalized checklist completion counter exists, so no counter divergence; multiple-sources-of-truth shows up instead as duplicate OperationalCardFieldValue rows.
- move_card does keep the card UPDATE, movement row, activity log and notifications in one transaction committed once, so there is no partial write there.
- No migration file creates the operational tables (migrations/ covers only leads/tasks/conversas); the schema comes from Base.metadata.create_all, so adding constraints needs a new migration plus data dedupe.
- Top three fixes by value: board membership check in the service layer; with_for_update plus destination-board check in move_card; a real position column on cards.
NOT VERIFIED: no runtime reproduction of the races; boards.html and pending.html not read by this agent; /api/users not traced.
