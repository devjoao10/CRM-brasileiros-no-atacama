AGENT: A03-models
RANGES: app/models/__init__.py 1-0 (empty); chat.py 1-40; internal_task.py 1-41; lead.py 1-50; pipeline.py 1-76; segment.py 1-41; tag.py 1-29; task.py 1-46; team.py 1-29; user.py 1-32; operational/__init__.py 1-1, board.py 1-85, card.py 1-121, checklist.py 1-66, notification.py 1-107; app/schemas/__init__.py 1-0 (empty); internal_task.py 1-84; lead.py 1-204; operational.py 1-284; pipeline.py 1-178; segment.py 1-64; tag.py 1-36; task.py 1-39; team.py 1-31; user.py 1-68 — all reviewed, none uncovered. Enum behaviour verified by RUNTIME check on SQLAlchemy 2.0.35.

CRITICAL || CONFIRMED || enum || app/models/user.py || 21 || 21 || User.role || SAEnum(UserRole) persists the member NAME, so the DB holds "ADMIN"/"USER" while every Python reader sees "admin"/"user"; any row written with the LOWERCASE value by the other service or by raw SQL is UNREADABLE by the CRM ORM || RUNTIME CHECK on SQLAlchemy 2.0.35: INSERT of UserRole.ADMIN stores 'ADMIN'; UPDATE users SET role='admin' then an ORM read raises LookupError: 'admin' is not among the defined enum values (Possible values: ADMIN, USER); Postgres DDL emits a native enum whose labels are the NAMES || Every request that loads that user row (login, /api/users, joinedload(Lead.responsavel)) returns 500; the account is unrecoverable through the UI || Add values_callable=lambda e: [m.value for m in e] so the VALUE is persisted, plus a one-off UPDATE users SET role=lower(role); or drop SAEnum for String(20) + CHECK
HIGH || CONFIRMED || cross-service || conversas/app/seed.py || 54 || 54 || seed_dev_user || The Conversas service INSERTs into the SHARED users table with role="admin" (raw lowercase) through its String(20) declaration, producing exactly the row the CRM cannot read || User(nome="Admin Dev", ..., role="admin") over conversas/app/auth.py:19 __tablename__ = "users"; conversas/app/main.py:27 seed_dev_user() runs at every startup, guarded only by CONVERSAS_SEED_DEV_DATA (default true in development) || With both services on one DATABASE_URL the dev/staging admin becomes a 500-generating row in the CRM; on Postgres the same INSERT fails outright so Conversas cannot boot || Write "ADMIN", or fix the CRM to persist values and normalize the column once
HIGH || CONFIRMED || enum || app/models/task.py || 26 || 27 || Task.status/Task.tipo || Same NAME-vs-value split: the DB stores PENDENTE/EM_ANDAMENTO/CONCLUIDO/CANCELADO and MANUAL/AUTOMATICA while the API returns the lowercase values, so every out-of-ORM reader compares against the wrong form || runtime check: rows land as ('PENDENTE'),('CONCLUIDO') and Postgres DDL is a native enum with NAME labels; the AI SQL tool advertises the OPPOSITE at ai_tools.py:89 (status [pending, in_progress, completed]) and ai_tools.py:100 lets the model run that SQL || n8n, BI queries and the built-in AI SELECT tool silently return ZERO ROWS for status filters; ORM-side comparisons still work so the bug is invisible from the app || values_callable on both columns (plus data migration) and correct the schema string handed to the LLM
HIGH || CONFIRMED || validation || app/schemas/user.py || 30 || 42 || UserBase.role/UserUpdate.role || role is an unconstrained str in BOTH create and update and is written straight onto the SAEnum column, with no CHECK constraint anywhere || role: str = Field(default="user") and role: Optional[str] = None feed users.py:116 and the setattr loop at 158-159; RUNTIME CHECK: on SQLite user.role = "superadmin" COMMITS and stores it (SQLAlchemy 2.0 defaults create_constraint=False), after which every ORM read of that row raises LookupError; on Postgres the same write raises DataError -> 500 || An admin typo permanently BRICKS a user account in dev/SQLite (and in the whole test suite) and 500s in production; behaviour differs between dev and prod || Type the field as UserRole in both schemas, or add create_constraint=True / a CHECK
HIGH || CONFIRMED || mass-assignment || app/schemas/task.py || 13 || 29 || TaskBase.user_id/TaskUpdate.user_id || user_id (the OWNERSHIP column) is client-settable on create AND update, directly contradicting the comment two lines below it || schemas/task.py:20 CLAIMS "user_id is automatically assigned by the backend based on current user" but routers/tasks.py:63-69 honours a body-supplied value and 88-93 checks ownership of the CURRENT row then blindly setattrs including user_id || Any non-admin can plant tasks in another user list or hand their own task to someone else (losing access to it) || Drop user_id from TaskCreate/TaskUpdate (keep it in TaskResponse) and restrict re-assignment to an admin-only route
HIGH || CONFIRMED || nullability || app/schemas/lead.py || 94 || 110 || LeadUpdate || Fields mapping to NOT NULL columns are declared Optional[...] = None, and the update route copies EXPLICIT JSON NULLS straight onto the model || nome/campos_personalizados/status_venda/is_active all Optional in the schema versus models/lead.py:14,31,34,35 all nullable=False; routers/leads.py:509-511 model_dump(exclude_unset=True) then setattr - exclude_unset KEEPS explicitly-sent nulls; the same pattern is repeated in tags.py:97, users.py:145, segments.py:281, pipeline.py:174, teams.py:59 and every operational router || Uncaught IntegrityError -> 500 on a plain PUT || Reject null for non-nullable fields with a field_validator, or filter None values in the shared update path
HIGH || CONFIRMED || cross-service || app/models/user.py || 14 || 26 || User (vs conversas User) || The two declarations of the SAME users table disagree on FOUR columns; whichever service runs create_all first wins, and neither can insert a row the other accepts || CRM nome String(100) vs conversas String(200); CRM role SAEnum vs conversas String(20); CRM email_verified Boolean NOT NULL with NO server_default has NO counterpart in the conversas model; CRM api_key unique+indexed vs conversas neither; both services call create_all at startup and create_all NEVER ALTERs || A fresh shared database created by Conversas LACKS email_verified entirely -> the CRM 500s on every user query; created by the CRM -> Conversas cannot create users || Extract one shared model module (or one Alembic-owned schema) and have Conversas import it
MEDIUM || CONFIRMED || defaults || app/models/lead.py || 31 || 35 || campos_personalizados/status_venda/is_active || NOT NULL columns whose defaults exist ONLY in Python, never in the DDL, so anything that inserts without the ORM fails || compiled Postgres DDL is `campos_personalizados JSON NOT NULL, status_venda VARCHAR(30) NOT NULL, is_active BOOLEAN NOT NULL` with NO DEFAULT clause; same pattern in internal_task.py:25,30,32 and operational/* is_archived/position || Any raw-SQL import, n8n direct insert or second-service write must know all three column names or the INSERT is rejected || Add server_default alongside every default= on NOT NULL columns
MEDIUM || CONFIRMED || types || app/models/lead.py || 31 || 31 || Lead.campos_personalizados || Declared as generic JSON, which compiles to Postgres `json` (text-backed, no equality operators, NOT indexable) even though the only production consumer immediately casts it to jsonb on every row || compiled DDL `campos_personalizados JSON NOT NULL`; app/query_filters.py:53 `jb = cast(coluna, JSONB)` then jsonb_each_text - a PER-ROW cast used by both leads.py and segments.py || No GIN index is usable, the custom-field filter is a full scan at 19k leads, and the runtime cast is what makes the reported NUL-escape 500 reachable || Declare the column as postgresql.JSONB (with a JSON variant for SQLite) and add a GIN index
MEDIUM || CONFIRMED || constraints || app/models/task.py || 38 || 39 || Task.user_id/Task.lead_id || The ONLY two FKs in the whole app declared WITHOUT ondelete, so the DB blocks the delete instead of cascading and the router compensates by hand || plain ForeignKey with no ondelete versus CASCADE/SET NULL on every other FK; routers/leads.py:533-535 works around it by deleting tasks manually; FKs ARE enforced in dev too (database.py:24-28 PRAGMA foreign_keys=ON) || Any hard delete of a lead that does NOT go through that one route (raw SQL, the other service, a future endpoint) fails with an FK violation || ondelete CASCADE on lead_id and SET NULL on user_id, then drop the manual cleanup
MEDIUM || CONFIRMED || constraints || app/models/pipeline.py || 32 || 34 || FunnelEntry || No UNIQUE(lead_id, funnel_id) even though the create route enforces it with a SELECT-then-INSERT, and etapa_id is a free string pointing into a JSON blob nothing keeps in sync || routers/pipeline.py:574-579 does the check-then-insert; 181-184 lets an admin rewrite funnel.etapas with no check against existing entries; the board builder then does stage_entries.get(stage["id"], []) || Concurrent adds duplicate a lead in one funnel; removing a stage makes every lead in it SILENTLY VANISH from the Kanban while its rows stay in the table || UniqueConstraint(lead_id, funnel_id) and reject/remap stages that still have entries
MEDIUM || CONFIRMED || constraints || app/models/operational/card.py || 104 || 109 || OperationalCardFieldValue || No UNIQUE(card_id, definition_id) although the service treats the pair as a primary key (read-modify-write via .first()) || repositories/operational_card_repo.py:80-83 and services/operational_card_service.py:220-224 || Two concurrent writes to the same custom field create two rows; from then on one value is invisible and every later write silently updates only one || UniqueConstraint(card_id, definition_id)
MEDIUM || CONFIRMED || constraints || app/models/operational/card.py || 62 || 67 || OperationalCardAssignee || Same class: the join row is guarded only in Python || services/operational_card_service.py:120-125 || Duplicate assignees on a card (double notification, double count in the my-pending view) || UniqueConstraint(card_id, user_id) - the composite-PK pattern already used by lead_tags and user_teams
MEDIUM || CONFIRMED || constraints || app/models/operational/notification.py || 91 || 94 || OperationalCardMovement || The AUDIT table cascades away: the destination board/list FKs are NOT NULL + ON DELETE CASCADE while the source ones are SET NULL, so deleting a list ERASES the movement history that references it || from_board_id/from_list_id SET NULL nullable versus to_board_id/to_list_id CASCADE NOT NULL; same shape at notification.py:71-72 for OperationalActivityLog.card_id || Deleting a column silently REWRITES HISTORY - the audit trail loses exactly the rows an auditor would look for || Make the to_* columns nullable with ondelete SET NULL (an audit row must outlive the entity it describes)
MEDIUM || CONFIRMED || validation || app/schemas/user.py || 1 || 42 || UserBase.email/UserUpdate.email || EmailStr is IMPORTED AND NEVER USED; email format is validated in exactly one route, so the update path writes arbitrary strings into a UNIQUE NOT NULL column that doubles as the JWT subject || the import at line 1 with email: str at 29 and Optional[str] at 40; users.py:96 calls validate_email only in create_user while update_user checks duplicates but never the format; the value becomes the token identity at routers/auth.py:47 || An admin can save email = "nope" and LOCK THE ACCOUNT OUT of every login path; identity is unverified data || Use EmailStr (already imported) in both schemas, or call validate_email in update_user too
MEDIUM || CONFIRMED || validation || app/schemas/lead.py || 21 || 22 || LeadBase.email/whatsapp || The two contact columns the whole product depends on have NO format validation at all - only empty-string-to-None coercion || the only validator touching email is empty_str_to_none_str at 81-87; no validate_email import anywhere outside routers/users.py; both columns are indexed and searched with ilike || Garbage phone/email from n8n and WhatsApp webhooks lands unnoticed; downstream messaging silently fails per-lead || EmailStr for email and a digits/E.164 field_validator for whatsapp, normalizing before storage since the column is also the dedup key
MEDIUM || CONFIRMED || enum || app/models/lead.py || 34 || 34 || Lead.status_venda || A pseudo-enum kept as a free String with NO CHECK, written from paths that use values the readers do not know || the column comment documents three values; ai_tools.py:168 documents a FOURTH to the LLM ('arquivado') and 177-178 writes it unchecked; routers/analytics.py:140-142 counts only the three || A lead the AI marks 'arquivado' DISAPPEARS from every dashboard total without appearing anywhere as an error - the numbers just stop adding up || CHECK constraint or SAEnum with values_callable, and align the AI tool docstring
MEDIUM || CONFIRMED || constraints || app/models/lead.py || 37 || 38 || Lead.responsavel_id || The model DOCUMENTS a sentinel value that the FK makes IMPOSSIBLE, and the write paths never translate it || the comment "0 = Agente IA" immediately above a ForeignKey("users.id"); the read paths translate 0 to NULL (leads.py:187-189, 344-345) but leads.py:484 passes it straight through, and users.py:29 SERVES the sentinel to clients || An API/n8n client that posts the documented responsavel_id=0 gets an FK IntegrityError -> 500 instead of an AI-owned lead || Coerce 0 to None in a field_validator on LeadBase/LeadUpdate
MEDIUM || CONFIRMED || validation || app/schemas/operational.py || 119 || 120 || CardFieldDefinitionCreate.field_type || field_type is a free string the service uses as a dispatch key with NO else-branch, so an unrecognized type SILENTLY DISCARDS the value instead of failing || no pattern/Literal on the schema; services/operational_card_service.py:208-216 dispatches on three branches - anything else leaves value_text/value_date/value_boolean all None and still COMMITS and returns 200; select_options and is_required are never enforced on write || A board configured with a typo'd type accepts user input and stores NULL forever, reporting success || Literal on the schema plus an else: raise in the service and a membership check against select_options
MEDIUM || CONFIRMED || schema-contract || app/schemas/operational.py || 85 || 85 || CardUpdate.list_id || The schema ADVERTISES a field the service deliberately discards, so a client moving a card via PUT gets 200 and NO MOVE || services/operational_card_service.py:76-78 pops board_id and list_id; the activity log at :88 STILL RECORDS it as an updated field || Silent no-op writes plus a misleading audit entry || Remove list_id from CardUpdate, or 400 when present
MEDIUM || CONFIRMED || types || app/models/operational/card.py || 16 || 20 || DateTime(timezone=True) across all models || timezone=True is a NO-OP on SQLite, which is both the dev database AND the entire test suite, so tz correctness is untested and the API datetime serialization DIFFERS between dev and prod || RUNTIME CHECK on SQLAlchemy 2.0.35 + SQLite: a value of 2026-01-01 10:00-03:00 is stored as '2026-01-01 10:00:00.000000' and read back NAIVE, while Postgres returns aware; server_default=func.now() resolves to SQLite UTC CURRENT_TIMESTAMP but to the session-local now() on Postgres; response models declare bare datetime so dev emits ...T10:00:00 (parsed as LOCAL by the browser) and prod emits ...+00:00 || Off-by-hours due dates and overdue flags that NO TEST CAN CATCH; routers/tasks.py:49 compounds it by comparing the tz-aware column against a naive datetime.now() || Store UTC explicitly, use datetime.now(timezone.utc) in comparisons, and add an explicit serializer so both environments emit the same format
MEDIUM || CONFIRMED || schema-contract || app/schemas/segment.py || 36 || 43 || SegmentCreate.filtros/SegmentUpdate.filtros || Filters are WRITTEN through a closed model but READ BACK as an open dict, so any stored key SegmentFilters does not declare is silently DROPPED the next time the segment is saved (Pydantic default extra=ignore) || filtros: SegmentFilters on write versus filtros: dict on SegmentResponse; routers/segments.py:288-293 re-serializes whatever validated through; no extra="forbid" anywhere in app/schemas || A round-trip through the UI quietly DELETES filter criteria written by an older version or by another service; the segment then targets the wrong audience for automations || extra="forbid" on SegmentFilters, and keep the stored dict when the client does not send filtros
MEDIUM || CONFIRMED || cross-service || conversas/app/models/conversation.py || 44 || 44 || Conversation.lead_id || Conversations point at leads.id with NO ForeignKey at all, while the CRM offers a HARD DELETE of leads || plain Integer with no ForeignKey; routers/leads.py:516-541 delete_lead is a hard db.delete that manually cleans tags, funnel entries, history and tasks - and knows NOTHING about conversations || Deleting a lead leaves ORPHANED WhatsApp conversations pointing at a non-existent id; nothing errors and nothing cleans them up || Declare the FK now that both services share one database, or soft-delete leads
LOW || CONFIRMED || validation || app/schemas/segment.py || 17 || 22 || mes_chegada/tag_mode || Range- and membership-free filter fields that fail silently rather than loudly || mes_chegada/mes_partida with no ge=1 le=12 (compare pipeline.py:14 which DOES use ge=1), and tag_mode: str = "any" with no Literal compared by equality so any typo silently means any || A segment saved with month 13 or tag_mode="ALL" returns the WRONG AUDIENCE with no error || Field(None, ge=1, le=12) and Literal["any","all"]
LOW || CONFIRMED || performance || app/models/tag.py || 9 || 14 || lead_tags || The association table is indexed only by the composite PK leading column, so the "leads carrying tag X" direction has NO usable index || the composite PK is (lead_id, tag_id), leaving tag_id unindexed; the segment engine filters that direction at 19k-lead scale || Sequential scan of lead_tags on every tag-based segment resolution || Index on tag_id (same for user_teams.team_id)
LOW || MEDIUM || nullability || app/schemas/pipeline.py || 129 || 129 || HistoryResponse.dados || Response field is a NON-optional dict over a NULLABLE column, so a single NULL row 500s the whole history endpoint || dados: dict = {} versus models/pipeline.py:68 Column(JSON, default=dict) nullable with a Python-side default only || The lead-history list fails ENTIRELY rather than degrading for one row || Optional[dict] = None, or server_default on the column

AGENT NOTES — FULL ENUM INVENTORY (column -> declaration -> value ACTUALLY persisted -> readers):
1. users.role -> SAEnum(UserRole{ADMIN:"admin",USER:"user"}) -> DB stores "ADMIN"/"USER" (Postgres native type userrole, labels = NAMES) -> readers: app/auth.py:202 role != "admin" OK (ORM re-inflates); routers/tasks.py:29,88,110 != UserRole.ADMIN OK; services/internal_task_service.py:53,117 == "admin" OK; routers/users.py:57 filter(User.role == role) OK; schemas/user.py:49 role: str -> API emits "admin" (VERIFIED: Pydantic v2 unwraps the str-enum to its value); conversas/app/auth.py:149 is_admin_role() normalizes both forms OK; **conversas/app/seed.py:54 WRITES "admin" and BREAKS CRM reads**; **ai_tools.py:87 tells the LLM role [admin, agent] - wrong form AND a non-existent role**.
2. tasks.status -> SAEnum(TaskStatus) -> stores PENDENTE/EM_ANDAMENTO/CONCLUIDO/CANCELADO -> readers: analytics.py:56 in_(["pendente","em_andamento"]) OK (translated by the bind processor, VERIFIED); tasks.py:39,49 OK; schemas emit values; **ai_tools.py:89 documents [pending, in_progress, completed] - wrong in both language AND case**.
3. tasks.tipo -> SAEnum(TaskType) -> stores MANUAL/AUTOMATICA -> only ORM readers.
4. native_enum, values_callable, create_constraint, CheckConstraint, UniqueConstraint and __table_args__ appear ZERO TIMES in all of app/ (verified by grep) - every enum is name-persisted and unconstrained on SQLite.
5. Pseudo-enums kept as free String with no constraint and no shared constant: leads.status_venda, internal_tasks.status/task_type/recurrence/priority, chat_messages.role, operational_card_field_definitions.field_type, operational_notifications.event_type, operational_activity_logs.action, lead_history.evento, funnel_entries.etapa_id.
6. POSITIVE FINDINGS WORTH KEEPING: lead_tags/user_teams composite PKs are correct; database.py:24-28 enables SQLite FK enforcement so cascades behave in dev; passive_deletes=True is paired with ondelete CASCADE correctly throughout app/models/operational/; UserResponse leaks NEITHER hashed_password NOR api_key.
7. app/models/__init__.py and app/schemas/__init__.py are both EMPTY; model registration depends entirely on the explicit imports at app/main.py:20-32.
NOT VERIFIED: whether production Postgres actually holds 'ADMIN' today or was hand-patched; whether CONVERSAS_SEED_DEV_DATA and DATABASE_URL point at the same database in the deployed environment; whether the Postgres schema was originally created by create_all at all; whether the operational Kanban UI sends list_id in PUT /cards; the concurrency findings are inferred from SELECT-then-INSERT with no constraint, not reproduced under load.
