AGENT: A08a-frontend
RANGES: templates/pipeline.html 1-1381; leads.html 1-933; ai.html 1-930; operational/kanban.html 1-1000; partials/_lead_edit_modal.html 1-715 — all reviewed, none uncovered.

HIGH || CONFIRMED || data-loss || templates/pipeline.html || 413 || 415 || DOMContentLoaded/saveLead || Pipeline NEVER calls loadAllTags() (only leads.html:475 does), so the shared editor allTags stays []; the tag section renders "Nenhuma tag criada" and on save the checked-input query finds nothing, sending tag_ids: [] || pipeline.html:413-415 calls loadFilterTags/loadUsers/loadFunnels but not loadAllTags; _lead_edit_modal.html:706-707 collects checked inputs then PUTs /api/tags/lead/{id}; app/routers/tags.py:159 lead.tags = tags is a FULL REPLACE || Editing ANY lead from the Pipeline drawer SILENTLY DELETES EVERY TAG on that lead; tags drive the pipeline filters and the Conversas integration || Call and await loadAllTags() before openLeadModal, and skip the tags PUT when allTags.length === 0
HIGH || CONFIRMED || xss || templates/leads.html || 627 || 635 || getDestinoTags || destinos entries are interpolated into HTML with NO escaping; app/schemas/lead.py:23,36-49 accepts any free-form string list, and leads are created by the WhatsApp/n8n automation || return `<span class="destino-tag ${cls}">${d}</span>` at 633 while the sibling helpers renderLeadTags/renderFunis DO use esc() || Stored XSS with no CSP: a lead whose destino is an img/onerror payload executes for every operator opening /leads, and the JWT is readable from localStorage || ${esc(d)}
HIGH || CONFIRMED || xss || templates/pipeline.html || 995 || 995 || preencherDrawer || Destinations are joined straight into an innerHTML string while every neighbouring field on the same line uses esc() || info += ... ${(lead.destinos || []).join(', ') || '—'} then innerHTML = info at 1033 || Same stored-XSS vector as leads.html but fires when opening the lead drawer in the Pipeline || esc() the joined value
HIGH || CONFIRMED || xss || templates/partials/_lead_edit_modal.html || 494 || 515 || renderDatasDestinos || Per-destination arrival/departure values from the API are interpolated into a DOUBLE-QUOTED HTML ATTRIBUTE with no escaping; datas_destinos is Optional[dict] free-form JSON writable by the n8n/WhatsApp automation || value="${saved.chegada || ''}" at 503 and value="${saved.partida || ''}" at 509, fed from lead.datas_destinos at 262 || Attribute-breakout XSS in the SHARED editor, reachable from both /leads and /pipeline; no CSP || value="${esc(saved.chegada || '')}" (esc already escapes quotes)
HIGH || CONFIRMED || xss || templates/operational/kanban.html || 509 || 519 || fetchUsers || User names are concatenated into option markup with no escaping, unlike every other render in this file which uses the local esc() at 427 || select.innerHTML += `<option value="${u.id}">${u.nome}</option>` || Any user name containing HTML executes in the card-detail panel; the same page keeps the raw JWT in a variable at 411 || ${esc(u.nome)}
HIGH || CONFIRMED || xss || templates/operational/kanban.html || 914 || 946 || fetchCustomFields || Board field definitions AND stored field values are interpolated unescaped into element text AND into double-quoted value= attributes || value="${currentVal}" (930 date, 938 text), option value/text at 935, def.name at 942 - currentVal comes from valObj.value_text, free text saved by any user via saveCustomField || Attribute-breakout stored XSS persisted PER CARD; every operator opening that card executes it || esc() on def.name, the option value and currentVal
HIGH || CONFIRMED || xss || templates/operational/kanban.html || 844 || 868 || fetchAssignees || Assignee display name is injected unescaped || <span>👤 ${name}</span> where name = u ? u.nome : ... at 855 || Stored XSS via user name in the card detail panel || ${esc(name)}
HIGH || CONFIRMED || auth || templates/operational/kanban.html || 411 || 998 || token/init/all fetch calls || The page NEVER calls Auth.requireAuth(), snapshots the localStorage JWT ONCE at load, uses raw fetch everywhere, and handles nothing but response.ok - no 401 branch, no else, no error UI || let token = Auth.getToken() at 411 then if (response.ok) with no else in fetchBoardInfo (483), fetchLists (493), fetchCards (503), fetchUsers (513), drop (630), archive (992); app/auth.py:180-184 raises 401 for a bad Authorization header WITHOUT falling back to the cookie || A stale/missing localStorage token (cookie still valid) leaves the board permanently stuck on "Carregando quadro..." with ZERO feedback; a session expiring mid-page turns every action into a silent no-op || Route all calls through Auth.apiRequest and add Auth.requireAuth(); read the token per request
HIGH || CONFIRMED || contract || templates/operational/kanban.html || 509 || 520 || fetchUsers || GET /api/users returns UserListResponse {total, skip, limit, users:[...]} and is gated by require_admin, but the code treats the body as a BARE ARRAY || usersList = await response.json() then usersList.forEach - TypeError for an admin, and for a non-admin the 403 leaves usersList = [] || The assignee dropdown is ALWAYS EMPTY and fetchAssignees always falls back to "User #id"; for admins the TypeError also aborts init() before fetchFieldDefinitions(), so custom fields never render || Use /api/users/for-select and read data.users
HIGH || CONFIRMED || request-race || templates/pipeline.html || 618 || 642 || loadStage/applyFilters || loadStage DROPS any call that arrives while a request is in flight, and the global search box calls applyFilters() on EVERY KEYSTROKE with no debounce || if (!st || st.loading) return; at 620 with oninput="applyFilters()" at 182 and applyFilters looping loadStage for every column; the reqId guard at 622/632 is DEAD CODE because concurrent calls never start || Typing "maria" leaves the board filtered by "m"; the operator sees wrong leads with no indication the query was ignored || Debounce the global search like the per-column one (671-679) and queue/abort instead of dropping
HIGH || CONFIRMED || destructive || templates/leads.html || 804 || 808 || deleteLead || The confirmation says "Desativar" but DELETE /api/leads/{id} is a documented HARD DELETE that also wipes funnel entries, lead history and tasks; the 403 returned to non-admins is swallowed silently || confirm(`Desativar o lead "${nome}"?`) then if (resp && resp.ok) reloadLeads(); vs app/routers/leads.py:519-543 docstring "Exclui um lead permanentemente do banco de dados (Hard delete)" + require_admin || Operators believe they are archiving; the lead, its pipeline position, history and tasks are destroyed IRREVERSIBLY. Non-admins click delete and nothing happens, with no error || Reword the confirm to state permanent deletion (or switch to PUT is_active:false) and surface the response
HIGH || HIGH || availability || templates/ai.html || 7 || 10 || extra_head CDN scripts || marked and DOMPurify are loaded from jsdelivr with NO integrity/crossorigin (SRI) and no local fallback; the first statement in the page bootstrap dereferences marked || the two script tags then marked.setOptions(...) at 540 inside DOMContentLoaded || If either CDN is blocked (corporate proxy, offline, outage) the handler throws at 540 and the ENTIRE PAGE IS DEAD - no sessions list, no send button, no error message || Add integrity/crossorigin or self-host under /static; guard the bootstrap with a typeof check
MEDIUM || CONFIRMED || xss || templates/partials/_lead_edit_modal.html || 592 || 599 || renderTagCheckboxes || Tag colour is interpolated unescaped into a style attribute AND into a JS string inside an onclick, while the tag name right below it IS escaped || style="${checked ? 'background:'+t.cor+';' : ''}" onclick="toggleTagCheck(this, '${t.cor}')" - cor is Field(None, max_length=7) with NO hex-format validation || A 7-char colour such as ',x()// closes the string and executes attacker-chosen JS in the editor; the length cap is the only thing limiting the payload || Validate cor server-side against ^#[0-9a-fA-F]{6}$ and esc() both spots
MEDIUM || CONFIRMED || broken-feature || templates/partials/_lead_edit_modal.html || 121 || 121 || leadWhatsapp input || The shared partial calls formatWhatsappInput(this) but that function is defined ONLY in templates/leads.html:821 - pipeline.html does not define it || the oninput attribute; grep shows the function exists only at leads.html:821 || Editing a lead phone from the Pipeline drawer throws ReferenceError on EVERY KEYSTROKE and the input mask never applies || Move formatWhatsappInput into the shared partial
MEDIUM || CONFIRMED || error-handling || templates/partials/_lead_edit_modal.html || 702 || 713 || saveLead || The tags PUT that follows the lead save is fired WITHOUT checking the result, and onLeadSaved runs regardless || the await with the return value discarded; tags.py:154-157 returns 404 when any tag id no longer exists || Tag changes can be LOST while the modal closes reporting success; a deleted tag id makes the whole tag update 404 silently || Check resp && resp.ok and surface showLeadAlert
MEDIUM || CONFIRMED || error-handling || templates/pipeline.html || 908 || 939 || openDetailPanel || The lead GET result is parsed WITHOUT checking resp.ok, so an error body is fed to the drawer renderer || if (!leadResp) return; then const lead = await leadResp.json(); then preencherDrawer(lead) || A 404/500 opens a drawer titled undefined with every field an em dash, and "Editar Lead" then opens the editor bound to that garbage object, whose save would POST a NEW LEAD || Add an ok check
MEDIUM || CONFIRMED || broken-feature || templates/pipeline.html || 200 || 203 || pipeFilterDays/stageParams || The "Chegada em X dias" filter is READ by hasActiveFilters() but is NEVER added to the request params || the input at 202 vs stageParams at 552-566 which reads only search/destino/responsavel/viajantes/dateFrom/dateTo || The operator types a day window, the UI reports "filtro ativo", and the board is UNCHANGED - a silently wrong filter || Map it to chegada_de/chegada_ate or remove the control
MEDIUM || CONFIRMED || error-handling || templates/pipeline.html || 876 || 896 || onDrop || A failed pipeline card move produces NO FEEDBACK at all - the card silently stays in its origin column || if (resp && resp.ok) { ... } with no else, and the response body is never read || Operators believe a drag failed to register and retry, or assume it worked; 4xx/5xx are invisible || Add an else that reads detail and shows it, mirroring executeTransfer (1130-1133)
MEDIUM || CONFIRMED || error-handling || templates/operational/kanban.html || 617 || 634 || drop || Same class in the operational board, plus an EMPTY CATCH swallowing network errors || if (response.ok) { fetchCards(); } } catch (err) {} - no else, no revert, no message; operational_flow.py:38-45 returns 200 with a COMPLETELY DIFFERENT BODY for a no-op move which the UI cannot distinguish because it never reads the body || Failed or no-op moves are indistinguishable from success || Read the body, treat status === no_change explicitly, show detail on non-ok
MEDIUM || CONFIRMED || data-loss || templates/operational/kanban.html || 949 || 965 || saveCustomField || The POST result is NEVER inspected - the promise is awaited and discarded || await fetch(...) with no ok check and no try/catch || Custom-field edits (onblur/onchange) are LOST SILENTLY on 4xx/5xx or network failure; the input keeps showing the typed value so the user believes it saved || Check response.ok, restore the previous value, show an error
MEDIUM || CONFIRMED || double-submit || templates/operational/kanban.html || 637 || 683 || createCardForm/createListForm/addCommentForm || None of the three submit handlers disable the submit button or guard re-entry while the POST is in flight || the three onsubmit handlers at 637, 662 and 823 || Double-clicking creates duplicate cards/columns/comments; there is no idempotency key server-side || Disable the button and re-enable in a finally, as _lead_edit_modal.html:307-311 already does with setLeadSaving
MEDIUM || CONFIRMED || state || templates/operational/kanban.html || 754 || 762 || toggleChecklistItem || The checkbox is toggled NATIVELY by the browser before the request; on failure NOTHING reverts it || <input type="checkbox" ${i.is_checked ? 'checked' : ''} onclick=...> at 746 and if (response.ok) { fetchChecklists(); } with no else || A failed toggle leaves the UI showing a task as DONE while the server still has it pending - until the next full reload || Revert input.checked in an else/catch and surface the error
MEDIUM || CONFIRMED || auth/contract || templates/pipeline.html || 419 || 442 || loadUsers || GET /api/users is ADMIN-ONLY but this page calls it for a plain dropdown; the same mistake is at leads.html:485-496 || if (!resp || !resp.ok) return; - a 403 exits silently || For every NON-ADMIN operator the Responsavel filter and the drawer selector contain only "Agente IA", making reassignment impossible, with no error shown || Switch both pages to /api/users/for-select as three other pages already do
MEDIUM || CONFIRMED || error-handling || templates/pipeline.html || 713 || 719 || loadFilterTags/searchLeadsForAdd/showFunnelList/editFunnel || Four handlers parse the JSON body without checking resp.ok, then dereference a field that only exists on success || data.tags at 716-717, data.leads.length at 1162, data.funnels.length at 1217, showFunnelForm(funnel) at 1321-1322 || On any 4xx/5xx these throw TypeError on an error body, leaving the dropdown/modal stuck on "Carregando..." with an unhandled rejection || Add ok guards with a visible message
MEDIUM || CONFIRMED || error-handling || templates/ai.html || 855 || 884 || sendMessage || Auth.apiRequest returns NULL on 401 but the code dereferences it immediately; and the user typed text is cleared BEFORE the request so it cannot be recovered on failure || const response = await Auth.apiRequest(...); removeTypingIndicator(); if (!response.ok) - TypeError on null lands in the generic catch rendering a bogus connection error; chatInput.value = '' at 831 || Session expiry mid-conversation shows a bogus connection error instead of the login redirect, and ANY failure destroys the message the user just typed || Guard if (!response) return; and restore chatInput.value in the error paths
MEDIUM || HIGH || contract || templates/ai.html || 910 || 924 || formatSessionDate || Session timestamps are serialised with Python str(datetime) (app/routers/ai.py:95), producing a SPACE-SEPARATED non-ISO-8601 string that new Date() parses inconsistently || "updated_at": str(s.updated_at) then new Date(dateStr); the same raw str() is used for message created_at at ai.py:133 || Session list shows NaN/Invalid Date on non-Chromium browsers, or an offset-shifted relative time || Return .isoformat() from the router
MEDIUM || CONFIRMED || error-handling || templates/ai.html || 544 || 553 || loadSessions || A failed sessions request returns silently, leaving the sidebar in whatever state it was || if (!resp || !resp.ok) return; with no error branch || On a backend error the user sees an empty or stale conversation list indistinguishable from "no conversations yet" || Render an explicit error state with retry
MEDIUM || CONFIRMED || error-handling || templates/leads.html || 887 || 931 || importFile || The import BYPASSES Auth.apiRequest, sending the localStorage JWT on a raw fetch with no 401 handling, and injects the server detail into innerHTML || the raw fetch at 899-910 then resultsDiv.innerHTML = `Erro: ${data.detail || ...}` - note detalhes_erros on the SUCCESS path IS escaped at 917 || Session expiry during an import shows "Erro: undefined" instead of redirecting; a FastAPI 422 makes detail an array rendered as [object Object]; the field is the only unescaped sink on this path || Use Auth.apiRequest and textContent/esc() for detail
MEDIUM || HIGH || race || templates/leads.html || 475 || 481 || DOMContentLoaded deep-link || loadAllTags() is fired WITHOUT await and the ?open=<id> deep link immediately opens the shared editor; if the tags request has not resolved, renderTagCheckboxes renders zero checkboxes and a subsequent save WIPES the lead tags || the un-awaited call then editLead(parseInt(openId)); renderTagCheckboxes reads allTags synchronously || Intermittent SILENT TAG LOSS on the Conversas deep link - the hardest-to-reproduce class of data loss || await loadAllTags() before opening any modal, and skip the tags PUT when allTags is empty
MEDIUM || CONFIRMED || ux || templates/pipeline.html || 446 || 482 || loadFunnels/updateLeadStatus || Error and success reporting uses blocking alert(), including one that leaks raw server internals || alert("Erro do Backend em Funnels: " + (data.detail || JSON.stringify(data))) at 461, alert("Erro catastrofico...") at 479, alert("Status atualizado") at 962 || Raw exception text and JSON dumps are shown to end users, and every status change blocks the UI with a modal || Route through static/js/notifications.js and log details to console
LOW || CONFIRMED || xss || templates/pipeline.html || 941 || 953 || renderHistory || The history event type is interpolated into a class attribute without escaping (the description beside it IS escaped) || <div class="history-item ${h.evento}"> || Server-controlled today, but a future free-form event value becomes an attribute-injection sink || esc(h.evento)
LOW || CONFIRMED || xss || templates/operational/kanban.html || 968 || 982 || fetchHistory || Activity-log action strings are appended to innerHTML unescaped, with += inside a loop || container.innerHTML += `...<strong>${l.action}</strong>...` || Injection sink if action ever carries user text; the innerHTML += reparse also destroys any listeners in the container || esc(l.action) and one assignment
LOW || CONFIRMED || auth || templates/operational/kanban.html || 419 || 424 || isAdmin || Admin-only UI is gated on a role field read from localStorage, which the user controls || const _user = Auth.getUser(); if (_user && _user.role === "admin") { isAdmin = true; ... } || Any user can reveal the "Nova Coluna" control by editing crm_user in localStorage; harmless only if the create endpoints are server-checked || Keep the client check for convenience only and confirm the endpoints enforce require_admin
LOW || CONFIRMED || error-handling || templates/operational/kanban.html || 686 || 704 || openCardDetails || If the card GET fails the modal simply never opens, and activeCardId is already mutated || activeCardId = cardId then if (response.ok) { ... show } with no else || Clicking a card does NOTHING at all on error; rapid clicks on two cards race so the modal can open showing card A title with card B checklists/comments || Show an error and pass the card id through the sub-fetches instead of the shared activeCardId
LOW || CONFIRMED || correctness || templates/ai.html || 698 || 744 || download click handler || The filename captured from AI-generated markdown is concatenated into the URL without encodeURIComponent || the regex character class permits / and . so ../ sequences and unencoded specials pass through || Filenames with spaces/&/% break the download; ../ segments are normalised by the browser into a request for an unrelated path || encodeURIComponent and reject any match containing / or ..
LOW || CONFIRMED || correctness || templates/pipeline.html || 899 || 904 || atualizarTotais || After every drag-and-drop the header counts refresh by RE-RENDERING EVERY STAGE, which re-applies the deep-link target class and re-triggers scrollIntoView || boardMeta.stages.forEach(s => renderStage(s.id)) while renderStage ends with scrollIntoView - the exact side effect atualizarCardDoLead (1052-1053) was written to avoid || With a highlighted lead present, each card move yanks the board scroll position back to the highlighted card || Update only the count spans, or extract the count update out of renderStage

AGENT NOTES:
- The esc() helper is duplicated VERBATIM in 8 templates (leads.html:750, pipeline.html:1373, kanban.html:427, plus tags/tarefas/segmentacao/equipes/boards). It DOES escape quotes, so it is safe for attribute contexts - the failures above are all places where it simply WAS NOT APPLIED.
- kanban.html is the outlier: the ONLY assigned file that bypasses Auth.apiRequest entirely, and consequently the only one with no 401 handling anywhere.
- The shared _lead_edit_modal.html partial depends on THREE host-page globals (esc, formatWhatsappInput, allTags via loadAllTags). TWO of the three are missing on pipeline.html - one causes a broken input mask, the other SILENTLY DESTROYS TAGS. A self-contained partial removes both.
- /api/users (admin-only) is used by leads.html, pipeline.html and kanban.html while /api/users/for-select exists precisely for this and is used by three other pages. Fixing that one call site repairs the responsible-user selector for every non-admin operator.
- Both boards silently ignore failed moves; neither does optimistic DOM updates, so the failure mode is "nothing happens" rather than "wrong state", but the operator gets no signal either way.
NOT VERIFIED: nothing executed; whether POST /api/operational/lists and /field-definitions enforce require_admin; exact browser parsing of str(datetime) on Firefox/Safari; whether DB tags.cor can exceed the 7-char pydantic cap via another write path.
