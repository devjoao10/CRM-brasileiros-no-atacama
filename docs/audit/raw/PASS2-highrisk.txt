AGENT: PASS2-highrisk (independent second pass; had NOT seen the other agents' findings)
RANGES: app/auth.py 1-250; app/services/internal_ai_auth.py 1-88; app/query_filters.py 1-64; conversas/app/routers/webhook.py 1-700; conversas/app/services/whatsapp.py 1-406; conversas/app/services/outbound.py 1-210; app/services/ai_tools.py 1-629; migrations/m008 1-117; m009 1-99; m010 1-97 — all reviewed, none uncovered.

CRITICAL || CONFIRMED || ssrf/auth || app/services/ai_tools.py || 348 || 384 || call_internal_api || Path sanitization only blocks startswith("http") and ".."; a path beginning with @ turns the userinfo into the HOST, so urllib connects to an attacker host AND sends the valid internal-AI HMAC headers to it || the guard then url = f"http://127.0.0.1:8000{path}"; VERIFIED urlsplit("http://127.0.0.1:8000@evil.com/x").hostname == "evil.com" (independently re-verified by the orchestrator) || A prompt-injected LLM (injection arrives via lead notes/custom fields it reads with run_select_query) exfiltrates a live X-Internal-AI-Signature plus arbitrary API responses to an external host; the signature is replayable for 300s || Require path.startswith("/") and not "//", reject @ \ and control chars, build the URL with urljoin against a fixed base and assert the hostname is 127.0.0.1
CRITICAL || CONFIRMED || auth/fail-open || app/auth.py || 156 || 170 || get_current_user || The internal-AI HMAC path is accepted on ANY request regardless of source address; the design doc asserts the channel is loopback-only but NOTHING enforces it, and the signature does not bind the body || internal_ai_auth.py:23-24 claims loopback-only vs auth.py:159-170 which never inspects request.client.host; grep for client.host in app/ returns nothing || Anyone who obtains ONE signature (see the SSRF finding, or any log/proxy recording request headers) can replay it from the public internet through Traefik within the skew window, with an ARBITRARY body, acting as that user || Reject the internal-AI headers unless request.client.host is loopback (and configure Traefik not to forward them); bind a SHA-256 of the body into build_signing_message
HIGH || CONFIRMED || path-traversal || app/services/ai_tools.py || 486 || 487 || generate_excel_document || filename is an LLM-controlled parameter used in os.path.join with no sanitization beyond replacing spaces; .. and ABSOLUTE paths are not rejected || safe_filename = f"{filename.replace(' ','_')}_{hex}.xlsx" then os.path.join; os.path.join(UPLOAD_DIR, "/etc/cron.d/x") == "/etc/cron.d/x" || Arbitrary-location file write (content is a valid zip, extension forced to .xlsx) - can overwrite files inside the container image, e.g. static assets served to browsers || Reject / \ .. and drive letters; keep only re.sub(r"[^A-Za-z0-9_-]","",filename)[:64] and assert commonpath stays inside UPLOAD_DIR
HIGH || CONFIRMED || path-traversal || app/services/ai_tools.py || 589 || 591 || generate_pdf_document || Same unsanitized filename into os.path.join then pdf.output || safe_filename then filepath then pdf.output(filepath) || Arbitrary-location file write with attacker-controlled PDF content || One shared sanitizer used by both generators
HIGH || CONFIRMED || concurrency/data-loss || conversas/app/routers/webhook.py || 145 || 149 || receive_webhook || Blanket except Exception around the whole entry/change/message loop, followed by an UNCONDITIONAL 200. Any exception aborts every remaining message in the payload and tells Meta the delivery SUCCEEDED, so Meta never retries || the try at 130 wraps all three nested loops; except Exception logs then the comment "Always return 200 to Meta" then return ok (independently re-verified by the orchestrator) || A single failure (IntegrityError from a concurrent duplicate delivery, a DB blip, a NoneType in one message shape) silently drops all other REAL CUSTOMER MESSAGES in the same batch, permanently || Move the try/except inside the innermost for msg loop, and return 500 for infrastructure failures so Meta retries, reserving 200 for payloads actually consumed
HIGH || CONFIRMED || logic/silent-failure || conversas/app/routers/webhook.py || 526 || 544 || _debounce_then_forward || "Pending" inbound is computed as messages newer than the LAST OUTBOUND, but _send_auto_reply_if_needed runs AND COMMITS an outbound at line 447 BEFORE the debounce is scheduled at 451 - so the auto-reply is always newer than the customer message and the batch comes back EMPTY || last_outbound query at 528-531 then query.filter(Message.created_at > last_outbound.created_at) at 538; webhook.py:447 precedes 450-451; _save_outbound_message -> record_outbound_message with commit=True default. ORCHESTRATOR RE-VERIFIED THIS BY READING 435-460 AND 500-560 || Whenever a greeting / out-of-hours / waiting auto-reply actually fires, THE BIA NEVER SEES THAT CUSTOMER MESSAGE AND NEVER ANSWERS. The greeting fires on EVERY NEW CONVERSATION, so the first contact of every new lead is affected; because the waiting reply is deduped for 1h the failure looks intermittent and random in production || Track processing explicitly (a forwarded_at column or a status on inbound Message rows) instead of inferring it from outbound timestamps
HIGH || CONFIRMED || concurrency || conversas/app/routers/webhook.py || 365 || 384 || _process_incoming_message || Find-or-create on Conversation.whatsapp with no row lock and no DB uniqueness - conversations.whatsapp is index=True only, never unique, and no migration adds a unique index || SELECT at 367-369 then INSERT at 371-383; models/conversation.py:45; grep whatsapp in migrations/*.py returns nothing || Two first-messages from a new number arriving concurrently (or the same number across two workers) create TWO Conversation rows; messages, unread_count, the 24h window and the queue then split across duplicates with no code path that merges them || UNIQUE on conversations.whatsapp plus INSERT ON CONFLICT DO NOTHING / catch-IntegrityError-and-reselect
HIGH || CONFIRMED || idempotency || conversas/app/services/outbound.py || 96 || 130 || record_outbound_message || There is NO uniqueness guarantee of any kind on outbound sends - not in the DB, not in Python. Every call unconditionally INSERTs, and the WhatsApp send happens BEFORE persistence, so a send Meta accepted whose HTTP response timed out is recorded as failed and offered to the retry endpoint || outbound.py:96-108 builds a fresh Message with status sent/failed and no dedupe key; whatsapp.py:136-138 turns a read timeout into _error_result; conversations.py:1097 retry resends the same row || A customer receives the SAME WhatsApp message twice whenever Meta accepts a send but the response is lost and anyone presses retry; the only DB-enforced uniqueness (messages.whatsapp_msg_id) cannot help because the failed row has wamid NULL || Generate a dedupe key with a UNIQUE index, insert the row as pending BEFORE calling Meta, and reconcile against Meta rather than blind-retrying
HIGH || HIGH || authz/scope || app/services/ai_tools.py || 100 || 152 || run_select_query || The only scoping on LLM-authored SQL is a keyword regex; there is no table allowlist and no row-level filter, so SELECT * FROM users returns every bcrypt hash and every api_key hash to the model || the startswith check plus the _dangerous regex are the entire guard; get_database_schema() at line 96 ACTIVELY ADVERTISES the users table to the model || A hostile prompt, or injected text stored in a lead custom field which the same tool reads, exfiltrates password hashes, API-key hashes and all customer PII through the chat transcript || Allowlist queryable tables/columns and reject anything referencing users, or expose parameterized report queries instead of free-form SQL
HIGH || HIGH || authz/audit-bypass || app/services/ai_tools.py || 162 || 248 || update_lead_status/create_task/add_tag_to_lead || These tools write directly through SessionLocal, bypassing the routers whose validation/ownership/audit the module OWN COMMENT says all writes must use; lead_id is unvalidated and status_venda accepts any string || ai_tools.py:154-156 states run_sql_write_query was REMOVED for security and that the AI must use call_internal_api for all writes through the official routes - yet update_lead_status does lead.status_venda = status_venda; db.commit() with no auth context and no whitelist || Any lead can be moved to any arbitrary status by the LLM with no audit trail and no permission check regardless of who the calling user is; update_lead_status also has no except clause so an error escapes as a raw traceback into the tool loop || Route these through call_internal_api, or apply get_ai_user_context() ownership filters and a status enum whitelist inside them
MEDIUM || CONFIRMED || auth || app/auth.py || 64 || 75 || _get_user_from_jwt || The JWT is validated for signature and exp ONLY - no type, aud or iss claim is checked, while tokens minted for non-session purposes already exist and the same SECRET_KEY is shared with Conversas || auth.py:45 decode then payload.get("sub"); users.py:124 mints a verify_email token; conversas/app/config.py:17 shares the key || Any token signed with the shared key carrying a sub that matches an active CRM user email is a FULL CRM SESSION - including the email-verification token and any token Conversas issues || Add typ:"access" in create_access_token and require it; give each service a distinct aud and verify it
MEDIUM || CONFIRMED || concurrency || conversas/app/routers/webhook.py || 385 || 401 || _process_incoming_message || Read-modify-write on Conversation state with no with_for_update(): unread_count incremented in Python, and status/atendente_id/is_bot_active/queued_at reset from a stale in-session snapshot || unread_count = (unread_count or 0) + 1 and the reopen branch resetting atendente_id/is_bot_active/queued_at || Two distinct messages arriving concurrently lose one unread increment; worse, a reopen racing with a human claim can NULL OUT atendente_id for a conversation an operator just took, or reactivate the bot on a human-handled conversation || with_for_update() on the mutate path and an atomic UPDATE ... SET unread_count = unread_count + 1
MEDIUM || CONFIRMED || concurrency/partial-failure || conversas/app/routers/webhook.py || 660 || 683 || _forward_to_agent || Every part is sent to Meta FIRST and only persisted with commit=False, with a single commit after the loop and an await asyncio.sleep(1.2) holding the transaction open between sends || send_text_message / sleep(1.2) / record_outbound_message(commit=False) / db.commit() at 683 || If the process dies or the commit fails after messages were delivered, the customer HAS the replies but the DB has no outbound row - the next debounce re-selects the same inbound as pending and calls the Bia again, delivering duplicate replies and duplicate tool actions || Commit each part immediately after its send, or persist pending rows before the sends
MEDIUM || CONFIRMED || reliability || conversas/app/routers/webhook.py || 68 || 70 || _debounce_tasks || The 15-second debounce lives in a process-local dict of asyncio tasks with no persistence; a deploy, crash or worker restart during the window discards it silently || the dict plus create_task at 493 and sleep at 503 || A restart in the 15s window means the customer wrote and receives NOTHING - not even AGENT_FALLBACK_REPLY, which only exists inside _forward_to_agent and never runs; the contract asserted at line 627 ("Nao existe caminho em que o cliente escreve e nao recebe nada") is FALSE || Persist pending-forward state (a forward_due_at column) with a periodic sweeper
MEDIUM || CONFIRMED || timezone/window || conversas/app/routers/webhook.py || 293 || 399 || _process_incoming_message || Meta own timestamp field is extracted at 293 and then NEVER used; the 24h service-window clock is set from local server receive time instead || timestamp = msg.get("timestamp","") never read again vs last_customer_msg_at = datetime.now(tz.utc) at 380 and 399 || On a Meta retry or after a queue backlog the window is extended past the customer real message time; the system then believes free-form text is allowed when Meta considers the window closed, and the send fails in the operator face || Use datetime.fromtimestamp(int(msg["timestamp"]), tz.utc) when present, with a monotonic max() so retries never move the clock backwards
MEDIUM || CONFIRMED || logic || conversas/app/routers/webhook.py || 174 || 175 || _is_within_business_hours || Business hours compared as HH:MM STRINGS, which cannot express a window crossing midnight and silently breaks on non-zero-padded values || open_time <= current_time <= close_time with strftime("%H:%M") || A shift configured 22:00-02:00 evaluates False for all 24 hours so every message gets the out-of-hours auto-reply forever; a "9:00" entry compares as greater than "10:00" || Store minutes-since-midnight integers and handle open > close by OR-ing the two intervals
MEDIUM || CONFIRMED || concurrency || conversas/app/routers/webhook.py || 454 || 466 || _process_status_update || Blind last-writer-wins assignment of message.status with no ordering check || message.status = new_status; db.commit() with no comparison and no lock || A read can be overwritten back to delivered, and a terminal failed can be overwritten by a late sent, HIDING a delivery failure from the operator and from any retry logic keyed on status || Status precedence (sent < delivered < read, failed terminal), advance only, with_for_update or a conditional UPDATE
MEDIUM || HIGH || fail-open/config || app/services/ai_tools.py || 34 || 40 || _read_only_engine || The read-only Postgres engine falls back to the FULL READ-WRITE application URL when DATABASE_READONLY_URL is unset, leaving the keyword regex as the only barrier || app/config.py:39 defaults to DATABASE_URL; ai_tools.py:35 create_engine; the app URL is the crm_user owner per compose:53-54 || Any deployment that forgets the variable gives LLM-authored SQL the OWNER role; the regex misses Postgres-specific write vectors reachable from a SELECT || Make DATABASE_READONLY_URL required (fail at startup outside development)
MEDIUM || CONFIRMED || migration || migrations/m009_conversas_service_templates.py || 78 || 83 || run || The unique-constraint verification only appends a string to the returned action list; the script still exits 0 and prints OK when the constraint is missing, and nothing repairs it || actions.append(present/AUSENTE) followed by print OK (idempotente) at 99 || The exact drift the check exists to detect produces a successful-looking migration; duplicate (name, language) authorizations become possible and the fail-closed-by-construction claim in models/template.py:20-22 no longer holds || Raise on absence, or emit the ALTER TABLE ADD CONSTRAINT
MEDIUM || CONFIRMED || migration || migrations/m010_conversas_template_param_maps.py || 77 || 82 || run || Identical fail-open verification for uq_template_param_maps_key || actions.append(present/AUSENTE); the docstring at 75-76 states the absence means two rows could map the SAME position and the send would choose one at random || The migration own stated failure mode ships silently: a template parameter resolves to an arbitrary one of two mapped variables, producing WRONG VALUES in customer-facing template messages || Same as m009
MEDIUM || CONFIRMED || migration || migrations/m008_conversas_queued_at.py || 70 || 116 || run/__main__ || All three conversas migrations resolve their target from the Conversas config, whose DATABASE_URL DEFAULTS TO A LOCAL SQLITE FILE; run without the env var exported they migrate a throwaway file and report success || engine = engine or create_engine(DATABASE_URL) with conversas/app/config.py:11-14 defaulting to sqlite:///./conversas.db, then print OK (idempotente) || An operator runs the migration on the prod host, sees green output, and production Postgres is UNTOUCHED - with no ledger table nothing later detects that m008/m009/m010 never ran until the app errors on a missing column || Refuse to run when DATABASE_URL is unset or points at SQLite unless an explicit flag is passed, and record applied migrations in a ledger
MEDIUM || CONFIRMED || dialect-divergence || app/query_filters.py || 35 || 63 || campo_personalizado_match || Normalization is split between Python (.strip().lower(), full Unicode) and SQL (lower(), ASCII-only on SQLite, locale-dependent on Postgres), so the two sides of the SAME comparison disagree on non-ASCII data || chave_norm compared against func.lower(func.trim(...)); SQLite built-in lower() does not case-fold non-ASCII || A custom field key or value with accents - routine in this Portuguese CRM - matches in production Postgres but not in dev/CI SQLite, so THE TEST SUITE CANNOT DETECT A REGRESSION HERE and dev reproduces bugs prod does not have || Fold on one side only: normalize at write time, or register a Unicode-aware lower for SQLite
LOW || CONFIRMED || dialect-divergence || app/query_filters.py || 17 || 41 || _ESPACOS || The comment claims _ESPACOS matches what str.strip() removes, but it lists only six ASCII characters while str.strip() also removes NBSP, \x1c-\x1f, \x85 and the U+2000 block || _ESPACOS = " \t\n\r\v\f" used as func.trim against a Python-side .strip() || A key pasted from a spreadsheet with a leading NBSP is trimmed on the Python side but not on the SQL side, so the filter silently matches NOTHING || Restrict the Python side to .strip(_ESPACOS) so both agree, or normalize at write time
LOW || CONFIRMED || contract-violation || conversas/app/services/whatsapp.py || 379 || 381 || send_reaction || Returns bare None when credentials are absent, while every other send_* returns {"simulated": True} for the same condition || if not token or not phone_id: return None; outbound.py:11-12 documents simulated for dev and None as defensively treated as failure || In an unconfigured environment a reaction is persisted as failed with a bogus last_error instead of simulated, polluting the failed-message queue operators retry from || Return the simulated dict to match the documented contract
LOW || CONFIRMED || auth || conversas/app/routers/webhook.py || 95 || 95 || verify_webhook || The webhook verify token is compared with == rather than hmac.compare_digest, unlike the HMAC path at line 65 which correctly uses constant-time compare || if hub_mode == subscribe and hub_verify_token == expected_token || Byte-wise timing leak on a shared secret; low practical risk over the network but gratuitously inconsistent with _verify_meta_signature two functions above || hmac.compare_digest
LOW || CONFIRMED || ops/silent-outage || conversas/app/routers/webhook.py || 117 || 119 || receive_webhook || Missing META_APP_SECRET in a non-development environment fails CLOSED with a 500 on every delivery and only a log line - correct security-wise but indistinguishable from a crash and self-inflicted by the compose default || the elif branch raising 500; docker-compose.yml:125 META_APP_SECRET with an empty default and no :? guard || A missing .env entry means every inbound customer message 500s until Meta disables the subscription; nothing surfaces this at startup || Validate at application startup outside development and refuse to boot; use ${META_APP_SECRET:?} in compose
LOW || CONFIRMED || performance || conversas/app/routers/webhook.py || 630 || 637 || _forward_to_agent || The FULL conversation history is loaded and shipped to n8n on every agent call, with no limit || recent_msgs = query(...).order_by(created_at.asc()).all() - the name says recent but there is no limit; historico includes messages whose status is failed || Unbounded payload and unbounded LLM token cost on long conversations, and the model is shown FAILED (undelivered) outbound messages as if the customer had received them || .limit(N) on a descending order then reverse, and exclude failed outbound rows

AGENT NOTES:
- AI TOOL SURFACE (10 tools, all reachable by the LLM): get_database_schema(); run_select_query(query) free-form SQL with no table allowlist; update_lead_status(lead_id, status_venda, cancel_reason) direct DB write, no ownership check, arbitrary status string; create_task(lead_id, ...) unvalidated lead_id; add_tag_to_lead(lead_id, tag_nome) creates tags freely; create_lead(...) direct write with no owner assignment; get_api_endpoints(); call_internal_api(method, path, payload_json) — the widest surface, any method against any /api/ path as the logged-in user, with the SSRF escape above and no method/path allowlist; generate_excel_document(filename, ...) and generate_pdf_document(filename, ...) — both take a path-traversable filename.
- EXACT IDEMPOTENCY GUARANTEE, INBOUND: messages.whatsapp_msg_id UNIQUE (models/conversation.py:107), enforced BY THE DATABASE. The Python SELECT at webhook.py:359-363 is only an optimization; under a genuine race the losing transaction blocks on the unique index and aborts with IntegrityError BEFORE it can send an auto-reply, so a customer message is never double-processed. Correctly, the check runs BEFORE any Conversation mutation.
- EXACT IDEMPOTENCY GUARANTEE, OUTBOUND: THERE IS NONE — neither in the database nor in Python. record_outbound_message unconditionally INSERTs; the only unique column is NULL precisely in the failure case (timeout after Meta accepted) that the retry endpoint then re-sends.
- FAIL-OPEN/FAIL-CLOSED SUMMARY: HMAC verification is fail-CLOSED (500 when the secret is missing outside development, verified against compose which hardcodes ENVIRONMENT=production). Message PROCESSING is fail-OPEN in the data-loss direction — the blanket except at line 145 returns 200 and suppresses Meta retry.
- TIMEZONE: service_window_open (models/conversation.py:13-36) is the single CORRECT implementation and normalizes naive->UTC for the SQLite/Postgres split. The gap is upstream — the window clock is set from server receive time, not Meta timestamp, which is parsed and discarded.
NOT VERIFIED: runtime behaviour of the SQLite read-only URL rewrite at ai_tools.py:31 when DATABASE_URL carries query parameters; whether Traefik strips inbound X-Internal-AI-* headers (the CRITICAL replay finding assumes it does not); whether SQLAlchemy SQLite reflection reliably returns the named composite UNIQUE for the m009/m010 check; /api/ai/download/{filename} was not in scope (the traversal findings cover the WRITE side only).
