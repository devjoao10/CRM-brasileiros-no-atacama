AGENT: A13-db  (two CRITICALs independently re-verified by the orchestrator by reading init-hardening.sh + docker-compose mounts)
RANGES: migrations/README.md 1-39; migrations/__init__.py 1-2; m001 1-93; m002 1-40; m003 1-75; m004 1-55; m005 1-53; m006 1-43; m007 1-45; m008 1-117; m009 1-99; m010 1-97; docker/postgres/init.sql 1-66; init-hardening.sh 1-17; pg_hba.conf 1-26; postgresql.conf 1-32 — all reviewed, none uncovered.

CRITICAL || CONFIRMED || config/bootstrap || docker/postgres/init-hardening.sh || 11 || 15 || psql -f 01-init.sql || init.sql is BOTH mounted as /docker-entrypoint-initdb.d/01-init.sql AND re-executed by 02-hardening.sh, so the postgres entrypoint runs it TWICE; CREATE USER has no IF NOT EXISTS so the second run always fails || docker-compose.yml:17-18 mounts 01-init.sql and 02-hardening.sh; the official entrypoint runs *.sql via psql -v ON_ERROR_STOP=1 in sorted order so 01-init.sql executes FIRST WITHOUT -v CRM_READONLY_PASSWORD (init.sql:11 CREATE USER crm_readonly WITH PASSWORD :'CRM_READONLY_PASSWORD'), then init-hardening.sh:15 runs the identical file again || Container initialization ABORTS on a fresh volume: either pass 1 dies on the unresolved :'VAR' or pass 2 dies on role already exists. The whole hardening layer (readonly role, grants, timeouts, connection limits) is therefore UNPROVEN in production and cannot be reproduced in disaster recovery || Stop mounting init.sql into initdb.d; mount it elsewhere and let 02-hardening.sh be the only executor; make the SQL re-runnable with a DO block checking pg_roles
CRITICAL || CONFIRMED || security/config || docker/postgres/init-hardening.sh || 14 || 14 || CRM_READONLY_PASSWORD || Password is DOUBLE-quoted: the shell wraps the value in literal single quotes AND psql :'VAR' quotes it again, so the real crm_readonly password contains literal apostrophes and never matches DATABASE_READONLY_URL || init-hardening.sh:14 -v CRM_READONLY_PASSWORD="'${CRM_READONLY_PASSWORD:-crm_readonly_secret}'" sets the psql variable to 'secret' INCLUDING quotes; init.sql:11 :'VAR' emits '''secret''' i.e. the SQL literal 'secret' WITH apostrophes; docker-compose.yml:56 builds the URL from the RAW value || The AI read-only connection fails authentication permanently; ai_tools.py:34-39 errors on every SQL tool call, and app/config.py:39 means a missing var silently falls back to the SUPERUSER owner URL instead || Drop the shell-side quotes and keep :'VAR' in the SQL, which quotes exactly once
HIGH || CONFIRMED || security || docker/postgres/init-hardening.sh || 14 || 14 || CRM_READONLY_PASSWORD default || Hardcoded fallback password for a role with SELECT on every table || ${CRM_READONLY_PASSWORD:-crm_readonly_secret} - the :- default fires when unset OR empty in any path outside compose (which alone enforces :?) || Anyone reaching 5432 from the Docker/RFC1918 range with that guessable password reads all leads, users and conversations || Remove the default and fail loudly with :?
HIGH || HIGH || security || docker/postgres/init.sql || 28 || 34 || ALTER USER crm_user CONNECTION LIMIT 30 || The whole hardening block is applied to crm_user, which the postgres image creates as a SUPERUSER - superusers are exempt from CONNECTION LIMIT and bypass every schema-level REVOKE; init.sql never demotes it || init.sql:28,32-34; docker-compose.yml:10 POSTGRES_USER defaults to crm_user and the image always creates POSTGRES_USER with SUPERUSER CREATEDB CREATEROLE; no ALTER USER crm_user NOSUPERUSER anywhere in the repo || Both FastAPI services run every query as a database SUPERUSER: any SQL injection reaches COPY ... TO PROGRAM (host command execution), can DROP any table, and the 30-connection cap the pool sizing assumes is NOT enforced || After create_all bootstraps the schema, ALTER ROLE crm_user NOSUPERUSER and grant only CONNECT/USAGE/CRUD+CREATE; keep a separate superuser for maintenance
HIGH || HIGH || security/privileges || docker/postgres/init.sql || 16 || 21 || ALTER DEFAULT PRIVILEGES FOR USER crm_user || The readonly role access to FUTURE tables hinges entirely on tables being created by exactly crm_user, and the retroactive GRANT at line 21 runs against a still-EMPTY database and grants nothing || init.sql:17-18 and :21 both execute during initdb, BEFORE either app has called create_all (app/main.py:64, conversas/app/main.py:26) || Any table created by a different role - a pg_restore run as postgres, a manual migration by an admin, a future second app user - is invisible to crm_readonly with no error at creation time; the AI SQL tool starts returning permission denied only when someone finally queries it, i.e. AFTER the restore || ALTER DEFAULT PRIVILEGES for every role that can create tables, and re-run the retroactive GRANT as a post-deploy/post-restore step
HIGH || HIGH || security/privacy || docker/postgres/init.sql || 51 || 52 || ALTER USER ... SET log_statement || Statement logging is enabled for DML on the app role and for EVERYTHING on the readonly role; because psycopg2 interpolates bind parameters client-side, the logged SQL text contains the literal VALUES || init.sql:51 log_statement='mod' for crm_user and :52 'all' for crm_readonly; postgresql.conf:13-18 sets log_connections/log_disconnections but no logging_collector/log_destination, so everything lands on stderr captured by Docker json-file with NO rotation configured || Every lead INSERT/UPDATE writes nome, email and whatsapp in cleartext into container logs, and INSERT INTO users writes hashed_password and the SHA-256 api_key - a durable secondary copy of PII and credential material outside the encrypted database, on an unbounded log file || Drop to log_statement='ddl' for crm_user, or route audit logging through pgaudit/logging_collector with rotation and restricted permissions
HIGH || HIGH || correctness/migration || migrations/m001_schema_drift_leads_tasks.py || 56 || 83 || run() || All DDL runs inside ONE engine.begin() block while EVERY statement exception is swallowed; on PostgreSQL a single failure aborts the transaction, every later statement fails silently, and the implicit commit degrades to ROLLBACK - yet the script prints SUCCESS || :56 with engine.begin() wraps 57-81; :69-70 and :79-80 discard the error; on Postgres any error puts the session in InFailedSqlTransaction so the ADD COLUMNs already issued at :61/:73 are rolled back at block exit while :91-93 still print OK (idempotente) || An operator running the production migration after a verified backup gets a green OK for a migration that applied NOTHING; the app then writes dias_por_destino/resultado_ia to columns that do not exist || Use a SAVEPOINT per guarded statement (conn.begin_nested()) or a separate connection, and re-raise anything that is not the specific expected SQLite error
HIGH || CONFIRMED || schema-drift || conversas/app/auth.py || 17 || 30 || class User (__tablename__ = users) || Conversas declares its OWN users table in its own metadata and calls create_all() on the SHARED database; its definition is missing email_verified and disagrees on types/constraints. NO migration reconciles users at all || conversas/app/auth.py:19-20 with extend_existing; conversas/app/main.py:10 then :26 create_all; the CRM version differs on nome length, role type, is_active nullability, api_key uniqueness and email_verified presence; compose starts both services in parallel once postgres is healthy || On any fresh volume (DR restore, new environment) whichever service wins the race DEFINES users. If Conversas wins, users.email_verified never exists; create_all never ALTERs, no migration adds it, and app/seed.py:33 and app/routers/users.py:117,207,226 immediately fail with UndefinedColumn - the CRM cannot create or verify a single user || Give Conversas a mapping excluded from its metadata create, and add a migration that guarantees users.email_verified exists
MEDIUM || CONFIRMED || process/migration || migrations/README.md || 16 || 25 || Scripts table || There is NO migration ledger table and the README documents only m001 - m002 through m010 are absent, so which migrations already ran in production lives only in human memory || README.md:17-19 has exactly one row while migrations/ contains m001-m010; README documents `python -m migrations.mNNN` while m003-m010 docstrings prescribe `python migrations/mNNN.py` (they mutate sys.path so app.* means conversas/app); nothing anywhere writes an applied-migration record || Ordering is unenforced, running two conversas migrations in one process silently binds app.* to the wrong service, and after staff turnover nobody can tell whether m005 was applied || Ship a schema_migrations(version, applied_at) ledger written by a shared run() wrapper, or adopt Alembic; until then keep the README complete and one command form
MEDIUM || CONFIRMED || data-mutation || migrations/m008_conversas_queued_at.py || 99 || 107 || UPDATE conversations SET is_bot_active || The ONLY production data-mutating statement in the whole migration set rewrites business state with no record of the previous values, and it is not semantically idempotent || :100-106 UPDATE conversations SET is_bot_active = off WHERE atendente_id IS NOT NULL AND is_bot_active = on; the docstring says it fixed 5 rows in production. atendente_id is NOT cleared when a human finishes, so any conversation keeping a non-null atendente_id while an operator deliberately re-enabled the BIA is silently switched off again on every subsequent run || Re-running the migration - which README.md:14 explicitly encourages as safe - disables the bot on conversations where a human intentionally re-enabled it; no side table captures the pre-change state so it is irreversible without a full restore || Split the one-off data repair out of the schema migration, capture affected ids first, and gate on a run-once marker
MEDIUM || CONFIRMED || correctness/migration || migrations/m009_conversas_service_templates.py || 76 || 83 || uq_service_templates_name_language check || When the table PRE-EXISTS without the UNIQUE constraint the migration only appends a string to the action list and still reports success - the constraint the code relies on is never created || :78-83 appends either present or ":AUSENTE (verificar manualmente)" then returns normally and prints OK (idempotente); identical in m010:77-82 for uq_template_param_maps_key; the docstrings claim the opposite ("A regra fica no BANCO, nao numa verificacao em Python") || A pre-existing table without the constraint lets two rows map the same (name, language, position); per m010:76 the send path then chooses one at random, i.e. a WhatsApp template goes out with the WRONG customer value, and the migration meant to guarantee this exits 0 || Raise (non-zero exit) when the constraint is absent, or actually ADD CONSTRAINT guarded by a catalog lookup
MEDIUM || HIGH || correctness/migration || migrations/m005_conversas_tags.py || 32 || 45 || run() || Table-creating migrations run each create() as its OWN auto-committed transaction, so a failure between them leaves the schema half-migrated with no rollback and no marker || :43 table.create(bind=engine, checkfirst=True) called once per table inside the loop at 36-44 - two independent DDL transactions; same single-statement pattern in m002:31, m004:46, m006:34, m007:36 and m009:73/m010:72. None is wrapped in an explicit transaction || If the second table fails (statement_timeout, lock wait, dropped connection) the database keeps the first and the operator gets a traceback with no indication of which half applied || Wrap multi-object migrations in one engine.begin() and pass the connection to create(bind=conn) - PostgreSQL DDL is transactional
MEDIUM || HIGH || config || docker/postgres/init.sql || 17 || 51 || crm_user hardcoded || init.sql hardcodes the role name crm_user in six statements while docker-compose parameterizes it as ${POSTGRES_USER:-crm_user} || init.sql:17, 28, 34, 45, 51; docker-compose.yml:10 and .env.example:18 invite overriding it || Setting POSTGRES_USER to anything else makes every one of those statements fail with role does not exist; with ON_ERROR_STOP=1 the container never finishes initializing and the readonly role loses its default privileges || Use :'POSTGRES_USER' passed from the shell, or drop the override from compose and .env.example
MEDIUM || HIGH || capacity || docker/postgres/postgresql.conf || 6 || 7 || max_connections || Configured pool ceilings EXCEED the server non-superuser connection budget, and the AI read-only pool exceeds crm_readonly own CONNECTION LIMIT || postgresql.conf:6-7 max_connections=50 minus 3 reserved = 47; app pools 30 + conversas 15 + AI 8 = 53; init.sql:29 sets crm_readonly CONNECTION LIMIT 5, below the AI engine maximum of 8 || Under load the last connections are refused with FATAL too many connections; the AI SQL tool hits the role limit at 6 concurrent queries, well before any timeout protects anything || Raise the limits to cover the pools (readonly >= 8) or shrink max_overflow so the sum stays under 47; better, pgbouncer
MEDIUM || HIGH || operations || docker/postgres/init.sql || 44 || 45 || ALTER USER crm_user SET statement_timeout = '30s' || Manual migrations run as crm_user and therefore inherit a 30-second per-statement timeout that can abort DDL mid-migration || :45 sets the role-level default; migrations connect with create_engine(DATABASE_URL) which compose points at crm_user. statement_timeout covers lock waits too, and no lock_timeout is set anywhere || A CREATE INDEX or ALTER TABLE queued behind a live transaction, or simply slower than 30s on a grown table, is killed by the server; combined with the non-atomic create() pattern this yields a partially applied production migration || Have migrations SET statement_timeout = 0 and lock_timeout = '5s' on their connection before any DDL
MEDIUM || CONFIRMED || performance/locking || migrations/m001_schema_drift_leads_tasks.py || 40 || 46 || INDEXES || Indexes are created NON-concurrently inside an open transaction, taking a SHARE lock that blocks all writes to leads and tasks for the duration || :41-45 executed at :78 inside the engine.begin() opened at :56; same in m008:90-97. CREATE INDEX CONCURRENTLY cannot run inside a transaction block, so the current structure forecloses it || Every INSERT/UPDATE/DELETE on leads and tasks blocks while the index builds - tolerable today but a live outage once leads grows, and invisible in staging || Move index creation out of the transaction and use CREATE INDEX CONCURRENTLY IF NOT EXISTS with autocommit, checking pg_index.indisvalid afterwards
MEDIUM || CONFIRMED || config || docker/postgres/pg_hba.conf || 9 || 12 || host crm_atacama crm_app || Three HBA rules authorize a database role named crm_app that does NOT exist - that is the CRM Docker CONTAINER name, not a role || pg_hba.conf:10-12 with the comment "App user"; docker-compose.yml:36 container_name: crm_app while :54 connects as crm_user, separately covered by pg_hba.conf:20-22 || Dead rules that misdescribe the access model; the file reads as if the app had a dedicated least-privilege role when in reality it connects as the superuser owner - exactly the misunderstanding that lets the superuser problem persist || Delete the crm_app rules, or create a real least-privilege role and switch DATABASE_URL to it
MEDIUM || HIGH || schema-drift || migrations/m003_conversas_message_error_fields.py || 41 || 41 || send_attempts || The migration gives send_attempts a SERVER default while the model declares only a Python-side default, so a migrated database and a create_all database end up with DIFFERENT DDL || m003:41 INTEGER NOT NULL DEFAULT 0 vs conversas/app/models/conversation.py:114 Column(Integer, default=0, nullable=False) with no server_default; same divergence in m001:36 vs app/models/lead.py:26 || On a fresh create_all production database messages.send_attempts is NOT NULL with NO default: any INSERT not going through the ORM (manual repair, n8n/psql insert, COPY) fails, while the same statement succeeds on the migrated database. Schema comparisons between environments can never be clean || Add server_default=text("0") to the models so create_all and the migrations emit identical DDL
MEDIUM || HIGH || correctness/migration || migrations/m002_internal_tasks.py || 27 || 32 || run() || Table-creating migrations crash on a database whose FK targets do not exist yet, unlike m003/m008 which handle the absent-table case explicitly || m002:31 creates internal_tasks with FKs to users.id; m004:46 creates media_assets with a FK to messages.id; m005:43 and m006:34 reference conversations.id. None checks the parent exists, while m003:50-52 and m008:73-75 DO return a clean table-absent action || Running the documented order on an empty or partially built database aborts with a raw relation does not exist, mid-chain, with no ledger recording how far it got || Mirror the m003/m008 guard
LOW || HIGH || security || docker/postgres/pg_hba.conf || 10 || 22 || address ranges || HBA authorizes the ENTIRE RFC1918 space rather than the actual Docker network subnet || :10-12, 15-17 and 20-22 each allow 172.16/12, 192.168/16 and 10/8; the reject at :25-26 only catches what falls outside; compose defines no custom network so the real bridge subnet is a single /16 inside 172.16/12 || Any host on the operator LAN or VPN that can route to the container port is authenticated by password alone; only the absence of a published port prevents it today, which one ports: line would undo || Pin the rules to the compose network subnet
LOW || HIGH || security || docker/postgres/postgresql.conf || 20 || 22 || ssl = off || Database traffic between containers is unencrypted, including the scram exchange and every row of lead PII || :21 listen_addresses='*' with :22 ssl=off and a comment attributing protection to Traefik - which terminates TLS in front of the HTTP services and never sees 5432 traffic || Anyone able to sniff the Docker bridge or attach to the network namespace (a compromised sidecar, n8n running arbitrary workflow code on the same host) reads all queries and results in cleartext || Enable ssl with a self-signed cert and sslmode=require, or state the accepted risk honestly
LOW || MEDIUM || config || docker/postgres/postgresql.conf || 1 || 32 || config_file replacement || The custom config file REPLACES PGDATA/postgresql.conf wholesale without include-ing it, silently discarding every setting initdb wrote || docker-compose.yml:24 -c config_file=...; the file has no include_if_exists and defines only 15 settings, so initdb-generated values (timezone, log_timezone, lc_messages, dynamic_shared_memory_type, default_text_search_config) revert to compiled-in defaults || Server timezone silently becomes GMT rather than the detected value, which matters for the log_line_prefix '%t' timestamps used to correlate incidents || Add include_if_exists as the first line, or set the dropped values explicitly starting with timezone
LOW || HIGH || security || docker/postgres/init.sql || 13 || 38 || crm_readonly privileges || The readonly role is SELECT-only by GRANT CONVENTION alone - nothing enforces read-only at the session level, and PUBLIC keeps its default CONNECT/TEMPORARY || :13 GRANT CONNECT is redundant (PUBLIC already has it) and no REVOKE CONNECT, TEMPORARY ON DATABASE ... FROM PUBLIC is ever issued; :38 revokes sequences but there is no ALTER ROLE crm_readonly SET default_transaction_read_only = on || A single accidental future GRANT silently converts the AI connection into a writable one with no second barrier; any new role can connect and create temp objects || ALTER ROLE crm_readonly SET default_transaction_read_only = on and REVOKE CONNECT, TEMPORARY FROM PUBLIC
LOW || CONFIRMED || operations || docker/postgres/init.sql || 1 || 4 || init-only execution || Everything in init.sql runs exactly ONCE, on an empty volume - later edits to the hardening never reach the running production database || :3 says it runs on the FIRST boot; docker-compose.yml:15 pgdata is a persistent named volume and the entrypoint skips initdb.d entirely when PGDATA is populated || Every fix to the problems above (password quoting, NOSUPERUSER, default privileges) is a NO-OP on the live database; repo and production grants can diverge indefinitely with nothing detecting it || Extract grants/timeouts into a re-runnable harden.sql executed as an explicit deploy step

AGENT NOTES — PER-TABLE PK/FK/UNIQUE/INDEX SITUATION AND WHO CREATES IT:
leads: PK id; FK responsavel_id->users.id ON DELETE SET NULL; indexes nome, email, whatsapp, status_venda, responsavel_id, created_at (created_at only in prod via m001:41). NO UNIQUE on email/whatsapp - duplicate leads are representable.
tasks: PK id; FKs user_id->users.id and lead_id->leads.id BOTH WITHOUT ON DELETE (NO ACTION) - deleting a user/lead with tasks raises instead of cascading; indexes via m001:42-45. user_id made nullable by m001:67 (Postgres only; SQLite silently skips).
users: PK id; UNIQUE email, UNIQUE api_key; role is a Postgres ENUM (userrole). Created ONLY by create_all - by WHICHEVER SERVICE STARTS FIRST; email_verified exists only in the CRM definition. NO migration touches this table.
internal_tasks: PK id; FKs ON DELETE SET NULL; indexes due_date, status, is_archived, created_at. m002 or create_all.
conversations: PK id; NO FKs AT ALL - lead_id, atendente_id, responsavel_id are bare integers pointing at other-service tables, so orphans are structurally possible and unpoliced; indexes lead_id, whatsapp, status, atendente_id, responsavel_id, queued_at. whatsapp is indexed but NOT UNIQUE.
messages: PK id; FK conversation_id ON DELETE CASCADE; UNIQUE whatsapp_msg_id (present since the initial commit, so prod HAS it) - this UNIQUE is the ONLY real guard behind the webhook read-then-insert idempotency check. NO index on (conversation_id, created_at) despite that being the hot ordering.
media_assets: PK id; FK message_id ON DELETE CASCADE, UNIQUE; index meta_media_id. m004/create_all.
conversation_tags: PK id; UNIQUE nome. conversation_tag_links: composite PK, both FKs ON DELETE CASCADE. m005/create_all.
conversation_notes: PK id; FK conversation_id ON DELETE CASCADE; user_id NOT NULL but NO FK to users. m006/create_all.
message_variables: PK id; UNIQUE+index token; created_by a bare integer. m007/create_all.
service_templates: UNIQUE (name, language). template_param_maps: UNIQUE (name, language, position); token free text with no FK (deliberate). m009/m010/create_all.
message_templates: UNIQUE name, UNIQUE meta_template_id. CREATE_ALL ONLY - no migration.
api_config, auto_replies (UNIQUE trigger), business_hours (UNIQUE weekday), quick_replies (UNIQUE shortcut): CREATE_ALL ONLY.
CRM tags/lead_tags, funnels/funnel_entries/lead_history, segments, teams/user_teams, chat_sessions/chat_messages: CREATE_ALL ONLY.
All 15 operational_* tables: CREATE_ALL ONLY. They DO have proper FKs with ON DELETE CASCADE/SET NULL and indexes, but any future column added to them can never reach an existing database.
MIGRATION COVERAGE AUDIT (git -S on every model column): every column added AFTER its table initial creation is covered by some migration - leads/tasks by m001, messages by m003, conversations.queued_at + atendente_id index by m008 - EXCEPT users.email_verified, which today prod got from create_all and which remains uncovered for any DB created by Conversas first.
NOT VERIFIED: whether the live pgdata volume actually contains crm_readonly and the grants (init.sql cannot have completed cleanly as written; the DB may predate these files or have been fixed by hand) - requires DB access, out of scope; exact psql behaviour for an unset :'VAR' on pass 1 (the double-execution failure holds under either interpretation because pass 2 always hits role already exists); production row counts; whether any environment ever set POSTGRES_USER to something else.
