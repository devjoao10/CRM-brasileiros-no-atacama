AGENT: ARCH-crossfile (seams between files; no single-file coverage claimed)

CRITICAL || CONFIRMED || schema-divergence || conversas/app/auth.py || 17 || 30 || User || Conversas RE-DECLARES the shared users table without email_verified, with role as String(20) instead of the CRM SQLAlchemy Enum, nome as String(200) instead of String(100), and api_key without unique/index; BOTH services run create_all() at startup with NO ORDERING, so whichever boots first on a fresh DB permanently defines the table and create_all never repairs the loser || app/models/user.py:21-24 vs conversas/app/auth.py:22-30; app/main.py:64 and conversas/app/main.py:26; docker-compose.yml:48-50 and 109-111 make both depend only on postgres, never on each other; migrations/ contains NO users migration (grep email_verified matches only app/) || If Conversas wins the race the users table has NO email_verified column, so every CRM db.query(User) emits SELECT users.email_verified and raises UndefinedColumn - login, /api/auth/me, /api/users and every joinedload(Lead.responsavel) return 500 until someone ALTERs by hand || Delete the mirror model and import/reflect the CRM definition, or make the CRM the sole schema owner and add users columns to migrations/
CRITICAL || CONFIRMED || shared-table-write || conversas/app/seed.py || 50 || 58 || seed_dev_user || Conversas WRITES rows into the shared CRM users table with role="admin" (lowercase) and no email_verified, and hashes the password with SHA-256 while the CRM verifies with bcrypt || SQLAlchemy persists the member NAME so the Postgres enum labels are ADMIN/USER (conversas/app/auth.py:121-149 documents exactly this asymmetry in the READ direction); email_verified is NOT NULL with no server default; conversas/app/seed.py:30 sha256 vs app/auth.py:20-26 bcrypt || On a CRM-created schema the INSERT fails outright. On a Conversas-created schema the row persists as role='admin' and the CRM Enum result-processor raises LookupError on ANY query returning that user, so ONE SEEDED ROW breaks /api/users and lead listings; and that user can never log in to the CRM because bcrypt cannot verify a sha256 digest || Stop writing users from Conversas; create shared dev users through the CRM POST /api/users
HIGH || CONFIRMED || data-contract || conversas/app/services/crm.py || 248 || 259 || auto_create_lead_in_crm || The raw INSERT into lead_history OMITS the dados column, so it lands as SQL NULL, but the CRM response schema declares it a NON-OPTIONAL dict || app/schemas/pipeline.py:129 dados: dict = {} (not Optional) and app/routers/pipeline.py:765 model_validate; the CRM own writer always supplies it (pipeline.py:41) and Conversas OTHER history INSERT does too (crm.py:161) - only this one does not || GET /api/pipeline/history/{lead_id} raises a Pydantic ValidationError and returns 500 for EVERY LEAD AUTO-CREATED FROM WHATSAPP, i.e. exactly the leads the team most needs the timeline for || Add dados to the INSERT with '{}' as crm.py:161 does, or make the field Optional
HIGH || CONFIRMED || session-source-of-truth || conversas/app/routers/pages.py || 9 || 13 || _require_cookie || Conversas HTML page guard checks only that the cookie is PRESENT, never that it DECODES - the identical defect the CRM already fixed and documented as AUTH-LOOP-01 || app/auth.py:210-229 replaced presence-checking with require_page_session -> _get_user_from_jwt precisely so /hub and /api/auth/me cannot disagree (see the comment block and commit 04cd501); conversas pages.py:19,33,41 still call _require_cookie while conversas/app/auth.py:104-111 FULLY VALIDATES the same cookie for /api routes || An expired or tampered cookie makes Conversas serve the app shell, whose JS immediately gets 401 and redirects to /login, where login.html:165-173 sees the stale localStorage token and bounces back - THE SAME /login LOOP THE CRM FIXED, still live on conversas.* || Port require_page_session + page_login_redirect into conversas
HIGH || CONFIRMED || session-xss || conversas/templates/login.html || 213 || 213 || login submit handler || Conversas stores the SHARED JWT in a JavaScript-written, non-httpOnly cookie PLUS localStorage, while the CRM issues the same token as an httpOnly cookie - and both services validate it with the SAME SECRET_KEY || app/routers/auth.py:51-59 sets httponly/secure; conversas login.html:213 writes document.cookie with neither; conversas/static/js/auth.js:15 also puts it in localStorage; compose:57 and :115 pass the same key || Any XSS or malicious dependency on conversas.* reads a token that authorizes the ENTIRE CRM API - the CRM httpOnly protection is NULLIFIED by the sibling host || Set the cookie server-side in the conversas login route and stop writing it from JS
HIGH || CONFIRMED || logout || conversas/static/js/auth.js || 62 || 65 || logout || Conversas logout clears only localStorage; it NEVER deletes the access_token cookie it created and there is NO server-side logout endpoint in the service || login.html:213 creates the cookie with max-age=28800; auth.js:62-65 calls only clearAuth() - contrast app/routers/auth.py:102-106 delete_cookie which static/js/auth.js:86-94 DOES call; conversas pages.py:11 then still admits anyone holding that cookie || After "logout" the browser keeps a VALID 8-HOUR CREDENTIAL; on a shared machine the next user reaches the inbox, and the token still authorizes the CRM too || Add POST /api/auth/logout to conversas that deletes the cookie, and call it before redirecting
HIGH || CONFIRMED || lost-write || conversas/app/routers/conversations.py || 686 || 692 || update_conversation || responsavel_id is COMMITTED LOCALLY and only then pushed to the CRM in a SECOND INDEPENDENT TRANSACTION whose boolean result is DISCARDED - while every subsequent read treats the CRM as the source of truth and OVERWRITES the local value || db.commit() at 686 precedes sync_responsavel_to_crm at 692 whose failure path (crm.py:172-175) logs, rolls back and returns False with NOBODY CHECKING; _repair_responsavel_cache (262-281) re-writes from the CRM on every list (537) and every open (636); the same two-phase pattern is repeated at 763-767 || An assignment made in Conversas while the CRM row is locked or the DB hiccups APPEARS TO SUCCEED, then SILENTLY REVERTS on the next 5-second list refresh - the lead stays owned by the wrong person and no error is ever surfaced || One transaction (both services share the DB), or surface the sync failure as 5xx
HIGH || CONFIRMED || phone-normalization || conversas/app/routers/conversations.py || 357 || 359 || initiate_conversation || FOUR INCOMPATIBLE phone-number rules coexist on the same whatsapp value: force-prefix 55, raw Meta digits with exact match, last-10 substring match, and last-11 suffix match || conversations.py:357-359 forces 55; webhook.py:367-369 stores msg["from"] verbatim and matches exactly; crm.py:31-33 uses normalized[-10:] with LIKE %suffix%; app/routers/leads.py:807-826 tries exact, then +-prefixed, then ilike last-11 || THE COMPANY OPERATES IN CHILE: initiating with +56... stores 5556..., so when that customer replies the webhook exact match MISSES and creates a SECOND conversation. Independently, the last-10 rule COLLIDES ACROSS DDDs (5511987654321 and 5521987654321 both end in 1987654321), so an inbound message can auto-link to and be answered on behalf of the WRONG LEAD || One normalization function (E.164, no country assumption) in a module both services import, used on every write and every lookup
HIGH || CONFIRMED || trust-boundary || docker-compose.yml || 91 || 96 || n8n traefik labels || n8n is PUBLISHED TO THE PUBLIC INTERNET and every workflow webhook that fronts a privileged agent has NO authentication configured || the Webhook Gerenciador, Webhook Mensagem and Webhook Notificacao nodes all have authentication unset while the tool nodes in the same workflows carry the CRM API key credential and POST/PUT to /api/leads, /api/pipeline/... and /api/tasks; conversas webhook.py:648 also calls this same unauthenticated path internally || Anyone on the internet can POST to /webhook/gerenciador-leads and drive an LLM that HOLDS A VALID CRM API KEY - creating, mutating and re-assigning leads and moving pipeline stages - and to /webhook/notificacao to send WhatsApp messages from the business number || Header auth on those webhook nodes with a shared secret, or drop the public Traefik router for n8n
HIGH || CONFIRMED || rate-limit-boundary || app/limiter.py || 18 || 18 || limiter || The rate limiter keys on the SOCKET PEER while uvicorn runs WITHOUT --proxy-headers behind Traefik, so every request in the system shares a SINGLE BUCKET || limiter.py:14,18; Dockerfile:24 with no proxy flags; compose:41-42,64-67 remove the host port so the only ingress is Traefik; auth.py:22 5/minute on login; and conversas/app/routers/auth.py:91-97 PROXIES EVERY CONVERSAS PRODUCTION LOGIN to that same CRM endpoint from one container IP || The global 200/min and the login 5/min apply to the whole internet COLLECTIVELY: one noisy client or a handful of Conversas users locks EVERYONE out, and per-attacker brute-force protection does not exist || --proxy-headers --forwarded-allow-ips on both Dockerfiles, or key on the authenticated user id
MEDIUM || CONFIRMED || duplicated-logic-diverged || conversas/app/services/crm.py || 186 || 259 || auto_create_lead_in_crm || Lead creation exists in TWO PLACES THAT HAVE ALREADY DIVERGED: the Conversas version ALSO places the lead in a funnel, writes a created history event and applies the WhatsApp tag; the CRM official POST /api/leads does NONE of it || crm.py:188-197 INSERT leads, 203-210 pick funnel, 234-241 INSERT funnel_entries, 248-259 INSERT lead_history, 262-295 tag; app/routers/leads.py:469-489 builds a Lead, commits and returns - no FunnelEntry, no LeadHistory, no tag; the n8n Tool Criar Lead posts to that CRM endpoint || Leads Bia creates THROUGH N8N NEVER APPEAR ON THE KANBAN (pipeline.py:262 renders only entries whose etapa_id is in funnel.etapas, and these leads have no entry at all) and have an empty history, while identical leads created via the WhatsApp webhook DO - sales silently loses half the inbound pipeline || One CRM service function called by both POST /api/leads and Conversas
MEDIUM || CONFIRMED || layer-violation || conversas/app/services/crm.py || 224 || 245 || auto_create_lead_in_crm || Conversas writes funnel_entries with RAW SQL, bypassing every invariant the CRM pipeline router enforces: stage-exists validation and position assignment || pipeline.py:568-571 rejects an unknown etapa_id with 400 and 581-590 sets posicao = count; crm.py:219-221 FALLS BACK TO THE LITERAL STRING "nova_oportunidade" when funnel.etapas is empty or shaped differently, and :238 HARDCODES posicao 0 || A funnel whose etapas JSON is empty or differently shaped yields an entry whose stage id matches NO COLUMN, so the card exists in the DB but is INVISIBLE on the board and unreachable except via locate_lead; the fixed posicao 0 also collides with the existing top card || Call the CRM own service for funnel placement, or at minimum validate the stage id and compute posicao
MEDIUM || CONFIRMED || ownership-race || app/routers/leads.py || 492 || 515 || update_lead || responsavel_id is mutable from FIVE code paths with THREE DIFFERENT RULE SETS, and the generic PUT applies NONE of them || leads.py:509-511 blind setattr with responsavel_id accepted by the schema - no existence check, no is_active check, no LeadHistory; the dedicated endpoint at 851-872 does all three; crm.py:143-167 writes history but validates nothing; conversations.py:683 looks the user up WITHOUT is_active; the n8n tool pins responsavel_id=5 in the URL || Two operators (or the CRM UI and the Conversas inbox) can write CONFLICTING OWNERS in the same second with only ONE of the writes producing an audit row; the generic PUT can also set responsavel_id to a DELETED user id || Route all ownership changes through update_lead_responsavel; strip responsavel_id from LeadUpdate
MEDIUM || CONFIRMED || layer-violation || app/services/ai_tools.py || 162 || 295 || the four direct-write AI tools || Four AI tools open their OWN SessionLocal and write the ORM directly, CONTRADICTING THE POLICY COMMENT SITTING THREE LINES ABOVE THEM || ai_tools.py:154-156 states run_sql_write_query was removed for security and that the AI must use call_internal_api through the official routes that apply validation and auditing - yet 171-186 mutates lead.status_venda to ANY ARBITRARY STRING with no whitelist, and 271-283 create_lead skips the funnel/history work || AI-driven status changes produce values analytics.py:140-142 does not count in ANY bucket, leaving leads INVISIBLE in dashboard totals; AI-created leads land outside the pipeline with no history || Delete the four direct-write tools and expose them through call_internal_api
MEDIUM || CONFIRMED || ai-contract || app/services/ai_tools.py || 87 || 98 || get_database_schema || The HAND-WRITTEN schema the LLM is given to build SELECTs does NOT MATCH the real models in four places || ai_tools.py:96 says users(... name ..., role [admin, agent]) but the model declares nome and UserRole = ADMIN/USER; :93 says funnels(... descricao, is_default ...) which do not exist; :94 presents etapa_id as a closed list while the model stores etapas as free-form JSON per funnel; the read connection is a separate engine so nothing reconciles them || EVERY AI analytical query touching users or funnels fails with UndefinedColumn, and ai_tools.py:144-149 hands the RAW DB ERROR back to the model which then retries - wasted Gemini turns and confidently wrong answers about roles ("agent") that do not exist || Generate the schema string from Base.metadata.tables
MEDIUM || HIGH || sec-regression || app/routers/ai.py || 24 || 24 || _ai_limiter || A SECOND Limiter instance was reintroduced - the exact defect app/limiter.py was created to eliminate || limiter.py:2-11 documents the original incident; ai.py:24 creates a local Limiter and :333 decorates with it, while main.py:84-85 registers the OTHER instance as app.state.limiter and binds the 429 handler to it - that handler injects headers from request.app.state.limiter, whose storage never recorded this route window || /api/ai/chat 20/min is accounted in a storage NO OTHER COMPONENT CAN SEE, and the 429 path reads limit state from the wrong instance; the regression re-opens the bug WP-SEC-03 closed || Import the shared instance and delete the local Limiter
MEDIUM || CONFIRMED || service-window || conversas/app/routers/webhook.py || 344 || 451 || _process_incoming_message || A reaction is DELIBERATELY declared not to open the 24h window, but the same function still fires the auto-reply AND the Bia forward, both of which send FREE-FORM TEXT without consulting the window || opens_window = msg_type != "reaction" at 344 with a long justifying comment, and 398-399 only advances the clock when opens_window - yet 447 and 450-451 run UNCONDITIONALLY, reaching send_text_message at 238 and 663; the window rule is enforced only for OPERATOR routes (conversations.py:64-78) whose comment explains the webhook path is intentionally exempt because the window was just reopened - AN ASSUMPTION THE REACTION BRANCH BREAKS || A customer reacting with an emoji to a conversation older than 24h triggers a free-text send Meta rejects with 131047; the message is persisted as failed and NOTHING ALERTS THE OPERATOR || Skip the auto-reply/agent branches when opens_window is False, or check service_window_open before the two sends
MEDIUM || CONFIRMED || unread-sources-of-truth || conversas/app/routers/webhook.py || 680 || 682 || _forward_to_agent || The BOT zeroes unread_count after answering, so the same column means "messages a human has not read" in one place and "messages nobody has replied to" in another || webhook.py:388 increments on every inbound; 680-682 sets it to 0 when the Bia reply is accepted; conversations.py:639 also zeroes it BUT ONLY WHEN A HUMAN OPENS the thread; and conversations.py:575-579 serves payload["unread"] straight from that column as the OPERATOR notification dataset || Messages Bia handled DISAPPEAR from the operator unread badge although NO HUMAN EVER SAW THEM - precisely the handoff cases that need a human eyes; the badge and the inbox disagree about what is outstanding || Do not touch unread_count in the bot path; the comment at 677-679 already establishes that this function must not mutate operational state
MEDIUM || HIGH || trust-boundary || app/auth.py || 159 || 170 || get_current_user || The internal-AI HMAC headers are honoured ON THE PUBLIC LISTENER, while the module that defines them justifies its weaker guarantees by ASSERTING the channel is loopback-only || internal_ai_auth.py:22-24 states the body is not bound because the channel is loopback-only and the timestamp bounds replay; app/auth.py:159 branches on the MERE PRESENCE of the three headers with NO check of request.client.host, and compose:64-67 routes public traffic straight to :8000; the signer IS loopback but nothing enforces that on the VERIFYING side || The stated precondition for not signing the body is FALSE: a captured signature can be replayed against a DIFFERENT PAYLOAD for up to 300s, and a leaked secret is exploitable FROM THE INTERNET rather than requiring code execution on the host || Reject the internal headers unless client.host is loopback, and/or include a body digest
MEDIUM || CONFIRMED || config-boundary || conversas/app/config.py || 17 || 17 || SECRET_KEY || Conversas SILENTLY falls back to a hardcoded development key when SECRET_KEY is missing, while the CRM REFUSES TO START - and compose passes the variable to Conversas WITHOUT the mandatory-check it uses for the CRM || conversas config.py:17 vs app/config.py:16-23; compose:57 uses :? while :115 does not; the two services MUST agree because conversas decodes tokens the CRM mints || A deployment path that supplies the key to one service and not the other produces a Conversas that BOOTS HEALTHY and rejects every CRM-issued token with a plain 401 - the shared-secret contract fails open into an unexplainable auth outage rather than a startup error || Mirror the CRM fail-fast and add :? to the compose variable
LOW || CONFIRMED || validation-divergence || conversas/app/routers/conversations.py || 683 || 684 || update_conversation || Conversas assigns a responsavel WITHOUT checking the user is active, while the CRM REQUIRES it and the read-back query FILTERS ON IT || conversations.py:683 and :760 look up by id only vs app/routers/leads.py:852 which filters is_active and 404s otherwise; crm.py:367 LEFT JOINs on u.is_active and returns nome NULL for an inactive user || A conversation assigned to a DEACTIVATED employee keeps the id but loses the name on the next read-repair, so the inbox shows a linked conversation with a BLANK OWNER instead of falling back to Agente IA || Apply the same is_active filter and reject with 404
LOW || CONFIRMED || dead-config || app/config.py || 80 || 80 || API_KEY_EXPIRY_DAYS || A configured API-key expiry policy is NEVER IMPLEMENTED; the n8n key is permanent || config.py:80 is the ONLY occurrence in the repo; _get_user_from_api_key checks only the hash and is_active; models/user.py:24 stores NO issue timestamp so expiry could not be computed even if checked; the key is what every n8n tool node presents || The single long-lived credential that lets an INTERNET-REACHABLE n8n mutate the CRM NEVER ROTATES OR EXPIRES, and the config knob gives operators FALSE CONFIDENCE that it does || Implement expiry or delete the setting
LOW || MEDIUM || durability || conversas/app/routers/webhook.py || 68 || 70 || _debounce_tasks || The customer PENDING REPLY lives only in an in-process dict and an unawaited asyncio task, AFTER the webhook has already told Meta 200 || the dict, create_task at 493, the 15s sleep at 503 and the 240s agent timeout at 42; webhook.py:149 returns ok to Meta immediately so Meta WILL NOT RETRY; nothing persists the pending state and _fetch_agent_parts deliberately never retries || A deploy, restart or crash during the up-to-four-minute window DROPS THE CUSTOMER MESSAGE with no reply and no record that a reply was owed - the failure mode webhook.py:689-693 logs for is NOT EVEN REACHED || Persist a pending-agent-reply marker and sweep it on startup, or use a durable queue
LOW || CONFIRMED || broken-endpoint || conversas/app/routers/auth.py || 113 || 156 || get_current_user_info/validate_token || Two auth endpoints are STRUCTURALLY BROKEN: /api/auth/me declares a response model it cannot satisfy, and /api/auth/me/validate CLAIMS to validate a token while taking no token and performing no check || @router.get("/me", response_model=UserResponse) returning {"detail": ...}; validate_token() with no dependencies unconditionally returning valid True || Currently unreachable from the shipped frontend, but /me/validate is a LOADED FOOTGUN: the next caller that treats it as a session gate gets an ALWAYS-TRUE answer || Delete both, or implement them properly

AGENT NOTES:
- The codebase is unusually well-commented about its own past bugs, and THAT IS WHERE THE SEAMS SHOW: three separate comment blocks (app/limiter.py:2-11, app/auth.py:210-215, conversas/app/auth.py:121-149) describe a class of defect THAT STILL EXISTS ELSEWHERE in the system. Fixes were applied AT THE SITE OF THE INCIDENT rather than at the shared boundary.
- ROOT CAUSE BEHIND ROUGHLY HALF OF THESE FINDINGS: conversas reaches into CRM-owned tables - sometimes through a mirrored ORM model (users), sometimes through raw SQL (leads, funnel_entries, lead_history, tags, lead_tags) - with NO shared schema definition and NO shared write path. Every CRM invariant (enum values, NOT NULL columns, stage validation, history logging, position assignment) is re-implemented or SKIPPED on the Conversas side.
- SECOND ROOT CAUSE: the two hosts were treated as ONE application for secrets (same SECRET_KEY, same DB superuser) but as TWO applications for sessions (host-only cookie, two logins, two logout implementations, one of which is a no-op).
- conversas/app/models/conversation.py:13-36 (service_window_open) is the ONE piece of shared-fact logic DONE RIGHT: a pure function, one definition, consumed by the model property, the route guard and serialization. It is the pattern the phone normalization, lead creation and ownership updates need.
NOT VERIFIED: neither service was run and no DB was inspected; every schema claim derives from the model declarations plus the absence of a users migration. Which service actually created users in production is UNKNOWN - both failure directions are described. Traefik configuration is not in the repo (only labels), so whether X-Forwarded-For is set is unverified; the uvicorn side is confirmed and sufficient. n8n credential contents are not in the repo.
