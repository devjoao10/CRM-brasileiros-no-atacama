AGENT: SEC-threat (independent whole-system threat review; secret VALUES never reproduced)

CRITICAL || CONFIRMED || committed-credential || docs/n8n-toolHttpRequest-guia.md || 176 || 181 || Credencial de autenticacao block || Anonymous internet / anyone with repo or git-history read access. A PRODUCTION CRM API KEY is committed in plaintext as the n8n X-API-Key credential value. app/auth.py::_get_user_from_api_key accepts it on every /api/* route and conversas/app/auth.py::_get_user_from_api_key accepts the SAME key (shared users table), so it also unlocks the WhatsApp inbox. Keys NEVER EXPIRE (API_KEY_EXPIRY_DAYS is read in config and used nowhere) and are not scoped || Header Name: X-API-Key / Header Value: <redacted: 68-char key with bna_ prefix, matching generate_api_key() at app/auth.py:54>. ORCHESTRATOR INDEPENDENTLY RE-VERIFIED: the literal is present at docs/n8n-toolHttpRequest-guia.md:180 and in git history since 7fd122b || Full authenticated API access as the key owner over the PUBLIC INTERNET on BOTH crm.* and conversas.*: read/modify every lead, mint further keys via POST /api/auth/token, and send arbitrary WhatsApp messages via POST /api/conversations/send-notification || ROTATE THE KEY NOW (DELETE /api/auth/apikey then reissue), purge it from git history, replace the doc value with a placeholder, add expiry/rotation, and add a pre-commit secret scan
CRITICAL || CONFIRMED || missing-authentication/ssrf || n8n/workflows/live_exports/20260708_1443/Gerente_Autonomo_de_Tarefas_IA.json || 236 || 246 || CRM API Tool || Anonymous internet, or a WhatsApp customer via prompt injection. The tool method AND full url are $fromAI(...) - entirely LLM-CHOSEN - while genericCredentialType/httpHeaderAuth ATTACHES THE CRM API KEY to whatever host the LLM names. The agent prompt interpolates task titulo/descricao straight from GET /api/tasks, and tasks are creatable by the lead-manager agent and by POST /api/tasks, so ATTACKER TEXT REACHES THE PLANNER || the two $fromAI expressions with genericAuthType: httpHeaderAuth || Injected task text can POST the CRM API key to an attacker host, call any CRM endpoint with full privileges including POST /api/auth/token to mint a fresh permanent key, and reach internal-only hosts (postgres, conversas, n8n) from inside the Docker network || Fixed-URL tools per operation; NEVER bind a credential to an LLM-chosen URL
CRITICAL || CONFIRMED || missing-authentication || n8n/workflows/live_exports/20260708_1443/Agente_Gerenciador_de_Leads_BnA.json || 214 || 221 || Webhook Gerenciador || Anonymous internet, NO precondition. EVERY n8n webhook node in the repo declares "options": {} - no httpHeaderAuth, no basic auth - and n8n is published by Traefik at n8n.crmbrasileirosnoatacama.cloud || the webhook node with no credentials block; compose:92 traefik router rule; same for /webhook/agent-bia (WF-01:93-100) || POST /webhook/gerenciador-leads hands ARBITRARY ATTACKER JSON to an LLM agent whose tools carry the CRM API key: anonymous full read/write of CRM lead and pipeline data, plus arbitrary WhatsApp sends and unbounded Gemini spend, with no authentication or rate limit anywhere on the path || Header Auth on every webhook node, secret in n8n credentials, and a Traefik source allow-list; treat /webhook/* as a PUBLIC API SURFACE
HIGH || CONFIRMED || json-injection || n8n/workflows/live_exports/20260708_1443/Notificacao_WhatsApp.json || 100 || 110 || Enviar WhatsApp jsonBody || Anonymous internet: POST to the unauthenticated /webhook/notificacao with a crafted nome. The Code node copies input.nome VERBATIM into nome_lead, and the HTTP node interpolates it INSIDE A JSON STRING LITERAL with no escaping, so a double quote closes the string and lets the caller APPEND JSON KEYS. Later duplicate keys win in JSON.parse || the jsonBody template with {{ $json.nome_lead }} inside a quoted string; destinatario is hardcoded earlier but CAN BE OVERRIDDEN by a later injected key || Arbitrary approved-template WhatsApp messages sent to ARBITRARY NUMBERS from the company official WABA number using the Meta token - phishing under the brand, plus Meta quality-rating/ban risk || Build the body with JSON.stringify in the Code node, never string-interpolate untrusted values into a JSON literal; also authenticate the webhook
HIGH || CONFIRMED || weak-default-secret || conversas/app/config.py || 17 || 19 || SECRET_KEY || Anonymous internet, precondition: SECRET_KEY unset or empty for conversas. The CRM REFUSES TO BOOT without it but conversas silently falls back to the committed constant, and compose:115 passes it WITHOUT the :? guard the CRM entry uses at 57, so an empty .env value yields an EMPTY HMAC KEY rather than fail-fast. Both services sign/verify the same sub=email HS256 token against the same users table || the getenv default consumed by conversas/app/auth.py:39 jwt.decode || If the fallback is ever active, anyone can FORGE A TOKEN FOR ANY EMAIL (e.g. the admin) and it is accepted by BOTH conversas and the CRM - COMPLETE AUTHENTICATION BYPASS ACROSS THE WHOLE SYSTEM || Mirror the CRM: raise at import, add :? to compose, and give each service its own key with aud/iss checked on decode
HIGH || CONFIRMED || broken-authentication || conversas/app/routers/auth.py || 153 || 156 || validate_token || Anonymous internet, NO precondition. The endpoint takes no arguments, reads no header, and returns 200 {"valid": true} to EVERY CALLER while its docstring claims it validates the token || @router.get("/me/validate") / async def validate_token(): / return {"valid": True} - no Depends, no Header || Any component that gates on this route grants UNAUTHENTICATED ACCESS; combined with _require_cookie page gating it makes "is this session valid?" answerable as YES by an anonymous caller. Nothing consumes it today, so it is a live trap for the next integration || Delete the route, or implement it with Depends(get_current_user)
HIGH || CONFIRMED || session-management || conversas/templates/login.html || 210 || 214 || login submit handler || Any XSS on conversas.*, or a network attacker on any plaintext request to that host. The conversas session cookie is WRITTEN BY CLIENT-SIDE JAVASCRIPT so it cannot be HttpOnly and is not marked Secure; the same token is also mirrored into localStorage. The CRM sets its own cookie correctly, so this is a PER-SERVICE REGRESSION || document.cookie = access_token=Bearer ... (also line 171 on the re-verify path) vs app/routers/auth.py:51-59 httponly+secure || Any script execution on the conversas origin steals a token that, BECAUSE OF THE SHARED SECRET_KEY, authenticates against the CRM as well - full account takeover of that agent, including admin || Set the cookie server-side with httponly/secure/samesite and stop mirroring the JWT into localStorage
HIGH || CONFIRMED || broken-access-control || conversas/app/routers/pages.py || 9 || 43 || _require_cookie || Anonymous internet, precondition: set ANY access_token cookie value. The gate checks only PRESENCE and never verifies the signature, exp, user existence or is_active - exactly the divergence the CRM fixed in app/auth.py:218-229. Conversas kept the broken version on /, /templates and /settings || if not request.cookies.get("access_token"): return None - no decode_token, no DB lookup || A deactivated or expired user (or an anonymous visitor) renders the inbox, settings and template-management shells; the API calls behind them still 401, but the disabled-account and expired-session boundary is NOT ENFORCED at the page layer || Port require_page_session + page_login_redirect into conversas
HIGH || CONFIRMED || privilege-escalation/credential-minting || app/routers/auth.py || 67 || 82 || generate_api_key_endpoint || Any authenticated principal INCLUDING THE AI acting through call_internal_api (whose internal HMAC identity passes get_current_user). The route requires only get_current_user - no re-authentication, no admin gate, no rate limit - and returns a PERMANENT PLAINTEXT KEY carrying the caller full privileges forever; API_KEY_EXPIRY_DAYS exists in config but is NEVER ENFORCED || the endpoint body returning ApiKeyResponse(api_key=new_key) || Prompt injection into Perpetua (via lead text, an uploaded document, or a low-privileged user simply asking) yields a NEVER-EXPIRING BEARER CREDENTIAL printed into the chat transcript; an admin session compromised for 5 seconds becomes PERMANENT admin access with no revocation trail || Require a password re-prompt or require_admin, set and enforce an expiry, and EXCLUDE /api/auth/token from the AI callable path
HIGH || CONFIRMED || sensitive-data-exposure || app/services/ai_tools.py || 100 || 128 || run_select_query || Any authenticated CRM user (LOWEST PRIVILEGE) who can chat with Perpetua, or anyone who prompt-injects it. The guard blocks only semicolons, non-SELECT prefixes and a DML keyword list - it NEVER RESTRICTS WHICH TABLES may be read, and docker/postgres/init.sql:21 grants SELECT ON ALL TABLES to crm_readonly INCLUDING users || the startswith check and the _dangerous regex; SELECT email, hashed_password, api_key FROM users PASSES EVERY CHECK || Any user can dump EVERY BCRYPT PASSWORD HASH and every API-key SHA-256 for offline cracking, plus all lead PII regardless of the UI own filters - a horizontal-to-vertical escalation path that BYPASSES require_admin entirely || Restrict crm_readonly with column/table grants or a dedicated view excluding users.hashed_password and users.api_key, and reject queries referencing users in the tool
HIGH || CONFIRMED || ssrf || app/services/ai_tools.py || 348 || 394 || call_internal_api || Any authenticated CRM user through the AI chat, or anyone who prompt-injects Perpetua via lead/notes/uploaded-file content. The anti-SSRF guard rejects only paths starting with http or containing .., but the value is CONCATENATED straight into the URL, so a LEADING @ turns the intended host into USERINFO || the guard then url = f"http://127.0.0.1:8000{path}" and urlopen with the internal HMAC headers attached; VERIFIED: urlsplit("http://127.0.0.1:8000" + "@evil.com/x").hostname == "evil.com" (also independently re-verified by the orchestrator) || Server-side requests to ARBITRARY EXTERNAL HOSTS (exfiltrating whatever the LLM places in the body, plus the signed internal headers) AND to internal-only services unreachable from the internet - @postgres:5432/, @n8n:5678/webhook/gerenciador-leads, @conversas:8001/api/... || urlsplit the value, require an empty netloc and a path starting with a single /api/, rebuild from a fixed base, reject @ backslash and //
MEDIUM || CONFIRMED || path-traversal || app/services/ai_tools.py || 486 || 488 || generate_excel_document || Any authenticated user via the AI, or prompt injection. filename is FULLY LLM-CONTROLLED and only has spaces replaced before os.path.join; os.path.join with an ABSOLUTE argument DISCARDS THE BASE DIRECTORY, and ../ segments are not stripped. Same bug in generate_pdf_document (589-591). The DOWNLOAD route sanitises correctly (ai.py:269-272), so the WRITE side is the gap || safe_filename = f"{filename.replace(' ','_')}_{hex}.xlsx" then os.path.join then wb.save || Writes a ZIP/PDF to any path the container user can reach: overwrite files in /app, fill a volume, or clobber mounted media - and the container RUNS AS ROOT (no USER in either Dockerfile) || os.path.basename plus an [A-Za-z0-9_-] whitelist, then verify the resolved path is inside the upload dir (the resolve_local_file pattern in conversas media_storage.py:72-90 is the in-repo reference)
MEDIUM || CONFIRMED || rate-limiting-bypass || app/limiter.py || 13 || 18 || limiter || Anonymous internet, NO precondition. get_remote_address reads request.client.host, but uvicorn is started WITHOUT --proxy-headers, so behind Traefik EVERY REQUEST IN THE WORLD resolves to the single Traefik container IP. The login 5/minute and the global 200/minute share ONE BUCKET for all users. Conversas has NO LIMITER AT ALL and its login PROXIES TO THE CRM, consuming the same shared bucket || the Limiter construction and the Dockerfile CMD || A single attacker sending 5 login attempts per minute LOCKS EVERY LEGITIMATE USER OUT of the CRM and of conversas login, and 200 req/min denies the whole API; per-IP brute-force accounting and per-IP audit attribution are both meaningless || --proxy-headers --forwarded-allow-ips, or a key_func reading the rightmost trusted hop; add per-account lockout
MEDIUM || CONFIRMED || missing-security-headers/supply-chain || templates/ai.html || 7 || 10 || extra_head || Anonymous attacker who compromises jsdelivr, or any DNS/TLS-interception position. NO Content-Security-Policy is emitted anywhere in the codebase, and third-party scripts are loaded from a CDN with NO integrity attribute - INCLUDING DOMPURIFY ITSELF, which is the app only stored-XSS/prompt-injection defence for AI output || the two script tags; chart.js and fullcalendar are loaded the same way in dashboard.html:7, relatorios.html:7, tarefas.html:7 || Arbitrary JavaScript execution on an authenticated ADMIN page: the sanitizer can be NEUTERED and the localStorage JWT (valid on BOTH origins) exfiltrated; no CSP means nothing constrains the injected script egress || Vendor the two libraries into /static or add SRI hashes, and emit a CSP in SecurityHeadersMiddleware mirrored into conversas
MEDIUM || CONFIRMED || missing-security-headers || conversas/app/main.py || 37 || 60 || app construction || Anonymous internet, NO precondition. The conversas service registers CORS and static files but NO SECURITY-HEADERS MIDDLEWARE AT ALL - no X-Frame-Options, no X-Content-Type-Options, no Referrer-Policy, no HSTS, no Cache-Control no-store on auth responses - while the CRM has all of them. CORS also allows both origins with allow_credentials and allow_headers ["*"] || the add_middleware call with no SecurityHeadersMiddleware anywhere in the file || The WhatsApp inbox (customer PII, full message history) is FRAMABLE for clickjacking, MIME-sniffable, cacheable by intermediaries, and downgradeable without HSTS || Duplicate the CRM SecurityHeadersMiddleware into conversas and narrow the allowed headers
MEDIUM || CONFIRMED || broken-access-control/abuse || conversas/app/routers/conversations.py || 286 || 336 || send_notification || Any authenticated principal - INCLUDING ANY HOLDER OF THE API KEY COMMITTED IN docs/ - with no admin gate, no conversation binding, no destination allow-list and NO RATE LIMIT (conversas has no limiter at all). The number and the full message body come STRAIGHT FROM THE REQUEST || data: NotificationCreate then send_text_message(data.to, data.message, db) with no validation of data.to || Arbitrary WhatsApp text to ARBITRARY NUMBERS from the company official business number: phishing under the brand, mass spam, and Meta account suspension || require_admin or a dedicated integration role, bind sends to an existing conversation or an allow-list, and add per-principal rate limiting
MEDIUM || CONFIRMED || secret-exposure-to-workflow-code || docker-compose.yml || 84 || 88 || n8n service environment || Anyone who reaches the n8n editor (public host, single owner account, no SSO/MFA) or who can get a workflow to evaluate an expression. Environment-variable access is EXPLICITLY UNRESTRICTED while the long-lived Meta Cloud API token sits in that same environment || N8N_RESTRICT_ENVIRONMENT_VARIABLES_ACCESS=false alongside META_ACCESS_TOKEN || Any Code node or expression ($env.META_ACCESS_TOKEN) reads the WhatsApp Business token, which allows sending as the business number and reading media for 30 days - OUTSIDE ANY CRM AUDIT TRAIL || Set the restriction true, move the token into n8n credentials, remove it from the container environment
MEDIUM || HIGH || denial-of-service || conversas/app/routers/webhook.py || 629 || 650 || _forward_to_agent || A REAL WHATSAPP CUSTOMER, no privileges needed. Every agent invocation loads the ENTIRE message history (.all(), unbounded) into memory and POSTs it to n8n with a 240-second read timeout, one in-flight request per conversation; receive_webhook swallows every exception and always returns 200 so NOTHING BACK-PRESSURES META || the unbounded query then the historico list posted with AGENT_TIMEOUT || A customer with a long history (or several coordinating numbers) pins worker memory and holds 4-minute outbound connections, degrading the inbox for all agents and inflating Gemini token spend without bound || Cap the history slice, cap concurrent agent calls with a semaphore, lower the read timeout with an explicit degradation path
MEDIUM || HIGH || insecure-deployment || .github/workflows/deploy.yml || 49 || 61 || Deploy via SSH || An attacker who obtains VPS_PASSWORD (a workflow-write compromise, a malicious action, or a repo admin) gets an INTERACTIVE SHELL on the production host. Password authentication also implies PasswordAuthentication yes on the VPS, exposing SSH to internet-wide credential stuffing || password: secrets.VPS_PASSWORD with the git pull + docker compose script || FULL HOST COMPROMISE (all containers, the Postgres volume, the .env with every secret) from ONE LEAKED REPOSITORY SECRET || SSH key restricted by command= in authorized_keys, disable password auth on the VPS, deploy an immutable image tag rather than git pull on the box
MEDIUM || CONFIRMED || container-hardening || Dockerfile || 18 || 24 || image build || Anyone achieving code or file-write execution inside a container (see the AI file-write and SSRF findings). NEITHER Dockerfile declares a USER, so uvicorn and every AI-invoked tool RUN AS ROOT. Both also COPY . . after a .dockerignore that excludes .env but the CRM image STILL SHIPS tests/, scripts/ and docs/ - INCLUDING THE FILE HOLDING THE COMMITTED API KEY || COPY . . and CMD with no USER directive || Any in-container write primitive becomes a ROOT write; the leaked-key document is REDISTRIBUTED INTO THE PRODUCTION IMAGE; container escape surface is maximal || Non-root USER, read_only + cap_drop ALL in compose, and tighten .dockerignore to exclude docs/, tests/, scripts/, n8n/
LOW || CONFIRMED || sensitive-data-exposure || conversas/app/routers/api_config.py || 35 || 42 || get_api_config || Any authenticated conversas user (non-admin). The response schema deliberately masks meta_access_token into a boolean but RETURNS meta_verify_token - the webhook verification secret - IN CLEARTEXT, and the read route uses get_current_user while EVERY MUTATING ROUTE on the same object uses require_admin || the response builder setting meta_verify_token from the model || A low-privileged user learns the value needed to complete Meta webhook GET handshake, useful for re-pointing the webhook subscription || Mask it as has_verify_token and require_admin on the read route
LOW || CONFIRMED || hardcoded-fallback-credential || docker/postgres/init-hardening.sh || 11 || 15 || psql init invocation || An operator who runs the init script outside docker-compose (compose itself enforces the variable with :?). The shell default SILENTLY CREATES the read-only database role with a KNOWN, COMMITTED PASSWORD || the :- default in the -v argument || The crm_readonly role - which holds SELECT on all tables including users.hashed_password - would be created with a PUBLICLY KNOWN password; pg_hba.conf limits this to the Docker networks, which is the ONLY thing preventing direct exploitation || Drop the :- default and exit 1 when unset
LOW || CONFIRMED || weak-signature-scope || app/services/internal_ai_auth.py || 41 || 52 || build_signing_message || An attacker who obtains ONE internal signature (for example through the call_internal_api SSRF above, which FORWARDS THE SIGNED HEADERS to an attacker-chosen host). The signed message covers only user_id, timestamp, method and the bare path - the BODY and the QUERY STRING are excluded - and there is NO NONCE || the f-string message; the constant-time compare at 86 IS correct and the empty-secret case DOES fail closed at 70 || Within the 300s window a captured signature for POST /api/leads can be replayed with ATTACKER-CHOSEN JSON; the unsigned query string also means ?responsavel_id= style parameters are unauthenticated || Include a SHA-256 of the body and the raw query string, and add a single-use nonce cache
LOW || CONFIRMED || session-management || app/auth.py || 34 || 48 || create_access_token/decode_token || Any attacker holding a stolen token, for up to 8 hours. Tokens carry NO jti, NO aud, NO iss, and there is NO DENYLIST - logout only deletes the browser cookie so the token itself STAYS VALID; deactivating a user works only because _get_user_from_jwt re-checks is_active per request. Because the two services share the key and the claim set, a token minted by EITHER is accepted by the OTHER with no audience separation || the encode/decode calls || A leaked token CANNOT BE REVOKED before expiry and "log out everywhere" is impossible; a token intended for conversas grants CRM access and vice versa || jti + a revocation table, or shorter expiry with refresh tokens; distinct aud per service
LOW || HIGH || insecure-default-configuration || .env.example || 42 || 44 || CONVERSAS_SEED_DEV_DATA || An operator who copies .env.example to .env and adds an env_file/passthrough for conversas (compose:112-128 does NOT currently pass this variable, WHICH IS WHAT SAVES THE DEPLOYMENT TODAY). With the flag on, conversas seeds an account with role="admin" into the SHARED users table using an UNSALTED SHA-256 password, and switches /api/auth/login from the CRM proxy to LOCAL SHA-256 verification || CONVERSAS_SEED_DEV_DATA=true with CONVERSAS_DEV_PASSWORD, consumed by seed.py:53 and routers/auth.py:79-88 || A PRODUCTION ADMIN ACCOUNT protected by an unsalted single-round SHA-256 hash (trivially cracked from any DB read, e.g. via the AI SQL tool finding), plus an authentication path that bypasses the CRM bcrypt AND its login rate limit || Ship .env.example with false, and make both the seed and the local login branch refuse to run when ENVIRONMENT == production regardless of the flag
LOW || MEDIUM || ssrf-credential-forwarding || conversas/app/services/meta_templates.py || 188 || 196 || _fetch_meta_templates || Precondition: control of the Graph API response body (a compromised/misrouted graph.facebook.com, or an admin who sets meta_api_version to a value that redirects). The pagination loop RE-SENDS the Authorization Bearer Meta token to WHATEVER URL appears in paging.next, with no host validation and no redirect policy. The same pattern exists in whatsapp.download_media_content || next_url = data.get("paging", {}).get("next") inside while next_url with client.get(next_url, headers=headers) || The long-lived Meta Business token is forwarded to a host chosen by the UPSTREAM RESPONSE rather than by the application || Validate the host before reusing the credential; disable redirect following

AGENT NOTES — DEFENCES THAT ARE GENUINELY WELL BUILT (recorded so they are not regressed): media_storage.resolve_local_file (confinement by is_relative_to); server-side-only media filenames; hmac.compare_digest in BOTH the Meta webhook and the internal AI verifier; the fail-closed empty-secret branch in verify_internal_signature; escapeHtml applied consistently in conversas.js message/list rendering; campo_personalizado_match using autoescape=True against LIKE metacharacters; bindparam(expanding=True) throughout conversas/services/crm.py; and _safe_color hex validation before a value reaches a style attribute.
The /api/ai/chat limiter is a SECOND Limiter instance not registered in app.state.limiter. Tracing slowapi: the decorator still evaluates its own limit (the middleware never sets _rate_limiting_complete) and the 429 handler tolerates the mismatch. It WORKS - but it is fragile and shares the Traefik-IP key problem.
NO SQL string interpolation exists anywhere in application code outside the deliberate AI tool; no eval/new Function/document.write; no unsafe deserialisation; no command injection in scripts/. .gitignore correctly covers .env, *.db, uploads/; .env.example contains placeholders only.
THE CRM HAS NO TENANCY MODEL: every authenticated user reads and writes every lead, conversation, note and media asset. That reads as INTENTIONAL for a single-agency CRM, so it is not filed as IDOR - but it means "low-privileged user" is a MUCH WEAKER BOUNDARY than the role field suggests, which AMPLIFIES the AI-tool findings above.
The role-string mismatch between the services is currently handled CORRECTLY by conversas/app/auth.py::is_admin_role, including the deliberate isinstance(role, Enum) unwrap. Worth keeping as-is.
NOT VERIFIED: whether the committed API key is still active in production and which user owns it; whether the deployed n8n has user management/MFA and whether Traefik applies middleware not expressed in compose (no Traefik dynamic config is in this repo); whether META_APP_SECRET is actually set in production (if empty, the webhook fails CLOSED with 500 - an availability, not a bypass, risk); whether the live n8n workflows match the exported JSONs; exploitability of the Content-Disposition filename path in conversas media.py:80-85 (Starlette percent-encodes, which appears to neutralise it); runtime confirmation of the @evil.com SSRF against a live CRM.
