# Bia V2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Construir, em paralelo à V1 em produção, um pipeline de atendimento em que nenhuma decisão de estado ou efeito operacional dependa de interpretação de LLM.

**Architecture:** O backend Conversas passa a conter a máquina de estados, o motor de regras e a execução dos efeitos. A IA é reduzida a duas funções sem efeitos colaterais: interpretar linguagem (saída validada por schema estrito) e redigir a comunicação de uma ação já autorizada. O CRM continua fonte de verdade comercial e ganha uma operação de domínio idempotente. O n8n permanece como borda de IA, sem nenhuma tool de escrita.

**Tech Stack:** Python 3.12 (Conversas) / 3.11 (CRM) · FastAPI · SQLAlchemy (síncrono) · Pydantic v2 · PostgreSQL 16 (prod) / SQLite (dev+CI) · n8n · Gemini

**Spec:** `docs/audit/automacao-2026-08-28/TO-BE-A-B-C.md` (seções A/B/C aprovadas), fundamentado em `docs/audit/automacao-2026-08-28/AS-IS.md` e `docs/audit/automacao-2026-08-28/VALIDACAO-CRUZADA.md`

---

## Global Constraints

Aplicam-se a **toda** task. Um gate que falhe aqui interrompe a fase.

- **A V1 não é alterada.** Nenhum arquivo lido pelo caminho de produção da V1 é modificado nas Fases 0–10, com a única exceção nomeada na Task 0.1 (configuração de logging, aditiva e sem mudança de comportamento). Qualquer necessidade de alterar V1 vira item da trilha HOTFIX e **para a fase**.
- **Nenhum deploy, nenhuma migration executada em produção, nenhuma escrita no n8n.** O plugin n8n é READ-ONLY durante toda a implementação.
- **Branch dedicada:** `feat/bia-v2-2026-08-29`. Nunca `main`. Nenhum merge sem aprovação explícita.
- **Testes:** um arquivo executável por caso, rodado com `python tests/test_<nome>.py`. Não existe `pytest`, `ruff`, `mypy` nem `python -m build` neste projeto — não invoque nenhum deles.
- **Todo teste de Conversas deve conter a string literal `CONVERSAS_DIR`**, ou o CI o roteia para o job errado (`.github/workflows/test.yml` separa por `grep -l CONVERSAS_DIR`).
- **Toda coluna nova é nullable ou tem `server_default`.** `default=` do SQLAlchemy é client-side e não protege INSERT gerado por outro processo (lição já paga em `conversas/app/models/conversation.py:190-195`). **Exceção documentada:** `conversation_events.event_id` e `event_type` (m013) são `NOT NULL` **sem** `server_default`, de propósito, não por esquecimento. Quatro motivos tornam isso seguro para esta tabela especificamente: (1) tabela **nova**, sem processo legado escrevendo nela — não existe INSERT de fora da ORM para quebrar; (2) **escritor único e controlado** — só `registrar_evento()` (`conversas/app/v2/eventos.py`) insere, e ele já valida os dois campos antes de qualquer `db.add()`; (3) os dois campos são **semanticamente obrigatórios** — um evento sem `event_id` ou sem `event_type` não é um evento, é lixo, e um `server_default` teria que inventar um valor sem sentido de negócio só para satisfazer a regra; (4) a proteção que a regra normalmente dá (INSERT externo não quebra o schema) já existe aqui por outro caminho — um INSERT que não fornece os dois campos **deve falhar alto** (`IntegrityError`), não silenciar atrás de um default artificial. Teste: inserir `ConversationEvent` sem `event_type` levanta `IntegrityError` (`tests/test_v2_eventos.py`).
- **Idioma:** comentários, docstrings e mensagens de erro em PT-BR; identificadores de domínio em PT.
- **Código V2 vive sob prefixo `v2_`** em nomes de arquivo, ou em `conversas/app/v2/`, para que o diff contra a V1 seja trivialmente separável.

---

## Contrato de segurança da V1

Como cada fase garante que a produção continua intacta:

| Mecanismo | Como funciona |
|---|---|
| **Arquivos disjuntos** | Todo código novo em `conversas/app/v2/**` e `app/routers/v2_*.py`. A V1 não importa nada de lá. |
| **`state='LEGACY'` em todas as linhas existentes** | O CHECK constraint é guardado por `state IN ('WAITING_HUMAN','HUMAN_ACTIVE')`. Com `'LEGACY'` a guarda avalia `FALSE` determinístico e o CHECK nunca dispara sobre linha da V1. **CONFIRMADO** por análise de lógica de três valores. |
| **Independência semântica, não invisibilidade** | Existe **um único** model `Conversation`, compartilhado. Ao acrescentar as colunas nele, o ORM da V1 **passa a conhecê-las e a carregá-las** em todo SELECT, e a citá-las em todo INSERT com seus defaults. A garantia correta **não** é invisibilidade — é que **nenhum fluxo da V1 lê, escreve ou ramifica sobre `state`, `state_version` ou `closed_at`**. O que permanece verdadeiro sobre exposição: `ConversationResponse` (`conversas/app/schemas/conversation.py:98-137`) não declara esses campos, então eles **não vazam para as respostas da API da V1**. Provado por teste, não por afirmação — ver Task 0.3. |
| **Dry-run por padrão** | `V2_MODE=dry_run` é o default. Nenhum efeito real: não envia ao Meta, não escreve em `conversations`, não chama o CRM. |
| **Endpoint separado** | `/webhook/agent-bia-v2`. O `/webhook/agent-bia` da V1 não é tocado. |
| **Sem escrita no n8n** | Workflows V2 só serão criados após aprovação em fase própria. |

---

## Mapa de fases

| Fase | Entrega | Migration? | Toca V1? | Gate |
|---|---|---|---|---|
| **0** | Observabilidade **do Conversas**: tabela de eventos + logging que sai do processo | m013 (tabela) | **Sim, 1 arquivo** — `conversas/app/main.py`, 1 linha | Eventos gravados e log INFO visível |
| **1** | Contratos Pydantic estritos (`extra="forbid"`) | não | não | Payload malformado levanta, nunca degrada |
| **2** | Motor de triagem determinístico | não | não | 6 casos de triagem passam |
| **3** | Pré-filtro determinístico | não | não | 6 casos de pré-filtro passam |
| **4** | Máquina de estados + `state`, `state_version`, `closed_at` + CHECK | m014 (colunas), m015 (CHECK) | não | CHECK inerte para `LEGACY`; transições ilegais recusadas |
| **5** | Seleção de humano (reuso, sem hardcode) | não | não | `None` ⇒ `WAITING_HUMAN` inalcançável |
| **6** | Operação de domínio de handoff + `handoff_operations` | m016 (tabela) | não | Falha parcial nunca produz `WAITING_HUMAN` |
| **7** | Authorization gate por `state_version` | não | não | Resposta descartada quando versão mudou |
| **8** | IA Interpretadora (sem tools) | não | não | Saída fora do schema é rejeitada |
| **9** | IA Comunicadora + output guard de moeda | não | não | Valor monetário bloqueado deterministicamente |
| **10** | Endpoint V2 + replay/dry-run | não | não | Payload sintético percorre o pipeline sem efeito real |

**Fases 11+ (não cobertas por este plano, cada uma com aprovação própria):** workflows n8n V2 · **operação de promoção `LEGACY → V2`** · shadow mode contra tráfego real · canary · promoção definitiva. Ver "Pré-requisitos da Fase 11+" — cinco itens bloqueiam a ativação sobre tráfego real, e **três deles (P1, P2, P3) exigem desenho e aprovação que este plano não contém**.

---

## Migrations previstas

Nenhuma é executada. Todas seguem o molde de `migrations/m012_conversas_primeira_resposta_humana.py`: `run(engine=None, actions=None)`, gate de alvo, DDL em `engine.begin()`, gate pós-DDL, idempotente, compatível com SQLite e PostgreSQL.

| ID | Objeto | Backfill | Compat. V1 | Rollback |
|---|---|---|---|---|
| **m013** | tabela `conversation_events` | nenhum | total — V1 nunca referencia | `DROP TABLE` |
| **m014** | `conversations.state` (String(32), nullable, `server_default='LEGACY'`), `conversations.state_version` (Integer, `server_default='0'`), `conversations.closed_at` (DateTime(tz), nullable) | `state='LEGACY'` em todas as linhas existentes | total — colunas não declaradas no model da V1 | deixar sem uso (padrão do projeto; **não** `DROP COLUMN`) |
| **m015** | CHECK `ck_conversations_estado_humano`, com `NOT VALID` + `VALIDATE CONSTRAINT`. **Pulada no SQLite** (não existe `ADD CONSTRAINT`) | nenhum | inerte enquanto `state='LEGACY'` | `DROP CONSTRAINT` — metadado, instantâneo |
| **m016** | tabela `handoff_operations`, `UNIQUE(event_id)` | nenhum | total | `DROP TABLE` |

**Backfill de estados fica adiado**, deliberadamente. Motivo CONFIRMADO: nenhuma linha existente pode virar `HUMAN_ACTIVE` honestamente — a V1 apaga `queued_at` na primeira resposta humana, e o dado não é recuperável. E `FILA_SEM_DONO` (`atendente_id` NULL com `queued_at` preenchido) não tem estado correspondente no vocabulário de seis, por ser exatamente o que a invariante proíbe. Reconciliar essas linhas é decisão de negócio, não script.

---

## Registro de riscos

| # | Risco | Severidade | Mitigação |
|---|---|---|---|
| **R1** | **`HUMAN_ACTIVE` é inalcançável sem alterar a V1.** O TO-BE exige `queued_at NOT NULL`; `atendimento.py:233-234` grava `queued_at=None` na primeira resposta humana | **BLOQUEANTE para Fase 11+** | Fases 0–10 rodam em dry-run, onde nenhuma linha real recebe `state`. A preservação de `queued_at` vira **HOTFIX-01**, pré-requisito declarado do shadow mode. O plano não a implementa |
| **R2** | V2 rotula conversa real e um humano responde pela UI da V1 → violação do CHECK → 500 no envio do atendente | ALTA | Consequência direta de R1. Impossível em dry-run. Gate da Fase 11 exige HOTFIX-01 primeiro |
| **R3** | Migration de coluna não roda antes do deploy V2 → `ProgrammingError` na primeira query V2 | ALTA | `create_all()` nunca faz `ALTER`. Gate de deploy (fase futura) verifica a coluna antes de subir. A V1 não é afetada — nunca referencia a coluna |
| **R4** | Testes rodam só em SQLite; o lock `FOR UPDATE` e o CHECK adicionado por ALTER não são exercitados | MÉDIA | Testes de concorrência não dependem de `FOR UPDATE`: exercitam a lógica de `state_version` diretamente. O CHECK é exercitado em SQLite novo, criado por `create_all` com o CHECK no `__table_args__` |
| **R5** | `resolver_atendente_elegivel` devolve `None` → `WAITING_HUMAN` inalcançável e cliente fica sem encaminhamento | MÉDIA | Comportamento **desejado** sob a invariante. Vira `HANDOFF_FAILED` com `error_code='SEM_ATENDENTE_ELEGIVEL'`, conversa permanece em `AI_TRIAGE`, IA não anuncia encaminhamento. Teste obrigatório |
| **R6** | Tabela de eventos cresce sem limite | BAIXA | ≤50 conversas/dia. Sem retenção nesta fase; medir antes de otimizar |
| **R7** | Alterar o logging (Task 0.1) muda comportamento da V1 | BAIXA | Só acrescenta handler e formatter ao root logger do **Conversas**. Nenhum `.info` novo, nenhuma lógica tocada. Verificado por teste que compara o comportamento antes/depois |
| **R8** | **`state_version` não é versionamento autoritativo enquanto a V1 escrever campos operacionais sem incrementá-lo.** `_apply_human_state`, `marcar_atendimento_humano`, `claim`, `assign`, `release` e o reset de reabertura alteram estado operacional e **não** tocam `state_version`. Uma resposta da IA gerada antes dessas escritas passaria pelo gate da Fase 7 achando que nada mudou | **BLOQUEANTE para CANARY** | **Não bloqueia dry-run nem shadow** — ali a V2 não age sobre conversa real, então não há resposta a invalidar. Antes do canary é obrigatório existir garantia de que **qualquer** writer relevante invalide respostas em voo. A solução (trigger de banco, incremento nos writers V1, ou versionamento derivado de hash dos campos operacionais) **exige desenho e aprovação próprios** — não está neste plano. O teste B.4 da Task 0.3 documenta o risco de forma executável |
| **R9** | Conversa real promovida a V2 acidentalmente, ou a base inteira de uma vez | **BLOQUEANTE para CANARY** | `LEGACY → AI_TRIAGE` **nunca** acontece automaticamente. Exige a operação explícita descrita em "Promoção LEGACY → V2" abaixo. Nas Fases 0–10 nenhuma linha real recebe `state` diferente de `LEGACY` |
| **R10** | **A coluna `state` não é autoritativa enquanto writers V1 puderem alterar o estado operacional sem passar pela máquina de estados V2.** Generaliza o R8: o problema não é só `state_version` ficar defasado — é `state` **divergir do estado operacional real** | **BLOQUEANTE para CANARY** | Ver análise abaixo |
| **R12** | **`commit=False` não é honrado em SQLite.** pysqlite opera em *legacy transaction mode* e **não emite `BEGIN` antes de um `SAVEPOINT`** — então o savepoint inicia sua própria transação e o `RELEASE` do savepoint mais externo **comita**. Como `registrar_evento()` usa `begin_nested()` para capturar duplicata de `event_id`, em SQLite a linha já está gravada quando a função retorna, mesmo com `commit=False`. **CONFIRMADO** pela doc oficial do SQLAlchemy (*"SAVEPOINT statements emitted before a BEGIN fail to properly participate in the enclosing transaction"*) e por prova empírica: sem a receita, `rows after rollback: 1`; com ela, `0`. **Achado adicional (Task 0.2, segundo follow-up):** o comportamento em SQLite não é nem sequer estável dentro do próprio SQLite — é dependente da ORDEM/estado da sessão. Um `begin_nested()` anterior que falha (ex.: colisão de `event_id`) e é capturado sem um `db.rollback()` explícito subsequente deixa `db.in_transaction()` em `True`; o *próximo* `begin_nested()` na mesma sessão então passa a nascer dentro dessa transação "fantasma" em vez de nu, e a divergência deixa de se manifestar — não porque foi corrigida, mas por acidente de quais testes rodaram antes. Prova empírica registrada em `tests/test_v2_eventos.py` (comentário acima do `db.rollback()` explícito entre os checks 7b e 8) | **dev/CI apenas — PostgreSQL não é afetado.** Bloqueia qualquer teste que dependa de compor eventos numa transação (Fase 6) | **Antes da Fase 6, a semântica transacional de `registrar_evento(commit=False)` precisa ser PROVADA contra PostgreSQL real/efêmero** — nunca presumida a partir do comportamento em SQLite, que o achado acima mostra ser instável até dentro do próprio SQLite.<br>— **Opção A — teste de integração contra PostgreSQL real/efêmero. PREFERIDA.**<br>— **Opção B — correção aprovada do comportamento em SQLite (HOTFIX-09).**<br>— A Opção B **não** deve ser escolhida só para fazer o CI em SQLite imitar produção.<br>Registrado como **gate da Fase 6** (ver seção da Fase 6). `database.py` permanece INTOCADO por esta task; nenhum listener foi instalado. HOTFIX-09 deixa de ser prerequisito automático e passa a ser UMA das duas opções acima — não mais "a correção".<br>**Atualizado no closing de findings pós-`f14cbff`:** o mecanismo do eager-BEGIN é PROVADO, mas continua NÃO fixado a nenhum call site de execução longa confirmado (ver `.claude/memory/pysqlite-savepoint-rollback-gap.md`). `app/services/lead_creation.py:326,359` (`garantir_entrada_no_funil`, `_obter_ou_criar_tag`) já usa `begin_nested()` em CRM V1 **hoje**, num caminho cuja temporização transacional o fix MUDARIA — não é código hipotético. `whatsapp.py` e `meta_templates.py` combinam parâmetro `db: Session` com chamadas `httpx` e não foram auditados individualmente. `crm.py` NÃO combina os dois — verificado (`grep -in "httpx\|requests" conversas/app/services/crm.py` = zero ocorrências em 780 linhas) e o docstring do próprio módulo declara que ele consulta o PostgreSQL compartilhado diretamente, sem chamada HTTP |
| **R11** | **Habilitar o root em INFO expõe PII do cliente no log.** A revisão de segurança da Task 0.1 **CONFIRMOU** que as chamadas `.info` existentes registram nome, telefone completo e os primeiros 50 caracteres da mensagem (`webhook.py:616,626,689`, `conversations.py:489,1347`, `crm.py:279`, entre ~79 chamadas). Nenhum segredo foi encontrado abaixo de WARNING; a exposição é de PII | **local/dry-run: não bloqueia · staging com dados sintéticos: não bloqueia · PRODUÇÃO: BLOQUEANTE** | **DEPLOY-GATE-01** (abaixo). A injeção de linha de log já foi resolvida no formatter (`FormatadorSeguro`); a PII **não**, e não deve ser atacada por regex genérica |
| **R13** | **`conversation_events` guarda identificadores persistentes relacionáveis a pessoa (`whatsapp_msg_id`, `conversation_id`/`lead_id`/`target_user_id`) e não tem política de retenção/expurgo.** | BAIXA | Hoje não bloqueia desenvolvimento — a tabela não tem tráfego real (dry-run, Fases 0-10). **Antes de produção com eventos reais:** definir política de retenção, quem pode consultar a tabela, procedimento de expurgo/anonimização, e revisão LGPD/segurança. **Não implementar TTL arbitrário agora** — retenção é decisão de negócio/compliance, não escolha técnica unilateral |

---

## File Structure

### Arquivos novos

```
conversas/app/v2/__init__.py
conversas/app/v2/eventos.py          # DomainEvent + registrar_evento()
conversas/app/v2/contratos.py        # todos os modelos Pydantic estritos
conversas/app/v2/triagem.py          # avaliar_triagem() — determinístico
conversas/app/v2/prefiltro.py        # avaliar_prefiltro() — determinístico
conversas/app/v2/estados.py          # Estado, transições permitidas, aplicar_transicao()
conversas/app/v2/selecao.py          # selecionar_atendente() — envolve o reuso
conversas/app/v2/handoff.py          # handoff_to_human() — operação de domínio
conversas/app/v2/gate.py             # authorization gate por state_version
conversas/app/v2/output_guard.py     # filtro determinístico de moeda
conversas/app/v2/interpretador.py    # cliente da IA Interpretadora
conversas/app/v2/comunicador.py      # cliente da IA Comunicadora
conversas/app/v2/pipeline.py         # orquestra as etapas
conversas/app/models/evento.py       # ConversationEvent
conversas/app/models/handoff_op.py   # HandoffOperation
conversas/app/routers/v2_agent.py    # POST /webhook/agent-bia-v2
conversas/app/logging_config.py      # dictConfig
app/routers/v2_lead_domain.py        # operação comercial idempotente do CRM
migrations/m013_conversation_events.py
migrations/m014_conversas_state_columns.py
migrations/m015_conversas_check_estado_humano.py
migrations/m016_handoff_operations.py
tests/test_v2_eventos.py
tests/test_v2_contratos.py
tests/test_v2_triagem.py
tests/test_v2_prefiltro.py
tests/test_v2_estados.py
tests/test_v2_selecao.py
tests/test_v2_handoff.py
tests/test_v2_gate.py
tests/test_v2_output_guard.py
tests/test_v2_pipeline.py
tests/test_v2_v1_intacta.py          # guarda de regressão da V1
```

### Arquivos existentes tocados

| Arquivo | Alteração | Fase | Risco V1 |
|---|---|---|---|
| `conversas/app/main.py` | chamar `configurar_logging()`; importar os models novos (`# noqa: F401`) para `create_all()` criá-los | 0, 4 | Aditivo. Nenhuma rota alterada |
| `app/main.py` | registrar `v2_lead_domain` e chamar o `configurar_logging()` **próprio do CRM** | **6** (não 0) | Aditivo |
| `conversas/app/models/conversation.py` | acrescentar `state`, `state_version`, `closed_at` e o `CheckConstraint` em `__table_args__` | 4 | **Único ponto de contato com model da V1.** Colunas nullable / com `server_default`. A V1 **passa a carregá-las** — a garantia é independência semântica, provada por teste, não invisibilidade |

Nenhum outro arquivo da V1 é modificado. `atendimento.py`, `outbound.py`, `webhook.py`, `conversations.py`, `crm.py`, `leads.py`, `lead_creation.py`, `conversas_bridge.py` permanecem **byte-idênticos**.

---

## Gates por fase

Todos precisam passar antes de avançar:

1. **Testes da fase passam** — `python tests/test_v2_<nome>.py`, um processo por arquivo.
2. **`tests/test_v2_v1_intacta.py` passa** — guarda de regressão que prova que os arquivos V1 não mudaram.
3. **Suíte de Conversas continua verde** — os 39 arquivos com `CONVERSAS_DIR`, rodados um a um.
4. **Revisores obrigatórios** — `code-reviewer`, `security-reviewer`, `python-reviewer`, `fastapi-reviewer`, com os critérios do skill `uncle-bob-craft`.
5. **Diff revisado** — nenhum arquivo V1 no diff além dos três autorizados na tabela acima.
6. **Riscos novos registrados** neste documento.

---

# FASE 0 — Observabilidade

Primeira porque, sem ela, nenhuma fase seguinte é verificável. É também o item de maior retorno da auditoria: as chamadas `.info` já existem e hoje não saem do processo.

**Elas não foram auditadas quanto a conteúdo.** A revisão de segurança da Task 0.1 confirmou que várias registram nome, telefone completo e trecho da mensagem do cliente. Tornar o INFO observável é ganho de diagnóstico e, ao mesmo tempo, exposição nova de PII — ver R11 e DEPLOY-GATE-01. O que esta fase entrega é **visibilidade**, não logs prontos para produção.

### Task 0.1: Logging que efetivamente sai do processo — **somente Conversas**

**Files:**
- Create: `conversas/app/logging_config.py`
- Modify: `conversas/app/main.py` (chamar `configurar_logging()` no topo)
- Test: `tests/test_v2_logging.py`

**Interfaces:**
- Produces: `configurar_logging(nivel: str | None = None) -> None`, importável **apenas** de dentro do pacote `app` do Conversas.

**Contexto:** Nenhum dos dois `main.py` chama `basicConfig`/`dictConfig`; os Dockerfiles sobem uvicorn sem `--log-config`; o `LOGGING_CONFIG` do uvicorn não define a chave `"root"`. Root logger fica em WARNING sem handler. **CONFIRMADO empiricamente.**

**⚠ Restrição de pacote — motivo da Fase 0 cobrir só o Conversas.** CRM (`app/`) e Conversas (`conversas/app/`) são **dois pacotes independentes que se chamam `app`**. Um `from app.logging_config import ...` dentro do CRM resolveria para `app/logging_config.py` do CRM, não para o do Conversas. **Não existe import compartilhado entre os dois** — é a mesma parede de pacote que a auditoria confirmou como causa da duplicação de `criar_lead` e da normalização de telefone.

Consequências, deliberadas:
- A Fase 0 configura o logging **apenas do Conversas**, onde a V2 vive.
- O CRM recebe o seu **próprio** `app/logging_config.py`, independente e duplicado, na **Fase 6**, quando passar a hospedar `v2_lead_domain.py` e houver o que registrar. Duplicar é a opção correta aqui: compartilhar exigiria mudar a fronteira de pacotes, o que está fora do escopo da V2.
- O logging do CRM para fins da **V1** é item da trilha HOTFIX (**HOTFIX-08**), não desta fase.

- [ ] **Step 1: Escrever o teste que falha**

```python
# tests/test_v2_logging.py
"""Prova que o logging INFO sai do processo. CONVERSAS_DIR marker."""
import io
import logging
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parents[1] / "conversas"))

failures = []

def check(cond, msg):
    if cond:
        print(f"OK  {msg}")
    else:
        print(f"FAIL {msg}")
        failures.append(msg)

from app.logging_config import configurar_logging

configurar_logging()
buffer = io.StringIO()
handler = logging.StreamHandler(buffer)
handler.setFormatter(logging.Formatter("%(levelname)s|%(name)s|%(message)s"))
logging.getLogger().addHandler(handler)

logging.getLogger("app.teste").info("mensagem-de-info")
saida = buffer.getvalue()

check("mensagem-de-info" in saida, "INFO chega ao handler do root logger")
check("INFO|app.teste" in saida, "formatter inclui nivel e nome do logger")
check(logging.getLogger().level <= logging.INFO, "root logger em INFO ou mais permissivo")

sys.exit(1 if failures else 0)
```

- [ ] **Step 2: Rodar e confirmar que falha**

Run: `python tests/test_v2_logging.py`
Expected: FAIL — `ModuleNotFoundError: No module named 'app.logging_config'`

- [ ] **Step 3: Implementar o mínimo**

```python
# conversas/app/logging_config.py
"""Configuracao de logging — WP BIA-V2 Fase 0.

Sem isto, o root logger fica no default do Python (WARNING, sem handler) e
todas as chamadas .info do sistema morrem dentro do processo. As chamadas ja
existem; falta o handler. O CONTEUDO delas nao foi auditado — ver R11.
"""
import logging
import os
from logging.config import dictConfig

_FORMATO = "%(asctime)s %(levelname)s %(name)s %(message)s"


def configurar_logging(nivel: str | None = None) -> None:
    """Instala handler e formatter no root logger. Idempotente."""
    nivel_efetivo = (nivel or os.getenv("LOG_LEVEL", "INFO")).upper()
    dictConfig(
        {
            "version": 1,
            "disable_existing_loggers": False,
            "formatters": {"padrao": {"format": _FORMATO}},
            "handlers": {
                "stdout": {
                    "class": "logging.StreamHandler",
                    "stream": "ext://sys.stdout",
                    "formatter": "padrao",
                }
            },
            "root": {"handlers": ["stdout"], "level": nivel_efetivo},
        }
    )
    logging.getLogger(__name__).info("logging configurado em %s", nivel_efetivo)
```

- [ ] **Step 4: Rodar e confirmar que passa**

Run: `python tests/test_v2_logging.py`
Expected: PASS — três linhas `OK`

- [ ] **Step 5: Ligar no main.py do Conversas — e só nele**

Em `conversas/app/main.py`, imediatamente após os imports e **antes** de qualquer outra inicialização:

```python
from app.logging_config import configurar_logging

configurar_logging()
```

**Não** adicione nada a `app/main.py` (CRM) nesta task. O import acima resolve para `conversas/app/logging_config.py` **porque o processo do Conversas tem `conversas/` no `sys.path`**. Dentro do processo do CRM, o mesmo texto resolveria para outro arquivo, inexistente.

- [ ] **Step 6: Provar que a V1 não mudou de comportamento**

Run, um por vez: `python tests/test_conversas_queue.py`, `python tests/test_conversas_webhook_hardening.py`, `python tests/test_conversas_operational_state.py`
Expected: todos PASS, sem alteração no resultado

- [ ] **Step 7: Commit**

```bash
git add conversas/app/logging_config.py tests/test_v2_logging.py conversas/app/main.py
git commit -m "feat(v2): logging INFO observavel — o handler que faltava no root"
```

### Task 0.2: Tabela de eventos e `registrar_evento()`

**Files:**
- Create: `conversas/app/models/evento.py`, `conversas/app/v2/__init__.py`, `conversas/app/v2/eventos.py`, `conversas/app/v2/eventos_validacao.py`, `migrations/m013_conversation_events.py`
- Modify: `conversas/app/main.py` (import do model para `create_all()`), `migrations/README.md`
- Test: `tests/test_v2_eventos.py`, `tests/test_v2_eventos_validacao.py`

**Interfaces:**
- Produces: `registrar_evento(db, *, tipo, commit, ...) -> ConversationEvent` e `EventoDuplicado` em `eventos.py` (persistência); `TipoEvento`, `ResultadoEvento`, `OrigemEvento`, `CampoInvalido`, `PayloadInvalido` e todos os `validar_*` em `eventos_validacao.py` (contrato).
- Direção de dependência: **persistência → validação, nunca o contrário.** `TipoEvento` mora na validação porque `validar_tipo` precisa dele para coagir; deixá-lo na persistência fecharia um ciclo de import real.

**Steps:** teste primeiro (evento gravado com todos os campos; `event_id` duplicado recusado por `UNIQUE`; evento sem `conversation_id` aceito), depois model, depois função, depois migration idempotente no molde do `m012`, depois commit.

Campos obrigatórios da tabela, conforme o briefing: `event_id` (String(36), unique, not null), `event_type`, `conversation_id`, `lead_id`, `message_id`, `whatsapp_msg_id`, `state_before`, `state_after`, `action`, `target_user_id`, `model`, `model_attempt`, `duration_ms`, `result`, `error_code`, `payload` (JSON), `created_at` (`server_default=func.now()`).

Os 18 tipos: `MESSAGE_RECEIVED`, `MESSAGE_IGNORED`, `AI_INTERPRETATION_STARTED`, `AI_INTERPRETATION_COMPLETED`, `AI_INTERPRETATION_FAILED`, `TRIAGE_DATA_UPDATED`, `TRIAGE_COMPLETED`, `HANDOFF_REQUESTED`, `HUMAN_SELECTED`, `HANDOFF_COMPLETED`, `HANDOFF_FAILED`, `HUMAN_FIRST_RESPONSE`, `AI_RESPONSE_GENERATED`, `AI_RESPONSE_DISCARDED`, `MESSAGE_SENT`, `MESSAGE_SEND_FAILED`, `CONVERSATION_CLOSED`, `CONVERSATION_REOPENED`.

### Task 0.3: Guarda de regressão da V1

**Files:** Create `tests/test_v2_v1_intacta.py`

Duas famílias de verificação. É o mecanismo que transforma "não toque na V1" de intenção em prova.

**A. Imutabilidade de arquivo.** Hash SHA-256 dos arquivos V1 que **não podem** mudar: `atendimento.py`, `outbound.py`, `webhook.py`, `conversations.py`, `crm.py`, `leads.py`, `lead_creation.py`, `conversas_bridge.py`. Falha se algum divergir.

**B. Independência semântica das colunas V2** — substitui a afirmação errada de "invisibilidade". A partir da Fase 4, provar que:

1. **Nenhum fluxo V1 lê as colunas novas.** `grep` por `state`, `state_version` e `closed_at` nos arquivos V1 acima retorna zero ocorrências fora de comentário. (`state` exige regex de palavra inteira — `status` e `_STATUS_ABERTOS` não contam.)
2. **A API da V1 não expõe os campos.** Um `GET /api/conversations/{id}` sobre conversa com `state='AI_TRIAGE'`, `state_version=7`, `closed_at` preenchido devolve JSON **sem** essas três chaves — porque `ConversationResponse` não as declara.
3. **A V1 opera normalmente sobre linha rotulada pela V2.** Uma conversa com `state='AI_TRIAGE'` aceita todo o ciclo da V1 — listar no inbox, `claim`, `assign`, `release`, enviar mensagem, encerrar — sem erro e com o mesmo resultado de uma linha `LEGACY`.
4. **A V1 nunca incrementa `state_version`.** Após executar `claim` + `assign` + `release` + envio de mensagem pela V1, `state_version` permanece **inalterado**. Este teste não é uma verificação de saúde: é a **prova do risco R8** e falha propositalmente se alguém tentar resolver R8 sem novo desenho.
5. **O CHECK é inerte para `LEGACY`.** Gravar uma linha `state='LEGACY'` com `atendente_id != responsavel_id`, `is_bot_active=True` e `queued_at=NULL` é aceito pelo banco.

---

# FASE 1 — Contratos

Modelos Pydantic v2 com `model_config = ConfigDict(extra="forbid")`. Nenhum payload malformado degrada em silêncio.

**Files:** Create `conversas/app/v2/contratos.py`, `tests/test_v2_contratos.py`

Modelos: `InboundContextV2`, `AIInterpretationRequest`, `AIInterpretationResult`, `TriageData`, `DestinoTriagem`, `RuleEngineInput`, `RuleEngineDecision`, `AuthorizedResponse`, `HandoffRequest`, `HandoffResult`, `DomainEvent`.

Mais os tipos de retorno consumidos pelas fases seguintes, definidos aqui para que nenhuma fase invente o seu: `ResultadoTriagem` (`.completa: bool`, `.campos_faltantes: list[str]`) — Fase 2 · `MensagemLote` e `DecisaoPrefiltro` (`.ignorar: bool`, `.motivo: str | None`) — Fase 3 · `ResultadoGuard` (`.permitido: bool`, `.motivo: str | None`, `.texto_final: str`) — Fase 9.

`AIInterpretationResult` **não tem** campo `pronto_para_humano`. Tem `intent`, `explicit_human_request: bool`, `extracted: TriageData`, `duvidas: list[str]`. Um payload contendo `pronto_para_humano` levanta `ValidationError` — e há teste explícito para isso, porque é a regressão que a arquitetura inteira existe para impedir.

**Testes:** campo extra levanta · campo obrigatório ausente levanta · tipo errado levanta · `pronto_para_humano` levanta · payload válido constrói.

---

# FASE 2 — Triagem determinística

**Files:** Create `conversas/app/v2/triagem.py`, `tests/test_v2_triagem.py`

**Interface:** `avaliar_triagem(dados: TriageData) -> ResultadoTriagem` com `.completa: bool` e `.campos_faltantes: list[str]`. Função **pura**, sem `db`, sem IO.

**Regra aprovada — o modo é GLOBAL, não por destino.**

Campos base: `nome`, `email`, ≥1 destino, `total_pessoas`, `adultos`, `criancas`, e `adultos + criancas == total_pessoas`.

Além disso, o campo `datas_definidas` determina **um único modo para toda a viagem**:

| `datas_definidas` | Exigência |
|---|---|
| `True` | **TODOS** os destinos precisam de `data_inicio` **e** `data_fim` |
| `False` | **TODOS** os destinos precisam de `dias` |

**Modo misto é proibido.** Um destino com datas e outro com dias é triagem **incompleta**, mesmo que cada destino esteja individualmente preenchido. A viagem tem um modo só.

`quantidade_dias_pretendida` permanece no schema como fato extraível, mas **não satisfaz completude** em nenhum dos dois modos.

**Casos de teste obrigatórios:**

| # | Entrada | Esperado |
|---|---|---|
| 1 | nome, email, 2 adultos, 0 crianças, total 2, Atacama com datas, `datas_definidas=True` | `completa=True` |
| 2 | nome, email, total 3, 2 adultos, 1 criança, Atacama com `dias=5`, `datas_definidas=False` | `completa=True` |
| 3 | total 3, adultos 2, crianças 0 | `completa=False`, soma incoerente |
| 4 | Atacama+Uyuni, `datas_definidas=True`, datas só no Atacama | `completa=False`, falta período de Uyuni |
| 5 | Atacama+Uyuni, `datas_definidas=False`, só `quantidade_dias_pretendida=8` | `completa=False`, falta `dias` em ambos |
| 6 | tudo menos email | `completa=False`, `campos_faltantes=["email"]` |
| 7 | zero destinos | `completa=False` |
| 8 | `adultos=2, criancas=1, total=4` | `completa=False`, soma incoerente |
| **9** | **Atacama com datas + Uyuni com dias** | **`completa=False` — MODO MISTO PROIBIDO** |
| **10** | **Atacama+Uyuni ambos com datas, `datas_definidas=True`** | **`completa=True`** |
| **11** | **Atacama+Uyuni ambos só com `dias`, `datas_definidas=False`** | **`completa=True`** |
| **12** | `datas_definidas=True` mas destino só tem `dias` | `completa=False` — modo exige datas |
| **13** | `datas_definidas=False` mas destino só tem datas | `completa=False` — modo exige dias |

Os casos 9 a 13 existem porque o modo misto é a falha mais provável na prática: o cliente diz "Atacama de 10 a 15 de março, e uns 4 dias no Uyuni". A triagem **não** fecha aí.

---

# FASE 3 — Pré-filtro

**Files:** Create `conversas/app/v2/prefiltro.py`, `tests/test_v2_prefiltro.py`

**Interface:** `avaliar_prefiltro(lote: list[MensagemLote]) -> DecisaoPrefiltro` com `.ignorar: bool` e `.motivo: str | None`. Pura.

**Regras absolutas** (ignorar, sem chamar IA): reação isolada · sticker isolado · emoji isolado · mensagem vazia. **Se o lote contiver qualquer conteúdo substantivo, continua** — a avaliação é sobre o lote, não sobre a mensagem.

**Nunca ignorar automaticamente:** `ok`, `valeu`, `obrigado`, `sim`, `não`, `beleza`, `perfeito`, `entendi`. Seguem para interpretação contextual.

**Casos:** `👍` ignora · `❤️🙏` ignora · sticker ignora · reação ignora · `👍` + "Quero viajar em janeiro" continua · "obrigado" continua · lote vazio ignora · reação + "Somos 3 pessoas" continua.

---

# FASE 4 — Máquina de estados

**Files:** Create `conversas/app/v2/estados.py`, `migrations/m014_*.py`, `migrations/m015_*.py`, `tests/test_v2_estados.py`; Modify `conversas/app/models/conversation.py`

**Interface:** `Estado` (StrEnum: `NEW`, `AI_TRIAGE`, `WAITING_HUMAN`, `HUMAN_ACTIVE`, `CLOSED`, `LEGACY`), `TRANSICOES_PERMITIDAS: dict[Estado, set[Estado]]`, `aplicar_transicao(conversation, destino, *, motivo) -> None` (incrementa `state_version`, valida invariantes, levanta `TransicaoInvalida`).

O estado da **tentativa de handoff não vive aqui** — vive em `handoff_operations` (Fase 6), conforme a instrução explícita do briefing.

**Reabertura**, regra aprovada, decidida pelo motor e não por quem escreve primeiro. Janela configurável, default 24h, medida por `closed_at` próprio — **não** por `service_window_open`:

| Situação | Destino |
|---|---|
| Dentro da janela, dono anterior ainda elegível | `HUMAN_ACTIVE`, mesmo dono, bot OFF |
| Dentro da janela, dono anterior inelegível | `WAITING_HUMAN` com novo dono — **exige `handoff_operation` completa**, pois muda o responsável no CRM |
| Fora da janela, ou sem dono humano anterior | `AI_TRIAGE` |

**Testes:** cada transição permitida · cada transição proibida levanta · `state_version` incrementa em toda transição · CHECK inerte para `state='LEGACY'` · CHECK rejeita `WAITING_HUMAN` com `atendente_id != responsavel_id` · os três casos de reabertura.

---

# FASES 5 a 10 — definição estrutural

O detalhamento passo a passo destas fases é escrito quando a fase é alcançada, porque o código exato depende das interfaces que as Fases 0–4 estabelecerem. As interfaces, arquivos, testes e riscos abaixo já estão fixados e são o contrato de cada uma.

### Fase 5 — Seleção de humano
`conversas/app/v2/selecao.py` · `selecionar_atendente(db) -> int | None` envolve `resolver_atendente_elegivel(db)` sem alterá-la. Nenhum id hardcoded. **Teste crítico:** `None` ⇒ `WAITING_HUMAN` inalcançável, `HANDOFF_FAILED` com `error_code='SEM_ATENDENTE_ELEGIVEL'`, conversa fica em `AI_TRIAGE`.

### Fase 6 — Handoff

**Pré-requisito (gate) — R12:** esta fase é a primeira a compor `registrar_evento()` dentro de uma transação maior (`handoff_operations` + estado local + evento, na mesma unidade atômica via `commit=False`). Antes de iniciar a implementação, a semântica transacional de `registrar_evento(commit=False)` precisa estar PROVADA contra PostgreSQL real/efêmero — não presumida do comportamento em SQLite (ver R12 no Registro de riscos, achado adicional da Task 0.2: o comportamento em SQLite é instável até dentro do próprio SQLite, dependente da ordem dos testes). Opção A (teste de integração PostgreSQL, preferida) ou Opção B (HOTFIX-09, aprovado explicitamente) — nunca Opção B só para o CI em SQLite imitar produção.

**Segunda metade do mesmo gate — o caminho que a prova de rollback NÃO cobre.** Provar que `commit=False` é *desfazível* não protege contra o autor da fase simplesmente **esquecer o `db.commit()` final**. `get_db()` (`conversas/app/database.py:28-34`) só faz `db.close()` no `finally`: **não commita em sucesso e não faz rollback explícito**. Uma operação composta que chama `registrar_evento(commit=False)` e não fecha a transação **some em silêncio** quando a sessão fecha no fim do request — sem exceção, sem log, nenhum dado gravado.

Regra, portanto: **todo router ou serviço que compõe `registrar_evento(commit=False)` com outras escritas é o dono explícito da transação e deve chamar `db.commit()` ao final da unidade atômica.** É a extensão, para este caso novo, da convenção de fronteira de commit por camada já documentada no `CLAUDE.md` raiz.

`conversas/app/v2/handoff.py` + `app/routers/v2_lead_domain.py` + `migrations/m016`.
`handoff_to_human(db, *, conversation_id, target_user_id, reason, event_id) -> HandoffResult`.
Um único `target_user_id` alimenta `lead.responsavel_id`, `conversation.atendente_id` e `conversation.responsavel_id` — o contrato não aceita valores diferentes.
Sequência: registra `handoff_operations` (`REQUESTED`) → chama o CRM com `event_id` como idempotency key → confirma → aplica estado local → **verifica as seis invariantes** → só então `COMPLETED` + `WAITING_HUMAN`.
**Testes:** sucesso duplo · CRM falha ⇒ nunca `WAITING_HUMAN` · mesmo `event_id` duas vezes ⇒ mesmo resultado, zero efeito duplicado · crash após o CRM ⇒ reconciliação encontra a operação `PENDING` e completa · atendente X ⇒ responsável obrigatoriamente X.

### Fase 7 — Authorization gate
`conversas/app/v2/gate.py` · `autorizar_envio(db, *, conversation_id, state_esperado, version_esperada) -> bool`. Relê sob lock; recusa se `state_version` mudou, se o bot não pode mais falar, se um humano assumiu, ou se a janela Meta fechou. Emite `AI_RESPONSE_DISCARDED`.
**Teste do briefing:** IA começa em `state_version=10`, humano assume (`11`), IA termina ⇒ resposta descartada. Não depende de `FOR UPDATE`, para funcionar em SQLite.

### Fase 8 — IA Interpretadora
`conversas/app/v2/interpretador.py` · sem tools de escrita. Saída validada por `AIInterpretationResult`. Saída inválida ⇒ `AI_INTERPRETATION_FAILED`, nunca degradação.

### Fase 9 — IA Comunicadora e output guard
`conversas/app/v2/comunicador.py` + `conversas/app/v2/output_guard.py`.
Entrada: `allowed_action` + fatos confirmados + campos faltantes. A IA escolhe **como** dizer; o sistema escolheu **o quê**.
`filtrar_saida(texto: str, *, acao: str) -> ResultadoGuard` bloqueia valor monetário deterministicamente — símbolo, código de moeda e valor por extenso, cobrindo a lacuna confirmada de que a sanitização atual só cobre o campo `content` da KB.
**Testes:** `R$ 5.000` bloqueado · `5000 reais` bloqueado · `cinco mil reais` bloqueado · `USD 300` bloqueado · texto sem valor passa · ação não autorizada bloqueada.

### Fase 10 — Endpoint e pipeline
`conversas/app/routers/v2_agent.py` · `POST /webhook/agent-bia-v2`. `V2_MODE` default `dry_run`. Suporte a payload sintético e replay. Nenhum efeito real por padrão.
**Teste:** payload sintético percorre o pipeline inteiro, gera a cadeia de eventos esperada, e **nenhuma linha de `conversations` é escrita**.

**Registrado agora, implementado só na Fase 10 (nenhuma rota existe ainda):**
- `EventoDuplicado`/`CampoInvalido`/`PayloadInvalido` (`conversas/app/v2/eventos.py` e `eventos_validacao.py`) precisam de mapeamento HTTP deliberado antes de poderem escapar por qualquer endpoint V2 — nenhuma exceção de domínio pode virar 500 genérico por acidente. **O mapeamento é fixado aqui**, para que dois implementadores não escolham status diferentes e ambos continuem "dentro do plano":
  - `EventoDuplicado` → **409 Conflict** — é colisão de idempotency key, não erro de validação do cliente.
  - `CampoInvalido` e `PayloadInvalido` → **422 Unprocessable Entity** — mesma semântica do `RequestValidationError` nativo do FastAPI; ambas são violação de forma/contrato.
  **Onde registrar, sem ambiguidade:** via `add_exception_handler`/`@app.exception_handler` na instância `FastAPI()` **raiz** de `conversas/app/main.py` (a variável `app` já existente ali) — **nunca** num `APIRouter` (ex.: o `router` de `v2_agent.py`). `@app.exception_handler`/`add_exception_handler` só disparam quando registrados no app raiz; um handler registrado num `APIRouter` **nunca é chamado**, mesmo para uma rota que pertence àquele router — cairia direto no 500 default do Starlette, exatamente o caso que este mapeamento existe para evitar.
- Todo campo que alimenta `registrar_evento()` a partir de payload externo (n8n, Meta) precisa ser normalizado `"" -> None` na fronteira de ingestão, **antes** da chamada. Desde o closing de findings pós-`f14cbff`, `validar_model`/`validar_whatsapp_msg_id` rejeitam string vazia (e string só-espaço) com `CampoInvalido` — logo **422**, não aceitação silenciosa — então repassar `""` de um payload upstream sem normalizar quebraria o request. Precedente já existe na própria V1: `conversas/app/routers/webhook.py:504` já faz `msg_id = msg.get("id") or None`, com comentário explicando que `""` colidiria no UNIQUE de `whatsapp_msg_id`. Mesma normalização, mesmo motivo — agora se aplica a todo campo opcional de `registrar_evento()` alimentado por payload externo, não só a `whatsapp_msg_id`.
- A `Session` síncrona do SQLAlchemy não pode ser usada com I/O bloqueante direto dentro de um `async def` novo da V2. A Fase 10 decide entre uma rota síncrona (`def`, que o FastAPI já roda em threadpool) ou execução explícita em threadpool — nunca `Session` síncrona chamada direto de dentro de `async def`.

---

## DEPLOY-GATE-01 — Logging / PII

**Nenhum deploy que contenha a Task 0.1 pode chegar à produção** até existir:

1. **Revisão específica de todas as chamadas `.info`/`.debug` do Conversas** (~79, já
   inventariadas no parecer do security-reviewer), decidindo caso a caso o que pode ser
   registrado.
2. **Política escrita** sobre o que nunca vai ao log: PII (nome, telefone, e-mail),
   conteúdo de mensagem do cliente, tokens e secrets.
3. **Testes** que impeçam a reintrodução — verificando os call sites, não o texto final.

### Por que NÃO resolver com regex genérica no formatter

O `FormatadorSeguro` resolve **injeção de linha de log** — um problema sintático, com resposta
determinística: escapar `\n`/`\r`. PII é semântico e **não tem** resposta determinística por
regex. Uma tentativa de mascarar PII no formatter falharia nos dois sentidos:

- **Falso negativo:** telefone escrito em formato não previsto passa direto, e a equipe passa a
  confiar num filtro que não filtra.
- **Falso positivo:** um id de conversa, um wamid ou um valor legítimo vira `***`, destruindo
  justamente o diagnóstico que a Fase 0 existe para criar.

O lugar certo é o **call site**, onde se sabe o que a variável significa. Isso implica editar
arquivos da V1 — logo, é WP próprio na trilha HOTFIX, com aprovação própria.

### Estado atual

| Ambiente | Situação |
|---|---|
| Local / dry-run | Liberado |
| Staging com dados sintéticos | Liberado |
| **Produção** | **Bloqueado** até os três itens acima |

Não é exposição ativa: nada foi deployado, e os Global Constraints deste plano proíbem deploy.

---

### R10 em detalhe — divergência entre `state` e o estado operacional real

O R8 tratava de `state_version` ficar defasado. O R10 é maior: **`state` pode passar a mentir.**

Os writers da V1 — `claim`, `assign`, `release`, resposta humana (via `marcar_atendimento_humano`), fechamento, reabertura, e ativação/desativação da IA — alteram diretamente:

`is_bot_active` · `atendente_id` · `responsavel_id` · `queued_at` · `primeira_resposta_humana_at` · `status`

Nenhum deles passa pela máquina de estados V2. Numa conversa controlada pela V2, qualquer uma dessas escritas produz uma linha em que `state` afirma uma coisa e os campos operacionais dizem outra. O CHECK constraint **não salva** disso: ele só impede combinações inválidas dentro dos estados guardados, e várias divergências produzem combinações que passam no CHECK e mesmo assim estão erradas — por exemplo `state='WAITING_HUMAN'` após um `release` que zerou `atendente_id` violaria o CHECK e daria erro, mas `state='AI_TRIAGE'` após um `claim` que setou `atendente_id` e desligou o bot **passa** no CHECK e fica silenciosamente errado.

**`closed_at` faz parte do mesmo problema.** Um fechamento executado por writer V1 grava `status='encerrada'` e **não** grava `closed_at`, nem move `state` para `CLOSED`. A conversa fica com `state` afirmando um estado aberto, `status` dizendo encerrada, e `closed_at` nulo. Como a regra de reabertura aprovada mede a janela por `closed_at`, uma conversa nessa condição **reabre sempre como se estivesse fora da janela** — perdendo o dono humano exatamente no caso que a regra existe para proteger.

**Impacto por fase:**

| Fase | Bloqueia? | Por quê |
|---|---|---|
| **DRY-RUN** | Não | Nenhuma conversa real recebe `state` ≠ `LEGACY`. Não há o que divergir |
| **SHADOW** | Não — **condicionado** | Aceitável **desde que nenhuma decisão da V2 produza side effect**. A V2 observa e registra; se `state` divergir, o efeito é ruído no log de comparação, não dano operacional |
| **CANARY** | **BLOQUEANTE** | A V2 age sobre conversa real. Um `state` divergente leva a decisão errada com efeito real |

**Antes do canary**, toda escrita operacional sobre uma conversa controlada pela V2 deverá satisfazer **A** ou **B**:

- **(A)** passar pelo mesmo choke-point / motor de transição da V2; **ou**
- **(B)** possuir mecanismo determinístico equivalente que mantenha `state`, `state_version` e os campos operacionais **atomicamente coerentes**.

**A solução não é escolhida aqui.** Exige desenho e aprovação próprios. Nas Fases 0–10 o risco é inerte por construção.

---

## Promoção LEGACY → V2 — pré-requisito de ativação real

**Uma conversa real nunca passa de `LEGACY` para um estado V2 automaticamente.** Não há caminho de código, migration ou default que produza essa transição por efeito colateral. Fica **fora das Fases 0–10** e é pré-requisito declarado da ativação.

A promoção é uma **operação de domínio própria**, com evento próprio:

```
V2_CONVERSATION_ACTIVATED
LEGACY → AI_TRIAGE
```

Requisitos obrigatórios, cada um verificável:

| Requisito | Como |
|---|---|
| **Controlada por coorte** | Feature flag com lista explícita de `conversation_id` ou `whatsapp`. **Nunca** um predicado que possa casar com a base inteira — sem `UPDATE ... WHERE state='LEGACY'` sem cláusula limitante |
| **Auditável** | Emite `V2_CONVERSATION_ACTIVATED` em `conversation_events`, com quem ativou, quando e sob qual coorte |
| **Idempotente** | Reexecutar sobre conversa já promovida é no-op — não emite segundo evento, não altera `state_version` |
| **Reversível** | `AI_TRIAGE → LEGACY` (despromoção) é suportada e emite `V2_CONVERSATION_DEACTIVATED`, desde que não haja `handoff_operation` em `PENDING` |
| **Impossível em massa** | Teto rígido por execução, e recusa se o tamanho da coorte exceder o limite configurado. O teste que prova isso é obrigatório antes do canary |

**Gate:** a promoção só pode ser habilitada depois que **R1 (HOTFIX-01)**, **R8** e o shadow mode estiverem resolvidos e aprovados. Os três são independentes e todos bloqueiam.

---

## Pré-requisitos da Fase 11+ (shadow, canary, promoção)

Nenhum destes é implementado por este plano. Todos bloqueiam a ativação sobre tráfego real.

| # | Pré-requisito | Bloqueia |
|---|---|---|
| **P1** | **HOTFIX-01** — preservar `queued_at` na primeira resposta humana | Qualquer rotulagem real (R1/R2) |
| **P2** | **R8 resolvido** — garantia de que qualquer writer invalide resposta da IA em voo | Canary |
| **P6** | **R10 resolvido** — toda escrita operacional sobre conversa V2 passa pelo motor de transição (A) ou mantém `state` + `state_version` + campos operacionais atomicamente coerentes (B). Inclui `closed_at`: fechamento por writer V1 não pode deixar `state`/`status`/`closed_at` divergentes | Canary. **Shadow só é liberado se nenhuma decisão da V2 produzir side effect** |
| **P3** | **Operação de promoção LEGACY → V2** implementada e testada | Canary |
| **P4** | **Shadow mode aprovado** em fase própria, com seu gate | Canary |
| **P5** | Critérios objetivos de promoção V2 e procedimento de rollback documentados | Promoção |

---

## Trilha HOTFIX V1 — separada, não implementada aqui

Registrada para não se perder. Cada item é um PR próprio contra a V1, com aprovação própria, **fora** deste plano.

| ID | Item | Por que importa |
|---|---|---|
| **HOTFIX-01** | Preservar `queued_at` na primeira resposta humana | **Pré-requisito de R1/R2.** Sem ele a V2 não pode rotular conversa real |
| HOTFIX-02 | `db.rollback()` faltante em `crm.py:129-135` e `:177-179` | Transação abortada derruba a persistência da mensagem inbound |
| HOTFIX-03 | Reler `is_bot_active` antes do envio pós-240s | Bia fala por cima do humano |
| HOTFIX-04 | `extra="forbid"` em `LeadBase`/`LeadUpdate` | Typo em campo retorna 201 e perde o dado |
| HOTFIX-05 | `UNIQUE` em `leads.whatsapp` | Única proteção real contra lead duplicado |
| HOTFIX-06 | Reset incondicional na reabertura pelo cliente | Apaga o atendente e reativa a IA |
| HOTFIX-07 | `retryOnFail` nos nós de agente do n8n | Reexecuta tools de escrita não-idempotentes |
| **HOTFIX-08** | **Logging do CRM** — `app/logging_config.py` próprio, independente do Conversas | O CRM tem a mesma falha de root logger; fora do escopo da Fase 0, que cobre só o Conversas |
| **HOTFIX-09** | **Receita pysqlite nos dois `database.py`** — `isolation_level=None` no evento `connect` + `BEGIN` manual no evento `begin`, gated por `IS_SQLITE` | Resolve o R12. Afeta **infra compartilhada**: muda o início de transação de lazy para eager em dev/CI, e o caminho de 240s do `_debounce_then_forward` passa a segurar transação real — risco de `database is locked` sob concorrência. **O CRM também precisa**: `lead_creation.py:326,359` usa `begin_nested()` e `app/database.py` tem a mesma lacuna. Exige PR próprio, suíte inteira e revisão do caminho do webhook |

---

## Primeiro commit

Branch `feat/bia-v2-2026-08-29`, a partir do HEAD atual.

**Commit 1 — Task 0.1:** `feat(v2): logging INFO observavel — o handler que faltava no root`

Arquivos: `conversas/app/logging_config.py` (novo), `tests/test_v2_logging.py` (novo), `conversas/app/main.py` (5 linhas). **`app/main.py` do CRM NÃO é tocado nesta fase** — ver a restrição de pacote na Task 0.1; ele entra só na Fase 6.

Escolhido como primeiro porque é a menor mudança que produz valor imediato **para a própria V1** — destrava uma trilha de diagnóstico que já está escrita no código e hoje é muda — sem alterar comportamento algum, e porque todas as fases seguintes dependem de conseguir observar o que fazem.
