# Memory index

- [pysqlite SAVEPOINT rollback gap](pysqlite-savepoint-rollback-gap.md) — `db.begin_nested()` on SQLite in this repo (both `database.py`) doesn't actually undo on a later `db.rollback()`; works fine on PostgreSQL. Also: the SQLite symptom is order-dependent within a session — an uncaught-but-handled prior `begin_nested()` failure masks it for the next one unless you `db.rollback()` first. R12 is explicitly locked/documented by check 9b of `test_v2_eventos.py` — that check is DESIGNED TO PASS while the divergence exists (it asserts `visivel_sem_commit == 1`) and would FAIL if SQLite were fixed without revisiting the record. What remains BLOCKED is Phase 6's transactional composition, until proven against real/ephemeral PostgreSQL or an approved HOTFIX-09 (the fix is NOT a no-op: CRM's `app/services/lead_creation.py` already uses `begin_nested()` on a different, unaffected path today, and the fix changes shared engine-wide transaction semantics for every session).

